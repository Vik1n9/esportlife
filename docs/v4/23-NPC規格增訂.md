# S23 · NPC 規格增訂（§23 細部定案）

狀態：未開始
前置：S21 S22
預估：1 session
推理難度：高
建議模型：Opus 5（`claude-opus-5`）
理由：§23.1 已定案四件事不能推翻，但細部定案動到兩處平衡核心——§11.1 逐選手模型
的掛法（對手五人不均分？明星項替換？）與 §14.3 百分位改法，產出被庚組每一站依賴。

---

## 為什麼做這件事

規格 §23.1 定了四件事（一次到位、去姓名、Liquipedia 主源、腳本確定性＋LLM 語意
離線快取），但 §23 的細部規格還空著：NPC schema 長什麼樣、逐選手對手模型怎麼掛進
§11.1、§14.3 兩條路線的百分位怎麼算、隊友從 NPC 池抽選的規則。這些是 S24–S30 的
輸入，不定案後面七站無法開工。

---

## 入口狀態

```bash
npm test
```

全綠。S21（DEMO 組裝）與 S22（文件同步）已完成，V4 正式收尾。

```bash
node docs/v4/next-station.mjs
```

回報本站為下一站。

---

## 範圍

### 要做

**1. NPC schema 定案**（§20 資料模型的延伸，不併入玩家 state）

- `npc_roster.json` 欄位：`player_id`（僅 ID，無真實姓名）、`team_id`（縮寫）、
  `birth_year`、`position`、`career`（效力戰隊年表＋賽事名次）、六屬性／潛力／
  `lifecycle`（peak_age／rise_k／fall_k）、技能、`psych` 六維、特質、敘事標籤。
- 資料檔標頭 CC BY-SA 出處註記格式。
- 玩家 state 是否新增欄位（例如隊友改存 NPC 引用）——定案後回寫 §20.2。

**2. §11.1 逐選手對手模型定案**

- 現況：對手是聚合強度（`kernel/strength.js`），團隊強度差 → 單局勝率。
- 定案：五名對手如何由 NPC 池構成（按位置抽？強度如何聚合回 §11.1 公式？）、
  明星項（`min(6.0, 0.06 × (P − par)^1.35)`）與對手強度階梯（MSI 地板 72／世界賽
  74）在新模型下的去留、160 段不變式怎麼改寫。
- 輸出必須是一份 S29 可以直接照做的規格，不是開放式討論。

**3. §14.3 百分位改法定案**

- 兩條暫代路線改回百分位：究極綠葉「助攻前 10%」、網紅選手「評分低於中位數」。
- 定案：母體定義（全部選手？同年齡段？同賽區？）、由 `cleaned_players.json` 的
  哪些欄位算、門檻怎麼寫進引擎（靜態查表或函數）。
- §21.3 的登記條目與附錄 #32 在此站關閉。

**4. 隊友池規則定案**（S28 輸入）

- 隊友從 NPC 池抽選的規則：按年代（史實出場年）過濾？按位置？與現行
  `rollRoster`（`MATE_NAMES` 隨機抽 4 人配 rating）的關係——取代還是共存？
- `MATE_NAMES` 的去留決定。

**5. 探勘風險預先定案**

- GPL 早期（2012）Liquipedia 覆蓋稀薄時的 Leaguepedia 備援規則：何種缺口觸發
  備援、備援資料的標記方式（S24 依此執行）。

### 不要做

- **不推翻 §23.1 的四件定案**（一次到位、去姓名、Liquipedia 主源、LLM 語意離線快取）
- **不寫爬蟲、不碰程式**。純文件站——產出是 §23 細部規格與受影響節的回寫
- **不刪 §21.3 與附錄的歷史登記**——關閉條目時標記已定案，不抹掉痕跡

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `ESPORT-DESIGN-V4.md` | §23 細部回寫；§11.1／§14.3／§20.2 依定案修訂；§21.3 與附錄 #32 標記關閉 |
| `docs/v4/README.md` | 若定案改變了站範圍或前置，狀態表同步更新 |

---

## 規則與不變式

- `npm test` 前後完全不變（沒碰程式）
- 每項定案都要寫「S24–S30 哪一站吃這條」——交接鏈斷掉是 S20b 審查抓過的最大坑
- §23.1 引用不得改寫，只可往下細化

---

## 完成定義

- §23 全節有內容、無「待定」字樣（除刻意留給 playtest 者，需明列於 §21.3 風格）
- §11.1 逐選手模型有一份 S29 可照做的定案（公式、抽選規則、不變式改寫）
- §14.3 兩條路線的百分位定義、母體、計算方法寫死
- `node docs/v4/next-station.mjs` 回報 S24 為下一站

---

## 交接筆記

<!-- 做完由執行者回填 -->
