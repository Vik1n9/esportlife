# S29 · 引擎整合：逐選手對手模型（§11.1）

狀態：未開始
前置：S23 S27
預估：1 session
推理難度：高
建議模型：Opus 5（`claude-opus-5`）
理由：動 §11.1 團隊強度與對手強度階梯——160 段校準、世界冠軍人均、S12 的
`OPPONENT_SUPPORT` 推導全部咬在這一層。S23 定了規格，本站的風險在實作與
基線對照的紀律。

---

## 為什麼做這件事

對手現在是聚合強度：`kernel/strength.js` 一個數字差進 §11.1 勝率公式。
S27 的 `npc_roster.json` 讓「全聯盟有逐選手數據」成立——本站把對手的聚合
數字換成由 NPC 構成的逐選手模型，照 S23 定案的規格（構成規則、聚合回 §11.1
的公式、明星項與強度階梯去留）。

---

## 入口狀態

```bash
node docs/v4/next-station.mjs
```

回報本站（與 S28 並行可解鎖）。S23 定案在手：五名對手如何由 NPC 池構成、
強度如何聚合回 §11.1、`min(6.0, 0.06 × (P − par)^1.35)` 明星項與 MSI 地板
72／世界賽 74 在新模型下的去留、160 段不變式改寫。

---

## 範圍

### 要做

**1. 逐選手對手構成**

- 依 S23 定案：按位置從 `npc_roster.json` 抽五名對手（賽區／年代／層級過濾
  規則照 S23），聚合成 §11.1 的對手強度。
- 對手的 `psych`／特質若進模型（如受迫性失誤的對手端），照 S23 定案接線。

**2. §11.1 改寫與基線對照**

- 改寫後的單局勝率分佈與舊聚合模型對照：160 段基線的偏移必須可解釋
  （不是「模型變了所以數字變了」，是「哪個構成環節造成多少偏移」）。
- S12 交接筆記的警告適用：動到 `positionPower` 乘法項要重算
  `OPPONENT_SUPPORT`——**改動落點要逐項對照那條警告**。

**3. 不變式更新**

- S23 定案的不變式改寫落地：`tests/` 對應斷言更新（golden 基線怎麼處理
  依 S23 定案——重洗或新基準）。

### 不要做

- **不做校準收斂**——S30 的事。本站做「接上模型＋偏移可解釋」，收尾的
  常數重推交給 S30
- 不動隊友抽選與戰隊顯示（S28 的領地，併行時注意 merge 衝突）
- 不即時 LLM——對手一律讀 `npc_roster.json` 靜態檔

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `src/kernel/strength.js` | 聚合強度改逐選手（依 S23 規格） |
| `src/phases/*`（對手強度消費端） | 接新模型 |
| `tests/` | 不變式更新、基線對照 |

---

## 規則與不變式

- `npm test` 全綠；`history/` 分區（賽制史實）不動
- 逐項記錄「舊聚合 vs 新模型」的數字偏移（S30 的輸入）
- 對手構成完全離線（讀靜態檔，無網路、無 LLM）

---

## 完成定義

- 逐選手模型上線、§11.1 依 S23 定案改寫、測試全綠
- 基線偏移報告寫進交接筆記：每個改動環節的量測值
- S12 警告（`OPPONENT_SUPPORT` 重推）已逐項檢查並記錄結論

---

## 交接筆記

<!-- 做完由執行者回填：模型落點、偏移報告、OPPONENT_SUPPORT 結論、待 S30 的校準清單 -->
