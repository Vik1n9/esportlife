# S25 · 資料清洗

狀態：完成
前置：S24
預估：1 session
推理難度：中
建議模型：Sonnet 5（`claude-sonnet-5`）
理由：規則解析為主。LLM 只用於模糊比對且批次打包、結果落字典快取——極低 Token。
判斷點在實體對齊的別名映射，S23 的 `team_history.json` 是標準答案。

---

## 為什麼做這件事

raw_data 是 Wikitext／HTML，不能直接進參數生成。本站把非結構化文本轉成
`cleaned_players.json`：標準化基礎資料、生涯履歷、粗略賽事統計。S26（百分位母體）
與 S27（參數生成）都讀它，是資料線的分水嶺。

---

## 入口狀態

```bash
node docs/v4/next-station.mjs
```

回報本站為下一站。S24 的 `raw_data/`、`team_history.json`、`target_players.csv`
齊備，交接筆記有探勘結論。

---

## 範圍

### 要做

**1. 基礎欄位解析（規則制）**

- 解析 Infobox 與 Tournament Results 表格：出生年（不抓真實姓名，依 §23.1）、
  位置、所屬戰隊歷史、賽事名次。
- 依 S23 定案的 NPC schema 欄位對齊（schema 是權威，欄位對不上就回報）。

**2. 實體解析與標準化**

- 戰隊別名統一映射到 `team_history.json` 的 `team_id`（如 TPA／台北暗殺星／
  Taipei Assassins → 同一縮寫）。
- 模糊描述打包成批次（每次 10 筆）呼叫 LLM 輸出 `team_id` JSON 陣列；
  **映射結果寫入本地字典檔，後續查表不再呼叫 LLM**。
- 字典檔進 repo（S27 也用它）。

**3. 產出 `cleaned_players.json`**

- 每名選手一筆：標準化基礎資料、生涯履歷、Wiki 有提供的粗略賽事統計。
- 去重：同名 ID 跨年度合併成單筆生涯。

### 不要做

- **不做遊戲參數計算**（潛力、屬性、特質）——那是 S27
- 不抓真實姓名（§23.1）
- 清洗規則與 S23 schema 衝突時：**照 schema 改腳本**，不回頭改 schema
- 不碰 `src/` 與 `tests/`

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `tools/npc-clean/`（或 S23 定案位置） | 解析與清洗腳本 |
| `tools/npc-clean/team_alias.json` | 別名→team_id 字典（LLM 快取產物） |
| `tools/npc-clean/cleaned_players.json` | 標準化資料集 |

---

## 規則與不變式

- 同一段模糊描述查表命中即不呼叫 LLM（快取先於推理）
- 每筆 `player_id` 唯一；`team_id` 必須存在於 `team_history.json`
- 純工具站：`npm test` 前後完全不變

---

## 完成定義

- `cleaned_players.json` 覆蓋 `target_players.csv` 全部可解析選手；不可解析者
  有明確標記與原因（寫進交接筆記）
- 別名字典覆蓋率與 LLM 呼叫次數寫進交接筆記（快取效果可量測）
- 抽樣人工核對 10 筆（S23 定案時指定的抽樣規則）：欄位與史實一致

---

## 交接筆記

**2026-08-17 完成（S24c 返工後續做）。**

- 最終輸出 `cleaned_players.json` **2650 筆**（CSV 覆蓋 938/938、有名冊/生涯
  **2271**、有出生年 805、有位置 984、隊名殘餘 22、壞 team_id 0、
  player_id 唯一 true）。LLM 呼叫 0 次（執行者判讀替代，`team_alias.json`
  **54 筆**別名）。
- **本次修掉兩個解析 bug**（`gen-clean.mjs`／`parse.mjs`）：
  1. **巢狀模板截斷**：`TeamRoster`／`TeamCard` 的 regex 非貪婪匹配在
     內層 `}}` 提前終止——每卡只吃到第一名選手。改 `findTemplates`
     （深度感知）後名冊選手 1911→**2208**（+297）、cleaned 2334→2650。
  2. **`TeamRoster/Line` 誤當整卡**：findTemplates 的 `startsWith(名+'/')`
     把 Line 卡也當整卡解析（teamName=undefined→假「隊名未對齊」note）——
     過濾 `name==='TeamRoster'` 後清除。
- **classifyEvent 補年份**（`parse.mjs`）：Season N 頁題無 4 位年
  （`EU LCS/Season 3/Spring Season`＝Leaguepedia 備援頁、`GPL/Season 1`
  ＝2012、`GPL/Opening Event`＝2012-05-05 實測）→ `seasonMap {1:2011,
  2:2012, 3:2013, 4:2014}`＋`/OPENING EVENT/`→2012。「其他」4→1
  （`jay_taiwanese_player.wiki`＝DisambigPage，屬正確）。
- **殘餘 22 隊全部真缺**（`pending_team_alias.json`，需 S24c 再補）：
  HTICS（Team Heretics）、KCORP（Karmine Corp）、NAVI（Natus Vincere）、
  Dynamics、SHO、Shopify Rebellion、db（DragonBorns）＋2012-13 老隊
  （Azubu Blaze、MiG Blaze、MiG Frost、StarTale、SuperStar、Team OP、
  Team XD、Saint Club、RoMg、DDoL、GJR、Little Hippo、NEB、NeL、Hyper
  Youth Gaming）。受影響 CSV 選手 **28 名、37 人次**（全 p1/p2）——
  完整清單見 `tools/npc/clean/missing_teams.md`「S25 續做」節。
- **重複隊**：S24c 誤建 `TJ`（Taipei J Team）＝JT 同隊——已併入 JT
  （team_history 刪條目＋別名）。⚠ S24c 生成器重跑會重建，建議加排除。
- **finish 鍵語義**（承接 24d 定案）：賽區例行賽第一＝`champion` 鍵
  （如 Crownie 2023 LEC champion＝BDS 2023 Spring 例行賽第一，非奪冠）。
  消費端判「奪冠」須認事件鍵（worlds/msi vs 賽區代碼）。
- **MSI 2015 名次全缺**（TeamPrizePool 無 Opponent 連結、TeamCard 無
  placement）＝Liquipedia 靜態缺漏，不硬解（Faker 2015 段只有 worlds
  champion）。
- 13 筆未解消歧義全部判讀完成（生涯證據收斂，CSV 短名 id 與 cleaned
  一致）：Apex/Archer/Aria/Jelly/Lonely/Lucky/Mimic/Mountain/Naru/Noway/
  Steal/Style/Jay。
- **國籍規則**（使用者定案，2026-08-17）：選手與隊伍一律認賽區不認國籍。
  現況：`cleaned_players.json` 無 country 欄（合規）；`team_history.json`
  region 台港澳段 TW(37)/HK(5)＝S24b 的 Infobox 國籍（**不相合**，S24b
  需返工，見 `docs/v4/rule-去除國籍只留賽區.md`）——S25 收尾不阻塞，但
  S24b 返工後 S25 需重跑一次（region 不影響 career，只影響分群）。
- p1 無生涯 60 筆＝名冊真無（全無 note，抽樣核對未見解析漏）——維持原判。
- **OCR 審查閘門未跑**：本環境 `ocr` LLM 未設定（no valid LLM endpoint）——
  比照 S22 返工三往例記入交接筆記，下次有環境時補審
  （`node scripts/station-review.mjs --station 25`）。

**2026-08-17 續做（S24c 返工完成後）。**

- S24c 返工完成：team_history.json 174→300 隊，缺隊 118 全補（詳見 24c
  交接筆記與 `tools/npc/rework_s24c.md`）。
- 重跑 `node gen-clean.mjs`：cleaned 2326→**2334** 筆（CSV 覆蓋 938/938、
  有名冊/生涯 1529→**2009**、出生年 805 不變、位置 984 不變）；隊名殘餘
  pending 159→**38**（消 121）；壞 team_id 0；player_id 唯一 true。
- 殘餘 38 隊皆「頁題縮寫變體」（KCORP→Karmine Corp、TT CN→ThunderTalk
  Gaming、UP CN→Ultra Prime、AL CN→Anyone's Legend、NIP CN→Ninjas in
  Pyjamas、XL/SB/LSB/NAVI/DWG KIA…）或早期頁隊（2012 Champions 的
  DDoL/StarTale/Team OP…）——實體隊全在 team_history，**續做：在
  `team_alias.json` 補別名收斂**（縮寫變體→實體 team_id；早期小隊若為
  priority 1/2 選手生涯所需也補，純 p3 可暫緩）。
- **region 規則（使用者定案）**：選手與隊伍的賽區身分一律認賽區代碼
  （PCS/LMS/LCP/LCK…），不依國籍分 TW/HK/MO——LoL 賽事認賽區名。
  台港澳段的 TW/HK/MO 只是抓取優先級分群。S25 對齊與 S27 生成勿用國籍。
- 未完成（原樣）：13 筆未解消歧義候選判讀、MSI 2015 名次全缺、抽樣人工
  核對 10 筆、LLM 呼叫次數（別名補完後若殘餘量小可省略 LLM）。

**2026-08-17 暫停（進行中）——缺隊阻塞，等 S24c 返工。**

- 已寫出 `tools/npc/gen-clean.mjs`＋`tools/npc/clean/`（parse／align／llm）
  與 `clean/team_alias.json`（33 筆執行者判讀別名：SKT 姊妹隊、KT 雙隊、
  KOI→MAD2、GE Tigers→KOO、Gen.G Esports→GEN 等）。重跑冪等。
- 已跑通一輪：raw 1347（無頁題 0）；選手頁 984、隊頁 196、賽事頁 166、
  其他 4；名冊選手 1891、SquadAutoRow 192；cleaned 2326；CSV 覆蓋
  938/938；有名冊/生涯 1529；有出生年 805；有位置 984；未解消歧義
  13/13 在結果；player_id 唯一 true；壞 team_id 0。
- **阻塞**：`team_history.json` 缺 118 隊（split 名冊全參賽隊未收）→
  262 名 CSV 選手、431 人次受影響；priority 1 有 72 筆無 career。
  完整清單與返工範圍見 `tools/npc/clean/missing_teams.md`。
- 未完成：13 筆未解消歧義候選判讀、MSI 2015 名次全缺（TeamPrizePool
  無 Opponent、TeamCard 無 placement，Liquipedia 靜態缺漏）、「其他 4」
  檔內容核對、抽樣人工核對 10 筆、LLM 呼叫次數（本輪 0——執行者判讀替代，
  S24c 補隊後若殘餘量小，LLM 批次可省略，直接在字典補別名）。
- 續做：S24c 補隊 → 重跑 `node tools/npc/gen-clean.mjs` → 驗證殘餘收斂
  → 抽樣 → 收尾。
