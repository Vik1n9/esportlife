# WORKLOG — 電競人生（esportlife）

## 2026-08-13 — S08b 第二輪：傳說的觸發條件是語法，不是形狀

- **方向**：PR #12 的 review 指出第一版把「1 史詩 ＋ 1 稀有」寫進了 §12.3 的 schema
  描述與 S17b 的實作指示——「某傳說可能需要兩個稀有特質才能觸發，某傳說可能需要賽事
  成績加上一個稀有特質取得，若寫死會導致傳說的設計無法展現創意，玩家也無法通過賽事
  邏輯和過往賽事歷史推敲出獲得條件」。
- **這是第一版把兩件事混在一起了**：§14.2 那張 20 列的表是**校準用的預設配置**（它算的
  是素材面能供應多少條路線），與**條件的語法**是兩回事。把配置的形狀當成 schema 寫死，
  等於讓 20 張卡共用一套模具，而且玩家推敲取得條件的樂趣（§1.2 隱藏探索、§0.5 直覺
  檢驗）也一起沒了。
- **修正**：§12.3 新增「觸發條件是條件式，不是固定欄位」——可組合的謂詞語法（特質持有／
  賽事戰績／生涯軌跡／個人數據／狀態量／時代賽區 ＋ 且或非）。三條約束：`legend` 一律
  AND 上底線門檻、謂詞一律讀 S17a 的 `ledger.js`（加謂詞＝加查詢函式，不動求值器）、
  **條件必須推敲得出來**——隱藏心理六維不准當條件，因為玩家看不到（§9.1），
  推敲不出來的條件不是深度是黑箱。
- **§14.2 的表加註為「預設配置不是形狀」**，S18 可以把某張改成兩個稀有或戰績＋稀有。
  唯一代價是**改了形狀就要重推供給算式**——「每個史詩恰好進兩條、稀有依取得率分配次數」
  只在「每張卡一史詩一稀有」時成立。S18 改了要寫進交接筆記，S19c 開工先翻。
- **底線門檻不在這個自由度裡**：它是傳說持有率 ≤50% 的守門員，任何一張 `legend` 卡都
  繞不過。設計自由給的是條件式怎麼組，不是能不能繞過守門員。
- **防回歸的網子**：S17b 的測試要放三種形狀（一史詩一稀有／兩個稀有／賽事成績＋一個
  稀有）。形狀若被寫回固定欄位，後兩條會紅——這比在文件裡寫「不要寫死」有效。
- **狀態**：完成，`npm test` 10846 項全綠，程式仍未動。PR #12 已合併，這一輪走新的 PR。

## 2026-08-13 — S08b V4.1 增訂：傳說改由任務卡發放，但配方表一行都沒丟

- **方向**：外部提出《V4.1 規格企劃書修訂清單》六條增訂（四項設計檢驗、生涯傳記、
  種子天生特質、傳說事件卡、移除傳說的合成硬門檻、五條替代性生涯路線）。這一站做的是
  **可行性研究後的併入**，不是照抄——只改文件，程式一行沒動。
- **傳說配方不能整段丟，改成「原地轉成觸發條件」**。增訂書要「傳說完全由傳說事件卡
  決定」，但 §14.2 那 20 條配方帶著 160 段實測：兩池不重疊的結構、每個史詩恰好進兩條、
  底線門檻（國際賽四強）30.0% 的聯合命中率。整段刪掉會讓稀有與史詩變成**沒有出口的
  終點**，§13.3 第三層約束在頂端失效。定案是傳說事件卡當唯一發放口（照增訂書的意圖），
  但**觸發條件的語法包含「持有指定史詩＋稀有」**，20 組原封轉成 20 張 `legend` 卡的
  觸發條件。代價：持有率的推導從「持有素材即得」變成「**開卡率 × 達成率**」，30.0%
  從持有率降格成持有率的**上界**，S19c 要重量。
- **⚠ 真正會撞牆的是持有率上限，不是機制**。增訂書同時要「平庸局也有明確的 Build 終點」
  與「允許單局複數傳說」，兩件事都把傳說持有率往上推，而 S07 不變式寫死
  **「持有傳說特質的生涯不得超過一半」且註明不准放寬**（§14.2 目標值 28–35%）。
  定案是 **`route` 路線的終點是史詩特質＋生涯標籤，不是傳說**——增訂書自己的「未達成
  結果→轉為史詩或生涯標籤」已經蘊含這個階梯，只是把它從退化路徑變成常態。28–35% 與
  ≤50% 兩個數字一格沒動；新增兩條驗收線管新增的自由度：單局傳說 ≥3 的生涯 ≤5%（管疊加）、
  後段局的 `route` 覆蓋率 ≥60%（管心流檢驗有沒有真的兌現）。
- **兩條路線的條件引擎算不出來**：「究極綠葉：助攻數據前 10%」要全聯盟逐選手的個人
  數據列，引擎只有對手隊伍的**聚合強度**（`kernel/strength.js`），百分位沒有母體；
  「網紅選手：低於中位數」要跨局的評分母體。補這個母體等於在比賽模擬層新加一個子系統，
  成本遠高於這兩條路線的價值。暫用絕對門檻（場均助攻門檻＋獎項 0；`careerScore < 900`，
  沿用 §10 分級的既有中段線不新增常數），並在 §21.3 登記：**未來做歷史選手卡片資料庫
  之後要改回百分位定義**。
- **增訂書沒寫、但做下去會撞到的三件事**，各自變成一站：（一）五條路線要的量有一半
  不在 `state` 裡（國際賽勝負、歷任戰隊、降級危機累計、獎項計數），而 §15.5 生涯傳記
  要的是同一批資料 → **S17a 生涯軌跡帳本**；（二）§12.1 的觸發是單月結算完就結束，
  「達成目標／期限／未達成結果」需要跨回合存活的狀態機 → **S17b 生涯任務引擎**；
  （三）天生特質插進 `createState` 的 `birth` 流會**位移所有既有種子的心理／fame／
  英雄池**，可以接受（`SAVE_VERSION` 本來就要 bump），但抽取順序已在 §1.4 寫死，
  之後不准再插隊 → **S19d 種子天生特質**。加上 **S21a 生涯傳記生成**，25 站變 30 站。
- **順手解掉一個舊洞**：§14.2 死配方規則點名的五個死素材裡，`鐵人` 是**完全沒有來源**
  的那一個（取得率 0%）。它是體質類，收進天生特質池就活了，S18 不必再為它補卡。
  其餘四個是競技／媒體類，不進天生池，仍是 S18 的硬需求。
- **狀態**：完成，`npm test` **10846 項全綠**（與改動前逐項相同），
  `git diff --stat -- src tests` 為空。
- **未一起處理**：`docs/v4/08-規格書補洞.md` 是已完成站的歷史紀錄，裡面「通用→稀有→
  史詩→傳說」與「20 條傳說配方」的敘述**刻意未改**——改完成站的交接筆記等於竄改紀錄。
  README 的 v4.1 橫幅要求開工前先讀 `08b` 的交接筆記，那是給後續的人的入口。

## 2026-08-13 — S13 體力系統：草案數字自己算出來是 2.9 個月，不是它自己訂的 3–4

- **方向**：丁組第一站的地基。V4 §6 把「體力」從一個由 `vit` 導出、一輩子不動的**技能**
  升格成 0–100 的**消耗資源**——訓練扣、比賽扣、休息回、每月自然回，低了降低訓練成功率
  並提高受傷風險。沒有它，月回合制（S14）與設施制訓練（S16）都沒有取捨。
- **§6.1 的草案數字不成立，而且是算得出來的**：訓練月淨消耗 27＋12−12 = 27、休息月淨
  回復 50＋12−12 = 50，50 ÷ 27 ≈ 1.85 個訓練月 → **間隔 2.9 個月**，低於它自己那一頁
  寫的「保守玩法約每 3–4 個月休息一次」。實測驗證過：把自然恢復改回 +12 跑 160 段量到
  **2.92**。三個旋鈕裡只有**自然恢復**動起來不傷手感（調訓練消耗＝訓練變便宜、調休息
  回復＝休息變萬靈丹，後者正是 §6.2 明文要防的），解 (40+M)/(40−M) = 2.5 得 **+12 → +17**。
  改完實測 **3.57 個月**，休息佔 27.7% 的月份、透支月佔 28.3%。
- **⚠ 真正的地雷不是節奏，是「換讀數來源」被當成「改平衡」。** 體力接上去之後，
  `formFactor` 與 §11.1 的體力修正這兩個既有公式的輸入被換掉了，而它們的中點是配著舊
  輸入（`vit`，生涯中位數 63）訂的；新輸入的實測中心是 45.4。第一版把中點放在 45，
  160 段的平均手感 0.9935 → 1.0013——只差 0.008，但乘進 `(form − 1) × 1.6` 就是全聯盟
  **+1.25 個百分點的勝率**，`smoke.mjs` 的「五個等第都出現得到」當場紅（最低那一等第
  整個消失）。中點回推到 **47**、斜率 0.0056 → **0.0035**（體力擺幅比屬性大，斜率不收
  會整段生涯貼在 0.85／1.06 兩個界上，手感退化成二值旗標），§11.1 那一項改成
  `staminaPower`（係數 0.027 → 平均 1.23 點，對齊舊值 1.24），四項合計指標全部貼回基準。
- **`vit` 差一點變成死屬性**：S10 已經把它的 OVR 權重全部歸零，這一站又把它從 `formFactor`
  與 §11.1 拿掉——做完就沒有任何消費者了。所以恢復量掛 `vit`（60 中性、滿值 ±20%），
  幅度刻意小：那是體質差異，不是另一條成長線。§6.1 沒有這一條，已回寫。
- **BO 改成逐局計價**（每局 −4.2，取代草案的「季後賽每輪 −18／國際賽 −22」）：3:0 收工
  與被拖到第五局不該一樣累，而「國際賽比較貴」不必另寫規則——局數自己會算出來。
- **曲線的形狀**：§6.2 的四個區間一格沒動，補的是區間**內**線性內插（不然 59 與 30 是
  同一個懲罰，玩家沒有「還能不能再撐一次」的判斷空間），60 與 30 兩個邊界保留為斷崖
  ——它們就是決策點。疲勞區不加受傷風險（那一格的代價是練不出東西，不是身體垮掉），
  受傷走乘法，硬撐對 33 歲的人比對 20 歲的人危險。
- **自動駕駛是暫時的**：月回合制還沒做，但體力凍在 100 就等於沒做這個系統（手感、受傷、
  勝率三個讀數全讀到假的）。所以 `stamina.js` 有一段保守策略的 `planMonth`（體力 < 45
  就休息），由 `season.js` 每個賽段推進 `monthsFor(weight)` 個月。**S14 要換掉的只有那
  一個函式**，經濟與曲線不必動。
- **狀態**：完成，`npm test` **10846 項全綠**。SKIP 清單 2 → 1 項（「體力節奏」轉為實際
  檢查），並補了兩條 §6.2 兩個失敗模式的網子（休息不得佔一半以上、透支區必須出現得到）。
  四條「故意弄壞 → 確認會紅」全部驗過。
- **未一起處理**：復健年與休賽期目前不推進月份（要動 `phases/`，留 S14）；`successMul`
  還沒有真正的消費者，S16 接訓練成功率時那裡是唯一該改的地方；`ARCHITECTURE.md` 血緣表
  把 `engine/retire.js` 標成「V4 S13 重寫」但這一站的說明書沒提過它，未擅自更動。

## 2026-08-13 — S12 六維心理＋失誤系統：規格書沒說的是「§9.2 也發生在對手身上」

- **方向**：丙組第四站。V4 §9 把 v3 的社交五維（`nerve`／`chem`／`ego`／`fame`／`rep`）
  換成競技心理六維（抗壓／自信／動機／紀律／信任／韌性），聲量拆成獨立一層，並補上
  §9.2 的發揮公式與 §9.3 的失誤系統。核心分離（§2）：**屬性決定技能「數值」，心理決定
  技能「發揮穩定度」。**
- **⚠ 真正的地雷：`OPPONENT_SUPPORT` 6.5 → 9.0。** §9.2 的心理穩定修正是**乘法**進
  `positionPower`，但它不只發生在玩家身上——階梯上那些賽區冠軍等級的隊伍，它們的 carry
  也活在 §9.2 底下。S11 推導 6.5 時還沒有這個乘法項，照字面沿用等於「我方吃 §9.2、對面
  不吃」：640 段生涯的**世界冠軍人均 0.113 → 0.248**、有國際冠軍的生涯 122 → 150 段。
  9.0 是推導再由實測確認的（對手 carry ×1.04 之後明星項 3.0 → 3.56，加上它在自家隊伍
  強度裡多出的 2.0，3.5＋3.56＋2.0 = 9.06），改完回到 0.128／123 段。
- **排除過兩個假設，都跑 640 段**：把國際賽的抗壓獎勵減半完全無效（0.248 → 0.253），
  所以**不是「贏了長抗壓、抗壓又幫你贏」的回饋迴圈**；把 `stability()` 釘成 1 直接回到
  0.125，單一根因確認。
- **§9.2 的 0.85／0.30 一個字沒動，但兩條 v3 的心理加成必須讓路。** 接上去之後 S07 那條
  「戰力持平時心理造成的勝率擺幅 ≤ 30 個百分點」量到 **35.3** 直接紅。拆解：§9.2 佔
  17.8 pp（不准動）、信任→隊友 8.5、抗壓→決勝局 9.0。後兩者吃的正是 §9.2 已經在吃的
  `trust` 與 `comp`——**重複計價**，所以各砍到剩下 §9.2 表達不了的那一半（0.12 → 0.08、
  0.16 → 0.05），改完 28.5 pp。大心臟的份量因此改由特質層（`bigheart`／終極舞台）承擔。
- **失誤：因隱藏、果可見。** 新增 `engine/psych.js`（§9.2 十二技能 ← 六維的權重表、
  §9.3 失誤係數）與 `state.deathLog` 兩格紀錄。壓力係數**只乘在「短缺」那一半**——抗壓低
  的人在生死戰被放大到兩倍多，抗壓高的人拿到的減免跟常規賽一樣多，不會因為場合變大就
  多領獎金。實測 160 段：低抗壓在大賽每場多死 **+0.123**、高抗壓 **−0.063**，而全樣本
  平均幾乎持平（1.236 對 1.226）——**這是重分配不是通膨**。
- **`ego`→`conf` 是唯一沒照 §9.4 字面走的決定。** §9.4 說 ego 不留維度、機械對映時丟棄，
  但那句話講的是它**放大聲量的角色**改由特質副作用承擔；ego 的另一半（把自己看得多大）
  跟 `conf` 是同一件事。整個丟掉的話 18 張扮演卡沒有一張碰得到 `conf`，§9.3 明文要求的
  「自信過高→貪」那條雙向設計就永遠驗不到。回退方式寫在交接筆記。
- **`rep` 拿掉之後三個機制改讀教練評價＋特質副作用**（§9.4 已定案）：戰隊切割改看
  「打不到聯賽 par 8 點以上」＋圈內毒瘤加碼、報價縮水與鬼牧只剩特質旗標。`firedTimes`
  因此從 640 段 374 次掉到 178 次——舊值有一半是 harness 的 bold 打法把 `rep` 打到 −60
  以下刷出來的，而那一維已經不存在了。
- **玩家介面**：面板的「人物側寫」五列標籤整段刪掉（「大場面型」等於把抗壓值攤開），
  換成只有一列的「聲量」。**寫成了不變式**——`hiddenFromUi` 掃 `src/ui/*.js` 的原始碼，
  出現任何六維名稱或舊分級標籤就紅。這條**當場抓到我自己**：面板那句說明文案原本寫
  「你的心態、抗壓與紀律不會出現在這裡」，`抗壓` 兩個字就是洩漏。
- **驗收（四組獨立種子，對 S11）**：薪資 +2.5%、打進主流賽區 −3.8%、有國際冠軍的生涯
  +0.8%、平均巔峰 +0.1%，全部在 S11 的驗收帶內。
- **狀態**：完成，`npm test` **10782 項全綠**。SKIP 清單 3 → 2 項（「心理是放大器 · §9.2
  發揮公式帶寬」轉為一整組實際檢查）。八條「故意弄壞 → 確認會紅」全部驗過。headless
  Chromium 實測模組圖與面板渲染，無 console error。
- **⚠ 過程中的一次失誤**：驗證「故意弄壞」時用了 `git checkout -- src/` 當還原手段，
  把整個 `src/` 打回 HEAD，未提交的實作全數損失，只有兩個未追蹤的新檔倖存。已照本文
  的紀錄逐檔重建並確認數值與重建前完全一致（擺幅 28.5、失誤落差 +0.136／−0.037），
  後續改用檔案快照還原。**還原手段不要用 git checkout，除非工作已經 commit 過。**
- **未一起處理**：陣亡數目前只進畫面不進結算（`career.js` 沒有陣亡權重）；扮演卡只做
  機械對映，`disc`／`drive`／`resl` 三維沒有任何一張卡碰得到，只有出生的 ±10 天賦差。
  兩者都寫進交接筆記，後者已寫成測試檢查逼 S19a／S20 回來更新。

## 2026-08-12 — S11 教練評價取代 OVR：規格書的驗算只查了一半，補上之後才對得回 S10

- **方向**：丙組第三站。V4 §10 廢掉 OVR——不是刪掉，是拆成兩層：玩家看十二項技能，
  「需要單一數值」的場合（薪資／試訓／續約／FA／板凳／獎項）改讀**教練評價**這個內部值。
  同時依 §11.1 把隊伍強度的權重換成 0.60／0.40 並補上**明星效應**。
- **改名是機械的**（`ovr`→`coachRating`、`effectiveOvr`→`effectiveCoachRating`、
  `peakOvr`→`peakRating`、`mates[].ovr`→`.rating`、特質效果鍵 `ovrAdd`→`ratingAdd`…，
  `SAVE_VERSION` 10 → 11，舊存檔作廢）。**平衡工作完全不在說明書預期的地方**——說明書寫
  「要處理心理修正加進來之後分布形狀的變化」，但心理修正這一站是 0（等 S12）。
- **⚠ 真正的地雷：`w_p` 0.55 → 0.60 不只是加重玩家份量。** 舊的兩個權重**加起來只有
  0.90**，在 par 66 時把整隊強度系統性壓低 6.6 點，再由教練與體力那 ~5 點補回來，淨值約
  `par − 1.1`。§11.1 的新權重和是 **1.00**、教練與體力變純加項，所以同一支隊直接上移約
  5 點。照字面實作跑 160 段：**有國際賽冠軍的生涯 39 → 84 段**、`五個等第都出現得到`
  **翻紅**（沒有人再打得爛）、能把單局勝率頂到 92% 上限的生涯 1 → 18／640。
- **不是實作錯了，是規格書的驗算只查了一半。** §11.1 的驗算例（P=100、隊友 59、對世界賽
  淘汰賽 78）在這份程式上算出 94.1／72%，跟它寫的 93.1／76% 對得上；明星項的分段對照表
  七個點**一個數字不差**。它沒查的是同一節自己訂的 MSI 地板 72 與國內季後賽階梯。
- **修法是一個單位錯位**：階梯表給的 `par` 是「該聯賽**先發平均教練評價**」＝**一個選手**
  的水準，而我們的 `teamStrength` 是**一支隊**（含教練、體力、明星項）。直接相減等於讓
  對面用一個選手打我們一支隊；舊制的 0.90 權重和剛好把這個錯位遮住了。新增
  `OPPONENT_SUPPORT = 6.5` 與 `opponentStrength()`，五個拿階梯值當對手的地方全部走它。
  值有兩份來源互相印證：**推導**（對手也有教練/體力 3.5 ＋ 它自己的明星項約 3.0，後者
  查 §11.1 分段表）與**實測**（掃 3.5／5／6.5／8 跑 640 段，只有 6.5 把 S10 的校準整組
  帶回來）。**這是本站唯一對 §17.2「不要各自發明」的偏離**，回退方式寫在交接筆記。
- **驗收（四組獨立種子，括號內 S10）**：平均薪資 11746／10494／9992／11739
  （12035／10757／10152／11457，±2.5%）；打進主流賽區 83／66／62／77（86／66／62／76，
  ±3.5%）；有國際冠軍的生涯 42／27／20／33（39／30／22／34，±10%）。第三條驗收（單局勝率
  不得頂到 92%）過了，但 12／640 段能在它最有利的那一場摸到上限（S10 是 1／640），全部是
  戰力高出隊友 15–33 點的那一群，其中 11 段還要靠決勝局的心理加成——那正是 §11.1 要的 carry。
- **教練評價與位置戰力是兩個量**：`positionPower` 刻意**不吃** §10.2 的加法特質（神之領域
  +2、不老傳奇 +1）——那是「教練怎麼看你」，不是「你在場上打得出多少」。心理的兩條出口
  （§10.2 加法進評價、§11.1 乘法進戰力）寫在同一個 `TODO(S12)` 塊裡，**S12 不要合成一條**。
- **玩家介面拿掉了所有總評數字**（§10.1）：狀態列第三格改成本位置第一核心技能、面板刪掉
  「綜合 OVR」那一列、年度總結卡改印四項核心技能、結算與分享圖改印賽段冠軍數、業餘期的
  「綜合 45」改成球探評語（`scoutVerdict()`，門檻沒變只是不再攤成數字）。**寫成了不變式**
  ——測試會掃 `src/ui/*.js`，讀到 `coachRating(` 就紅。
- **狀態**：完成，`npm test` **10639 項全綠**。SKIP 清單 4 → 3 項：本站把「巔峰上界 ·
  教練評價分布」那條 gate 換成一整組實際檢查（兩個量分家、明星項分段表、92% 上限、
  UI 不得讀評價），四條都做過「故意弄壞 → 確認會紅」。headless Chromium 實測狀態列、
  面板與結算，無 console error。

## 2026-08-12 — S10 十二技能表：位置身分守住了，順手把 S09 記的兩個缺口補回來

- **方向**：丙組第二站。把十三項舊技能（`sta`／`ref`／`op`／`macro`／`lane`／`tf`／
  `roam`／`vis` ＋五路專屬）整表換成 V4 §8.1／§8.2 的十二項教練覆盤語彙。**§8.1／§8.2
  的權重一個數字都沒改**——這一站的工作是把規格書的表接上去、確認折疊之後位置身分守得住，
  不是設計新表。
- **鐵則二守住了，而且與 S07 的預測分毫不差**。四條位置身分檢查（S07 定的門檻，一個字
  沒動）全綠：任兩路 L1 距離 **0.110**（JG-SUP，門檻 0.10）、最扁一路前二佔 **0.532**
  （MID，門檻 0.45）、SUP 的靈巧權重 **0.000**（門檻 ≤0.06）、ADC／SUP 靈巧比 **∞**。
  S07 當年折疊 §8.2 草案算出來的預測是 0.110／0.532——**同一個數字**。S07 那條等這一站的
  SKIP 已自動轉為實際檢查且是綠的，SKIP 清單 5 → 4 項。做過「故意弄壞 → 確認會紅」：
  把每項技能改成沾六個屬性（就是 `WORKLOG` 記載的第一版陷阱），四條全部翻紅。
- **⚠ 完成定義那行指令會印 `Infinityx`**——`vit` 在五路的 OVR 權重全部歸零（§6 把體力抽
  出去當資源），最高／最低倍率因此爆掉。這正是 S07 警告「不要用倍率當門檻」的理由，
  不是壞掉。看非零倍率的話是 TOP 3.02×／JG 12.33×／MID 2.33×／ADC 3.19×／SUP 12.37×。
- **意外收穫：S09 交接筆記記的兩個缺口都被這張表關掉了。** 兩者同一個機制——`vit` 沒有
  OVR 權重之後，老手打法不會再把點投進去，新手打法照樣六格輪流撒，「投錯格子」的浪費
  回來了。四組獨立種子：**打法差距 ÷ 可成長空間從 0.059–0.073 跳到 0.133–0.218**
  （門檻 0.0463，餘裕從 1.3 倍變 2.9–4.7 倍）；**頂端訊號從 0.078–0.130 升到
  0.095–0.145**——S09 說「四組會誤報一組」（`alt-*` 的 0.0777 在門檻 0.08 之下），現在
  四組全在門檻之上。加點微基準 0.137 → **0.247**。
- **平均巔峰上移約 +0.02（比例），但這不是通膨。** 同一個機制的另一面：能長的方向少一個，
  同樣的訓練預算換到更多加權後的成長。關鍵在於 S09 的四組裡有兩組（.7178／.7236）**低於**
  §17.2 的驗收帶 0.735–0.775，S10 之後**四組全部落在帶內**（.7385／.7447／.7629／.7697）。
  換表是把分布推回規格書要的位置，所以 `GROWTH_BASE` 沒有動，也不該動——那是 S09／§5.3
  的旋鈕，不是這一站的。
- **⚠ 動了一條測試的母數，是換單位不是放寬，寫在這裡讓人有得反對**：`巔峰上界：沒有任何
  一段生涯超過突破後的硬上限` 換表後紅了（160 段裡 2 段，最高 101）。查下去不是通膨，是
  母數寫錯：它量的 `peakOvr` 含 §10.2 明文允許的加法特質修正（神之領域 +2、不老傳奇 +1），
  母數卻只寫屬性硬上限 100。舊表下碰不到那條線（要六格全滿），新表下打野／輔助的
  awr＋syn＋dec 佔 0.97，三格練滿就是 99，神之領域再 +2 就越線——兩段紅的都是神之領域
  持有者。改成 `硬上限 ＋ §10.2 允許的加法修正`，而且那個 3 是**掃特質資料表算出來的**，
  不是手填；有人違反 §10.2 加第三個 `ovrAdd` 時上界會跟著鬆，那是 review 該看到的訊號。
  **通膨真正的守門員（平均巔峰比例 0.68–0.82）一個字沒動且是綠的。**
- **四個舊鍵的去向**：`sta` 不再是技能（`season.js` 的 `formFactor` 與 `strength.js` 的
  體力修正各接一個暫時來源 `state.attr.vit`，都標 `TODO(S13)`）、`ref` → `pos`（陣亡數
  改吃走位，§8.1 自己寫「走位失誤幾乎都發生在壓力下」）、`roam` → `gank`（助攻）、
  `macro` → `rotate`（`營運鬼才` 的門檻 75 原封搬過來，兩者屬性組成幾乎相同，難度沒鬆）。
  **數據模擬的係數一個都沒動**——換的是讀哪一項技能，不是靈敏度。
- **`ROLE_SIGNATURE` 刪掉**：V4 沒有位置專屬技能，身分完全由權重表達。面板每路從 7~8 項
  收成 6 項（核心 4 標「本位置核心」＋次要 2），舊表那半排 .03~.06 的尾巴既不影響 OVR
  也不構成身分。`harness.allocate` 照說明書沒有手改。
- **狀態**：完成，`npm test` **10584 項全綠**。下一步 S11 教練評價取代 OVR——給它對照用的
  數字：`seed-*` 打進主流賽區 86 段、平均生涯薪資 12035 萬、平均職業季 17.77。

## 2026-08-12 — S09 屬性 0–100 ＋ 潛力衰減：刻度對上了，但「加點是決策」被起始值吃掉一半

- **方向**：丙組第一站。把屬性從 1–80 換成 0–100，並把「成長成本階梯」換成
  「潛力衰減係數」——這兩件是同一件事的兩面（舊制是每點越來越貴，設施制是越接近天花板
  長得越少），**換刻度不換模型會做出四不像**。實作上取衰減係數的倒數當單點成本餵回既有的
  `stepCost`／`carry` 介面，所以 UI 與 `harness.allocate` 的形狀沒變，S16 接設施制時再換。
- **等比重校本身完全對上了**：平均巔峰 **75.05／100 = 0.7505**（§17.2 驗收要 0.755±0.02），
  對 S01 基準 59.9×1.25=74.9 差 +0.2%、對 S07 基準 60.41×1.25=75.5 差 −0.6%。起始評價
  **58.14**（§7.3 驗算寫 58.0）。最極端組合的單局勝率 **76.9%**，沒有頂到 92%
  （§11.1 驗算過的也是 76%）。S07 那條等這一站的 SKIP「潛力衰減 · 衰減單調性」已自動
  轉為實際檢查且是綠的，SKIP 清單 6 → 5 項。
- **⚠ 但 3 項紅著交，全是同一個根因。** `npm test` 10587 項、3 項失敗，三項都是「打法
  差距」家族（微基準 0.0260 對門檻 0.0375、smoke 的巔峰差 1.1 對 1.5、國際當量 0.86 對 1.4 倍）。
  量出來的根因**不是刻度也不是成長曲線**，是 **§7.3 的起始值**：它把出生評價定成潛力的
  0.80／0.70，於是可成長空間從 **32.41（0.405×上限）掉到 18.96（0.190×上限）**。
  加點的價值與可成長空間成正比——微基準領先的比值 0.0260/0.0584 = **0.445**，
  可成長空間的比值 0.190/0.405 = **0.469**，兩者一致到小數第二位。
- **S08 留的處方在這裡無效，實測了才知道**：§7.1 說紅了就把天花板係數的底從 0.34 調到
  0.5、可回收 15%。實測 0.20／0.34／0.50／0.70 四個值的微基準是
  0.0259／0.0260／0.0255／0.0258——**沒有回收，還把老手勝出率從 95% 打到 91.3%**
  （另一條門檻是 ≥90%）。錐度起點 6／12／20 → 0.0275／0.0260／0.0239，全域最佳 0.0275，
  離門檻還差 27%。唯一推得上去的是把基礎成長值開到 4 以上（→0.0496），但那會讓平均巔峰
  衝到 0.95×上限——**通膨換來的差距不是設計出來的差距**。
- **所以照說明書「不要自己調鬆」，門檻一個字都沒動，紅著交。** 四組獨立種子的微基準是
  0.0229–0.0260（低變異，不是種子效應）。交接筆記寫了兩條修法：**改量法**（領先 ÷
  可成長空間，舊制 0.144 對新制 0.137，同一個結論差 5%——這才是 S07 真正想守的東西），
  或**重新談 §7.3 的 0.80／0.70**（它的推導量的是「練了三五年之後」的職業首季，
  當成出生值等於把那幾年送給玩家）。兩條都超出這一站的範圍。
- **第二個連帶效應：業餘階段實質被跳過。** 一隊挖角門檻是 `par − 10 = 56`，而出生評價
  58——每個新角色一出生就過得了。這是 §7.3 自己寫的、也是 DEMO 要的，但它在 S21 組裝
  DEMO 之前就到了。後果：邊緣選手 12 段 → **1 段**（`五個等第都出現得到` 只靠那一段撐著）、
  打進主流賽區 64 → 76 段、平均生涯薪資 8511 → 11212 萬（後兩項超出 §17.2 的 ±10%）。
  同一個根因：沒有淘汰，生涯普遍變長（showcase 13 個職業季 → 17 個）。
- **基礎成長值 `GROWTH_BASE = 0.80`——比 1 小不是筆誤**。直覺以為刻度 ×1.25 要把預算
  也 ×1.25，但要練的距離短了一半，需要的預算自然也少。上限被上面那件事鎖死：0.85 以上
  邊緣選手會歸零。
- **§17.2 全表套完，三類分開**：門檻／寬度 ×1.25、per-point 係數 ÷1.25、`games`／底薪／
  合約係數／心理六維不動。**另外抓出四處 §17.2 沒點名但同一條規則必須跟著動的**：
  `formFactor` 的中點與係數（一個 ×1.25 一個 ÷1.25）、`career.js` 的 `PEAK_FLOOR`／
  `PEAK_PER_OVR`（**方向相反**，只改一邊等第分布會垮）、外援名額的 `over × 5 → ×4`、
  大局觀特質的 `macro ≥ 60 → 75`。
- **年齡衰退用機率性取整**：§7.2 的 −2.5／+1.25 帶小數而屬性是整數，直接 `Math.round`
  會把 2.5 一律進位成 3，等於把 30–32 歲那段衰退平白加重兩成。
- **`harness.allocate` 沒有照 S07 說的「等比換算價位門檻」**，改成直接讀引擎的
  `decayCoef`——新制成本是連續的，再抄一份門檻表只會又埋同一顆雷。驗證過字面翻譯版
  （只讀級距係數）是 0.0236，比現行的 0.0260 更差。
- **刪掉決定論殘留**：`golden.mjs`／`golden.json`／`determinism.mjs` ＋ `test:golden`
  script（刪前確認 `invariants` 綠）。`ARCHITECTURE.md` 的測試那節一併改寫。
- **後記（同一站的第二段，經人確認後才做）**：三項紅燈採「改量法」而不是「改 §7.3」。
  三條紅的原因不同所以處理不同——兩條是**分母錯**（門檻寫成 `× 屬性上限`，那個寫法對
  刻度免疫但對起始值不免疫），改成 `× 可成長空間`；**嚴格度一字未放寬，只是換單位**：
  舊制可成長空間是 0.405×上限，所以 `0.0375×上限 ＝ 0.0926×空間`、`1.5 OVR ＝
  0.0463×空間`，拿舊程式跑兩種寫法給出同一個判定。第三條「國際當量 ≥ 1.4 倍」是比值、
  沒有單位可換，**刪掉**：它在舊程式上八組種子就有兩組誤報（1.15–4.37），S07 早就用
  `invariants` 的低變異版本取代同一個主張，而 S09 之後它系統性翻負（四組 0.71–1.22）。
  照 S07 的規矩做過「故意弄壞 → 確認會紅」（兩種打法都走 focus → `0.0000`；老手只准投
  第一個屬性 → `-0.3984`），四組獨立種子全過。`npm test` **10586 項全綠**。
- **⚠ 但訊號衰減沒有被修掉，只是量對了**：刪掉那條之後，頂端訊號只剩 `invariants` 的
  「頂端才兌現」一條守門員，餘裕從舊制 0.129–0.202 掉到 **0.078–0.130**——`alt-*` 那組
  的 0.0777 已經在門檻 0.08 之下。**四組種子會誤報一組**，狀況跟 S07 當年描述 smoke
  那兩條一模一樣。沒動它的門檻（它量的是真訊號不是單位問題），寫進交接筆記第七節。
- **狀態**：完成，`npm test` 全綠。下一步 S10 十二技能表——`data/skills.js` 這一站一個
  數字都沒動，位置身分三條全綠且與入口一致。

## 2026-08-12 — S08 規格書補洞：八個洞補完，丙組的數值有依據了

- **方向**：丙組（S09–S13）每一站都要引用 `ESPORT-DESIGN-V4.md` 的數值，但它有九個地方
  寫不下去或寫錯了。這一站**只改文件、不碰程式**——`npm test` 前後都是 10437 項全綠。
  規則是「每個補上的數值都要寫推導」，所以做法是**先量現行遊戲、再訂數字**，不是照抄
  §17.2 那份 ×1.25 等比草案。
- **最重要的一件事：那份草案的 par／min 其實是對的，錯的是它沒寫到的東西。** 說明書
  警告「等比放大不夠」，所以先驗算：把舊成本階梯與新潛力衰減各自跑同一條抽象生涯、
  各自把訓練預算二分搜尋到「平均巔峰 ÷ 上限 = 0.755」，兩邊的巔峰分位數 ÷上限 對得上
  到小數第三位（p2 .707/.709 … p98 .830/.823）。**刻度換算不移動相對位置**，所以門檻
  等比放大是對的。真正的坑是草案完全沒提的三類：① **per-point 係數要除以 1.25**
  （勝率斜率 2.2→1.76、薪資 delta 0.05→0.04、數據生成一整排）；② **`g` 單季場次根本
  不該縮放**——草案把 36/48/60/66/70 乘成 45/60/75/82/88，場次是賽制事實；
  ③ **`delta` 是「高於聯賽 par 幾點」不是「今年成長幾點」**，是水準量，門檻要跟著放大。
- **留給 S09 的地雷**：**潛力衰減比凸成本階梯少獎勵集中加點約三成**（同一條抽象生涯上
  0.0162 對 0.0236×上限，因為天花板係數會懲罰把單一屬性推到頂）。現行實測領先是
  0.0464×上限，折三成約 0.032，而 S07 微基準門檻是 0.0375——**餘裕會變薄**。修法寫進
  §7.1：把天花板係數的底 0.34 調到 0.5（實測回收約 15%），**不是放寬 S07 的門檻**。
- **起始屬性（洞 3）寫成「潛力的比例」而不是固定區間**：主屬性 ×0.80、其餘 ×0.70。
  區間寫法會讓「平庸 58」的屬性一出生就頂到天花板、第一年零成長空間；比例寫法則天花板
  越高剩餘空間越大。量現行遊戲的職業首季得到 0.86／0.58，主屬性調低是為了讓 DEMO 看得到
  成長，其餘調高是因為 DEMO 新人沒有業餘期的加點史、不該一出場就帶著最佳化的畸形 build。
  600 段生成樣本驗算：位置加權起始評價 **58.0**（＝現行實測 46.4 × 1.25），五路 57.7–58.6。
- **明星效應（洞 7）**：`min(6.0, 0.06 × (P − par)^1.35)`。上界 6.0 點 ＝ 勝率 +10.6pp，
  由「最極端的組合不能頂到 92% 上限」回推——滿等選手配生成下界隊友打世界賽淘汰賽是 76%。
- **洞 7 後半驗算後否決了一個機制**：原本考慮讓國際賽對手針對玩家最弱的技能。量過發現
  它打到錯的人——集中加點的「P − 最低核心技能」是 **10.13**，平均加點只有 **5.92**
  （SUP 最極端 14.63 對 7.06）。那等於懲罰集中加點，會把 S07「加點在頂端才兌現」的訊號
  吃掉三四成。改由對手強度階梯（MSI／世界賽有絕對地板 72／74）＋ §9.3 受迫性失誤承擔，
  後者吃的是抗壓與韌性、跟加點無關。
- **配方表（洞 2）**：四階全開，8 稀有 ＋ 10 史詩 ＋ **20 傳說**（每個史詩恰好進兩條）。
  傳說加一道**生涯門檻**（國際賽四強），因為配方 6→20 而素材面沒變——素材面上界實測
  58.1%，現行已經吃到 41.3%，而 S07 的硬上限是「不得超過一半」。門檻選單附了九個候選的
  實測聯合命中率，採用的那個是 30.0%。
- **意外發現：配方表有一半是死的，錯不在配方而在素材。** 實測 **鐵人 0%（完全沒有
  來源）**、休息室黏著劑 0%、全民偶像 0%（條件讀已廢除的 `rep`）、逆風翻盤 1.9%、
  神主牌 6.9%，連帶 4 條稀有 ＋ 3 條史詩配方全是 0%。另外 `諧星` 兩個素材都很常見卻 0%
  ——表的行序讓別的配方先把素材吃掉了。所以 §14.2 多了兩條衛生規則（死配方規則、
  同池撞素材取稀缺度最低者），五個素材列為 S18 的硬需求。
- **順手做的三件**：§16.3 冠軍積分改寫成已實作的史實制度（單一表 90/70/45/20/5，
  不是原本的春夏兩套）；§0.1 加一段現況補記回答 S06 留下的問題（原本三條一字未動）；
  §21 加一張「原本留在這裡、現在定在哪」的對照表並移出已定案項目。
- **驗證**：`npm test` 10437 項全綠（項數不變）；`git diff --stat -- src/ tests/` 為空。
- **狀態**：完成。下一步 S09 屬性 0–100 ＋ 潛力衰減（開工前先讀 S08 交接筆記的第二點）。

## 2026-08-12 — S07 測試網替換：不變式 suite 上線，取代即將被刪的 golden

- **方向**：鐵則一的執行站。V4 §20.1 廢決定論等於廢掉 `golden.json`（兩次大改平衡時
  唯一的安全網），S09 就要刪。這一站先把替代的網子織好——**在動任何核心層數值之前**。
- **先量再訂門檻**：設計不變式之前掃了**八組獨立種子**（各 160 段生涯）。結論比預期
  嚴重：生涯層級的稀有事件指標抖到不能當門檻。國際賽冠軍當量比實測 **1.15–4.37**、
  平均巔峰差實測 **1.43–4.91**——`smoke.mjs` 現行的 ≥1.4 與 ≥1.5 兩條門檻，**八組裡
  有兩組（`alt-*`／`e-*`）會誤報**。那兩條是配著 `seed-*` 這一組樣本長出來的。
- **處理方式**：依說明書「不得放寬現有門檻」，`smoke.mjs` 一個字不動；新 suite 用低
  變異的量法重寫同樣的主張，兩邊並存。判讀規則寫進交接筆記：**smoke 紅而 invariants
  綠 → 先懷疑種子效應**。
- **三個降變異手法**：① 微基準取代生涯統計——同一份骰子、只換加點策略、直接比 OVR，
  相對變異從 3.4 倍降到 5%；② 比例取代絕對值——門檻一律除以 `ATTR_CAP`，S09 換 0–100
  刻度時不必重訂；③ 換個問法取代比值——「加點在頂端兌現」改問「有國際賽冠軍者的巔峰
  是否明顯高於無冠者」，樣本從個位數變成幾十段。新 suite 八組獨立種子全綠。
- **改動**：只新增 `tests/regression/invariants.mjs`（15 條已生效檢查 ＋ 6 個 SKIP）。
  `src/` 零改動，`harness.mjs` 也沒動。
- **故意弄壞驗證**：13 種破壞逐一套上、確認會紅、還原，實際紅的訊息全抄進交接筆記。
  過程中**刪掉一條自己剛寫的檢查**——「老手國際當量不劣於新手的比例 ≥0.65」八組種子
  都穩，但把老手策略改到崩掉（巔峰 62→35）它仍然是 0.81：160 段裡多數配對兩邊都是
  0 冠，比例被平手灌滿。跑得過但量不到東西，正是這一站最該避免的產出。
- **留給下游的兩個地雷**：① **不要拿「每路最高／最低權重倍率」當位置身分門檻**——現行
  3.28 倍，但把 V4 §8.1／§8.2 十二技能草案折疊起來算是 **2.33 倍**（§6 把體力抽成獨立
  資源，`vit` 五路歸零），照規格書做也會紅；改用 L1 距離／前二佔比／輔助靈巧三個量法。
  ② `harness.mjs` 的 `allocate` 寫死 1–80 刻度價位門檻，**不在 S09 的檔案清單裡**，
  換刻度時漏掉會讓微基準失效。
- **驗證**：`npm test` **10437 項全綠**（S06 是 8943 項），2.2s → 2.4s；
  `git diff --stat -- src/` 為空。
- **狀態**：完成。下一步：S08 規格書補洞（丙組全部依賴它），之後 S09 屬性 0–100。

## 2026-08-12 — S06 授權線：README 授權敘述改寫、git 歷史保留

- **方向**：A 批淨室重寫（S03–S05）全數完成後，把「文件還寫著舊狀態」與「repo 歷史
  躺著原始棒球遊戲」這兩件事攤給人拍板。AI 不代決——只列事實與後果，等決定後照做。
- **人的決定**（問答確認）：① LICENSE／README **移除原作者授權狀態敘述**（等待作者
  回覆、未獲正式授權、請保留出處標示），改為「**靈感來自** YaKyuLife／LeoGGcat」，
  並說明清楚：承袭程式碼已在 S03–S05 全部淨室重寫，**現今專案不含原作者程式碼**。
  ② git 歷史 **保留不動**（3c23597～3379e0b 共 20 個原始棒球 commit 原樣留在 repo）。
- **改動**：只有 `README.md` 兩處——開頭引用塊（「大幅改寫衍生作品」→「靈感來自、
  不含原作者程式碼」）與「授權與出處」節（原始出處/本改寫/授權三條改寫為 靈感來源/
  開發者/授權：MIT）。`LICENSE` 是標準 MIT 掛 Vik1n9，本來就無原作者授權敘述，未動。
- **刻意沒做**：`ESPORT-DESIGN-V4.md` §0.1 依說明書未碰（其「未獲授權」敘述是否
  隨 README 一起調整留給 S08）；`ARCHITECTURE.md` 血緣表處置欄在 S03–S05 已標完成，
  無「待淨室重寫」殘留；未執行任何歷史改寫指令（filter-repo／rebase／push --force）。
- **驗證**：`npm test` 8943 項全綠，項數不變（S06 未碰程式碼）。
- **狀態**：完成。**甲組（S02–S06）全數完成**。下一步：S07 測試網替換或 S08 規格書補洞。

## 2026-08-12 — S05 淨室重寫：regions/formats 資料結構重排（內容一字未改）

- **方向**：S02 血緣表把 `data/regions/*`（A/B）與 `data/formats/playoffs.js`、
  `data/{coaches,disband,eras,heroes,leagues,teams}.js`（e07427c 拆自 world.js 的
  A 批）排入 S05。內容是查證過的公開事實（a140b9e／3af552d 的 LoL 改寫），要換的
  只有資料結構的排法——行為等價，golden 零偏移。
- **重排方向**（看過 S14 月回合制後選定）：`{ until }`「最後適用年」→ `[from, to]`
  閉區間，賽段史直接變成時間軸表，S14 做月序時按區間分配月份即可；region 欄位
  分成 `ladder`（聯賽靜態屬性）與 `timeline`（賽段／席位／隊名時間軸）；home 的
  `teamsByEra` 與其他區的 `teams` 統一成同一張區間表。
- **散表聚合**：playoffs 兩表合一、disband 巢狀物件→陣列、coaches 物件→陣列、
  heroes 陣列＋派生索引、teams 業餘場景聚合、eras 的 if 鏈→查表。
- **關鍵取捨**：測試依賴的導出契約全保留（teamNamesOf 簽名、TEAMS_AMATEUR、
  DISBAND_YEAR、LEAGUES/BUCKET_NAMES、eraOf().home/.salary）——測試全屬 B 批，
  不為重排動測試。`eraOf` 順手移除 `msi`/`worlds` 兩個全 repo 無使用者的廢棄欄位
  （史實已住進 `data/formats/*`）。coaches 陣列順序＝原物件鍵順序，因為 `rng.pick`
  依索引取，換順序會改到生涯結果。
- **驗證**：`npm test` 8943 項全綠、`history` 350 項全綠、golden 對入口（S03 commit）
  零 diff；雙版本逐值對比 16 個年份 × 8 個 region × 7 個 eraKey 的查詢全一致。
- **狀態**：完成。下一步：S06 授權線（甲組淨室重寫全數完成後）。

## 2026-08-12 — S04 淨室重寫：market/career（含 transfer 的 A 批段落）

- **方向**：S02 血緣表把 `engine/{market,career}.js` 標 A 批、`phases/transfer.js`
  標 A/B（FA 模型主體承袭、轉會窗口 B 批）、`phases/{media,salary}.js` 標純 A。
  V4 保留這些機制，所以排淨室重寫——只看介面與測試，關掉原檔重寫，行為等價。
- **A/B 分界**：transfer.js 以 `ef33e1b` 為界——rumours／buyout／run 的 heat 段落／
  freeAgency 外援名額卡是 B 批逐字保留，其餘 6ec9575 從 game.js 搬的 FA 模型主體
  （業餘三層出路、青訓、解散、休息室清算、自由市場）重寫。`msi/worlds.js` 的 run
  已被整段 B 批覆蓋，無可單獨提取的骨架，未動。
- **重寫重點**：career.js 把單季貢獻抽成 `seasonScore`、榮譽加分表驅動；market.js
  候選賽區改表驅動、續約/報價選項拆成 `renewalOptions`/`offerOptions` 小函式；
  行為全部維持——rng 消費順序是行為等價的命脈（shuffle→importSlotOpen→pick→
  sample→next→int 的順序不能動，連「失敗的試訓也會先消耗 pick」這種細節都要留）。
- **驗證**：`npm test` 8943 項全綠；golden 相對 S03 入口零變動（「160 段生涯與基準
  一致」逐位元過）。另做雙版本對比：160 段生涯 careerScore/careerTier 全一致；
  market.js 全部匯出函式輸出與 rng 流逐位一致。
- **完成定義備註**：說明書的 `git diff v4-before -- golden.json` 在 S03 後不再為空
  （S03 換 rng 重刷過 golden）。S04 的正確基準是 S03 commit，對它 diff 為空。
- **狀態**：完成。下一步：S05 淨室 regions/formats（或 S06 授權）。

## 2026-08-12 — S03 淨室重寫：rng 換 xorshift32、進入點與版面重寫

- **方向**：S02 血緣表把 `core/rng.js`、`main.js`、`ui/board.js`、`styles.css` 標 A 批，
  而 V4 沒有理由改它們，所以排 S03 淨室重寫——只看介面與測試，關掉原檔重寫。
- **rng**：sfc-like（mulberry32 系）→ **xorshift32**（13/17/5）＋ **FNV-1a** 種子 hash；
  `gauss` 從 4-均勻近似改 **Box-Muller**（截斷 ±4σ）。介面零變動：`Rng` class
  （`state`／`seedString` 可讀寫）、`next/int/chance/pick/shuffle/sample/gauss`、
  `clamp`／`randomSeed` 全保留，存檔格式（`rngState` 32-bit）不變。
- **關鍵發現**：換 rng 後 smoke 不變式「國際賽冠軍當量 ≥1.4 倍」在測試種子下掉到
  1.34。逐項排除：gauss 實現不影響（三種測法全 1.34）；cyrb128 系 hash 全不過；
  唯一過關是 **FNV-1a（2.07 倍）**。多組種子 FNV 中位 ~1.7——結論是該指標對種子
  敏感（國際賽冠軍是稀有事件），不是 rng 品質問題。
- **styles.css**：整檔重寫（選擇器契約零遺漏，與 v4-before 逐選擇器比對）。變數
  語義化重命名、註解重寫、幾何微調；視覺維持 v4.3 設計語言（tftactics.gg 系色票）。
  前端重設計那輪已合併，無進行中衝突。
- **驗證**：`npm test` 全綠 8943 項（golden 重刷）；存檔往返 PASS（checkpoint 還原
  續跑 rng.state 一致）；瀏覽器冒煙（本機 Chrome＋playwright）零 console error，
  開場／reroll／開局／面板／alloc→choice 推進全正常。
- **狀態**：完成。commit 標明「rng 演算法更換」。下一步：S04 淨室 market/career。

## 2026-08-12 — S02 血緣審計：把「哪些是承襲的」從粗分釘成逐檔標記表

- **方向**：原作者未回覆授權申請，承襲程式碼要淨室重寫；但 `00-共通規則.md` 的 A/B
  粗分是用 `git log --diff-filter=A` 查檔案建立時間得到的，對 refactor 搬移會判錯。
  這一站逐檔逐 commit 查血緣，產出 `ARCHITECTURE.md`「## 九、程式碼血緣」表（61 檔）。
- **關鍵發現**（粗分 vs 實際）：
  - `data/formats/{calendar,msi,worlds}.js` 是 B 批——6ec9575 年曆表、a140b9e MSI、
    3af552d 世界賽都是 2026-08 新寫，不是從 world.js 搬的。S05 原清單把它們當 A 是錯的。
  - `kernel/series.js` 是 B：667ec4c 搬自 `engine/playoffs.js`，而 playoffs.js 是
    7d19dce 新建（種子序門檻首見於此，原棒球專案沒有）。`kernel/strength.js` 才是 A
    （搬自 a71ee13 的 team.js）。
  - 00 規則漏列一整批 A：`data/{coaches,disband,eras,heroes,leagues,teams}.js`
    （e07427c 拆自 world.js）與 `ui/{dom,log,panel,runner,storage,summary}.js`
    （a71ee13 建立）。前者補進 S05，後者比照 actions.js 交給 V4 UI 階段（S16）。
  - `phases/{msi,worlds}.js` 骨架承袭、本體已被 2026-08 重寫覆蓋——A/B，殘留 run
    骨架隨 S04；`phases/{media,salary}.js` 是純搬移 A，已加進 S04。
- **改動**：只動文件（ARCHITECTURE.md 新增血緣節；00-共通規則.md 第二節重寫；
  S04/S05 說明書「要動的檔案」依審計修正；02 交接筆記回填；狀態表更新）。未碰任何
  `src/` 檔案。
- **狀態**：完成。`npm test` 前後皆 8939 項全綠。tag `v4-before` 之後的 commit
  `8d62c7a`（golden 重刷）仍是這站的入口基準。
  下一步：S03 淨室 rng，或 S04/S05。

## 2026-08-12 — v4 穩定點：換掉一條量不到東西的平衡不變式

- **方向**：四階特質合成紅著提交（2 項檢查失敗），V4 重建需要一個已知良好的比對基準，
  先把地板掃乾淨。
- **改動**：`弒神者` 拿掉 `ovrAdd: 4` 與 `seriesGame: 6`——兩項都是不管點投得準不準
  都照領的平白加分，份量改由 `growthMult` ×3 → ×3.5 承擔。
- **關鍵發現**：「新手打法傳奇要罕見（不到老手三分之一）」這條門檻已經量不到東西了。
  六屬性改制拿掉「投錯格子」的懲罰之後，兩種打法的分布幾乎重疊——最高屬性 ≥76 是
  15/80 對 13/80，集中度中位數新手甚至略高（8.83 vs 8.67）。80 段裡個位數的傳奇，
  ×3 的比值等於在量雜訊：掃 `growthMult` ×1.6～×4，傳奇數非單調（5/2→2/3→5/3→5/4）。
- **換成**：國際賽冠軍當量（世界冠 ×2 ＋ 世界亞 ＋ MSI 冠）老手 ≥ 新手 1.4 倍。
  三組獨立種子量到 1.92 / 1.69 / 1.88 倍，穩定；同樣三組的生涯評分中位數有兩組是
  新手贏，國際賽出場數只有 1.06～1.33 倍，兩者都不能用。故意把兩種打法弄成一樣，
  新檢查確實紅在 1.00 倍。
- **為什麼是這個指標**：加點準度只在頂端兌現。常規賽門檻低，練得糊也打得過；世界賽
  與 MSI 的對手是全聯盟頂端，位置戰力差幾點就被吃掉。參考機制來自賽馬娘的後期賽事
  門檻拉高。目前這是勝率公式的副產品，**還不是被設計出來的機制**——要把它做成明確的
  「對手強度隨賽事等級階梯」已記進 `docs/v4/08-規格書補洞.md` 的洞 7。
- **狀態**：完成。8939 項全綠，golden 已重刷，tag `v4-before`。
  下一步：S02 血緣審計，或 S07 測試網替換。

## 2026-08-12 — v4 穩定點：把「現在是什麼樣子」釘死一次

- **方向**：v4 一口氣改掉賽制、種子語義與養成模型三件事，散在 CHANGELOG 與 WORKLOG
  的敘述已經追不上現況。新增 `docs/v4/01-穩定點.md`，記錄已驗證的基準：承重牆、
  安全網覆蓋範圍、量到的分布、已知缺口，以及「怎麼算還站在這個穩定點上」三條自檢。
- **對出來的裂縫**：`determinism.mjs` 的「同種子＝同天賦」比對寫 `s.ability`，
  但六屬性改制時已改名 `state.attr`——兩邊都是 `undefined`，屬性那一半從此沒被比對到。
  同一支 suite 還有一條 `!== undefined` 的恆真檢查。前者改成 `s.attr`，後者改成比對
  `potential`（出生固定、只讀不寫，是唯一驗得了「天賦沒被人生改寫」的欄位）。
- **文件漂移**：`README`／`ESPORT-DESIGN` 還寫 `node tests/headless.mjs`（v4 已拆成
  `tests/run.mjs`）；`ESPORT-DESIGN` 的規則對應表整張指向 `world.js`／`abilities.js`／
  `international.js`／`team.js` 這些已不存在的檔；`ARCHITECTURE` 目錄樹還列著
  `abilities.js`。最嚴重的是 `ESPORT-DESIGN` 與 `WIKI` 的國際賽章節仍寫著「MSI 徵召、
  可婉拒、國際賽消耗」——v4 整套拆掉的國家隊模型還留在玩家會讀的攻略裡。全部重寫。
- **順帶**：`PRODUCT` 補上「可加點的屬性列 vs 唯讀的技能列必須看得出差別」與
  「心理值不給數字」兩條設計原則；`DESIGN` 補上 `.abrow` 的 static 態與 `<details>`
  展開列的規格。
- **狀態**：完成。8935 項檢查全綠（檢查數與修正前一致——替換掉的是空檢查，不是刪檢查）。

## 2026-08-12 — 九項素質縮成六屬性，舊素質降級為導出技能

- **方向**：九個滑桿每季來一次，決策沒有質變只有量變，玩家會用「哪個 OVR 權重高就
  餵哪個」把它變成打卡。改成 D&D 兩層：六屬性（體能／靈巧／意識／技巧／默契／決斷）
  是玩家唯一經營的一層，原九項素質＋位置專屬共 13 項降級成技能，由屬性加權導出、
  不能直接加點。
- **關鍵取捨**：技能刻意只掛 2～4 個屬性。第一版讓每項技能都沾到五、六個屬性，折疊
  後五路的屬性權重長得差不多，位置身分被平均掉。寫純之後輔助的靈巧權重掉到 .028。
- **通膨**：同一份訓練點分給六項而非九項，每項多吃 1.5 倍。實測巔峰 OVR 60.7 → 63.7、
  傳奇 5 段 → 14 段。成長門檻整體下移 4 之後回到 59.9 / 5 段。
- **量到但沒解的事**：老手 vs 新手打法的差距從 17.5 點 OVR 縮到 2.3 點。查下來是結構
  性的——舊制的懲罰有一半來自每路有 2/9 素質權重為 0，平均分配等於丟掉兩成的點；
  六屬性對每路都有份量，加上權重和固定為 1、成本階梯是凸的，平均分配拿到的就是
  平均值。要再拉開只能重新引進「對某些位置無用的屬性」，那正是這次要拿掉的東西。
  兩條冒煙不變式因此換了指標（守「傳奇仍罕見」與「巔峰 OVR 差距」）。若之後想找回
  加點的份量，方向應該是讓 OVR 對「尖峰」有非線性回報，而不是加回死屬性。
- **改動**：`data/attributes.js`（新）、`data/abilities.js` → `data/skills.js`、
  `engine/abilities.js` → `engine/attributes.js`；`state.ability` → `state.attr`；
  事件卡 `ability` → `attr` 全部重對；面板拆成屬性（可加點）與技能（唯讀）兩區；
  `SAVE_VERSION` 6 → 7，舊存檔作廢。
- **狀態**：完成。8935 項檢查全綠（黃金種子已重新 baseline），瀏覽器實測加點列六列、
  面板技能區數值與權重表一致。
- **未一起處理**：同期另有一輪前端 UI 重設計在進行，`styles.css` 歸那邊改，所以這次
  的新標記全部沿用既有的 `.abrow` 類，沒有新增樣式。

## 2026-08-12 — v4.0.0：賽制脫離棒球模型

- **方向**：原參考專案是棒球，賽制與過程跟 LoL 差距極大。最刺眼的是 MSI 被寫成
  「國家隊徵召」——可以婉拒、資格看個人 OVR、打完累積傷病風險。MSI 是俱樂部賽事，
  門票發給戰隊。追查下去共 12 項棒球殘留，橫跨整個年度流程。
- **切分方法論**：模組邊界不靠美感決定，用倒推測試——拿真實歷史 BUG 與已知未來改動
  當測試集，數每項要開幾個檔。這個方法推翻了三個原本覺得理所當然的決定：按階段切
  完全沒碰到散在十個檔的特質效果；賽區按屬性切會拆散實體（新增賽區從 1 檔變 4 檔）；
  資料與邏輯分家要看改動頻率而非一律分。
- **賽制**：MSI 改俱樂部身分並歸位到賽季中段；世界賽門票改種子序決定、最後一張打
  地區資格賽；兩者都改用 BO 系列逐輪出比分（`kernel/groups.js` 新增小組循環與
  Swiss）；席位數與種子序來源隨時代切換。
- **過程**：先發／板凳二元制（體力進表現不進場次）、傷勢改成缺席週數、轉會窗口每年
  都開、外援名額（各區每隊 2 名，出海的硬門檻）。
- **種子**：拆成出生流與人生流。種子只決定天賦，人生每次都不一樣；引擎本身仍可重現，
  所以存檔續玩與黃金種子回歸都還成立。順帶修掉 `createState` 把 `seed` 宣告兩次的
  BUG（出生種子從來沒存進 state）。
- **架構**：`game.js` 1033 → 221 行，年曆變成一張表，賽事各自住進 `phases/*.js`。
  新增一個賽事＝寫一個檔、註冊一行、年曆加一列。
- **測試**：拆成 kernel/phases/history/regression 四區，runner 自動掃描；新增黃金
  種子快照（純重構必須逐位元一致）與 history 史實斷言。4047 → 9400 項。
- **量測**（80 段生涯）：MSI 出賽人均 1.32 次、38% 的生涯至少打過一次；世界賽冠軍
  從人均 0.83 座降到 0.09 座；51% 的生涯被下放過板凳；整季報銷從所有重傷降到人均
  0.05 次。大心臟依國際賽經驗分層 47 / 64 / 85 / 93。
- **同批**：另一輪獨立進行的前端 UI 重設計（樣式表 v3.1 → v4.3，改以 tftactics.gg
  的計算樣式為準）也在這次一起合併。那批檔案在合併時被掃進 `6ec9575` 與 `3af552d`
  兩個賽制 commit，不是獨立 commit——查 UI 變動要用 `git diff`，別照 commit 訊息找。
- **狀態**：完成。

## 2026-08-11 — v3.4.0：賽段制、BO 系列、心理／性格軸

- **方向**：一次補上四件互相咬合的事——一年只抽一次卡不夠、季後賽沒有過程、
  沒有心理維度所以下剋上劇本不成立、也沒有任何機制讓「和隊友吵翻」或
  「被輿論淹沒」發生。
- **賽段制**：`splitsOf(year, region)` 依史實把一年拆成 1～3 個賽段
  （2012 單季 → 春／夏 → 2025 三賽段；韓國 2012–2014 本來就是 OGN 冬／春／夏，
  歐洲 2023 起三賽段）。每個賽段各自跑例行賽、季後賽、抽事件卡，場次按權重切分——
  賽段變多的是決策點，不是比賽場數。全年冠軍點數決定世界賽種子序。
- **BO 系列**：新增 `engine/playoffs.js`。季後賽從「一次 rng.chance 吐冠軍」改成
  八強 BO3 → 四強 BO5 → 決賽 BO5 逐局模擬，種子序決定從哪輪打起，決勝局另外
  吃大心臟加成。每輪進場與決賽前各留一個扮演路口。
- **心理／性格軸**：新增 `data/mental.js` + `engine/mental.js`，五個維度
  （大心臟／隊友默契／自我／知名度／風評）。刻意跟能力值完全分開：訓練點碰不到，
  只能靠扮演卡累積。知名度與風評分兩條軸，才做得出 Bin 那種「人氣與負評同時暴漲」。
  加了邊際遞減，否則長生涯會把每一項推滿，性格就變成打卡。
- **扮演卡**：新增 `data/roleplay.js`，18 張，分賽前記者會／媒體採訪／休息室／
  教練談話／日常訓練五個時機。**不擲骰**——扮演不是賭博，選了就是那樣的人；
  隨機的只有外界反應，力道由知名度放大。7 個性格特質＋2 個史詩合成
  （奇蹟劇本、話題製造機）。
- **後果**：`clubVerdict()`——默契見底被迫轉隊（Aming）、風評見底被球團切割，
  合約直接作廢；反過來知名度高則續約時被迫加薪留人。三年冷卻，避免變成常態。
- **顯示**：心理值全程不給數字，面板只出粗略標籤（「一上大場面就抖」「毀譽參半」），
  事件結算只給箭頭。攤成數字玩家就會開始最佳化它，扮演就沒了。
- **量測**（200 段 × 3 種扮演路線）：每段生涯約 100 個決策點、45 張扮演卡、
  38 張能力事件卡、22 場 BO 系列。被開除／被迫轉隊：張揚路線 0.45 次/生涯、
  其餘 0。下剋上奪冠 21–29/200 段。性格特質依路線分化（嘴砲王 95% vs 0%）。
- **狀態**：完成。headless 檢查擴到 19 項全綠；瀏覽器實測賽段戰報、BO 比分、
  扮演卡與人物側寫面板都正常。實測到一段第 3 種子進世界賽奪冠的完整 DRX 劇本。
- **已知限制**：舊存檔失效（`SAVE_VERSION` 4 → 5）。`disc`／`leader`／`laneking`
  這些**能力系**隱藏素質仍偏難，這次沒動它們的門檻。

## 2026-08-11 — v3.3.0：事件卡改成玩家決策

- **方向**：使用者回報「除了訓練和合約，玩家無法觸發事件」。查下來事件其實有觸發
  （每年一張，headless 300 段生涯平均 18.45 張），但 `drawEvent()` 是引擎背著玩家
  擲一次 50/50 就把結果貼出來——玩家從頭到尾只是讀者。全部 6 個 `choice` 決策點
  都是合約／試訓／國際賽／退役，16 張事件卡一個決策都沒有。
- **改動**：每張卡加 `prompt`（處境描述）與 `options`（2–3 條應對路線）。流程變成
  「敘述 → 玩家選 → 結算」。選項欄位 `odds`／`gain`／`loss`／`traits`／`flags`，
  選項按鈕上的成功率與幅度說明由引擎從數值生成，不在資料層寫死。
  `天才操作`／`神之手` 從「固定 70%」改成「所有選項 +20%」。
- **設計**：三條路線互相不支配——安全牌 EV 高、變異低，代價是 `traits: false`
  完全不碰隱藏素質（也不推進自律計數）；穩健路線素質機率最高；冒進成功時
  數值翻倍、失手加重。修掉 3 個純陷阱選項（版本補習「憑感覺」、宵夜「陪坐」、
  夜生活「露個臉」），它們原本 EV 與素質機率雙輸。
- **量測**（300 段 × 3 種打法）：每段生涯 18.7 個事件決策點。平均生涯評分
  穩健 2264／安全 2043／冒進 1826；安全打法解鎖 0 項事件系隱藏素質，
  冒進打法多出 `macroG`、`神之手`。
- **狀態**：完成。headless 13 項檢查全綠；瀏覽器實測選項與結算卡正常。
- **已知限制**：`disc`（自律，1.7%）、`leader`（3.0%）、`laneking`（2.7%）、
  `macroG`（≤1%）仍然極難解鎖——瓶頸在「每年只抽一張卡」與素質本身的額外門檻
  （如 macroG 要 `大局觀 ≥ 60`），這次沒動抽卡頻率與門檻。下一步若要處理，
  應該從提高事件抽取次數著手，而不是調單張卡的機率。

## 2026-08-11 — v3.2.0：業餘階段改為「達標就簽」

- **方向**：使用者要求不要強制三年業餘，數值達標隨時該有職業隊簽約。
  實測後發現問題比想像大：舊版三年期滿的「主場試訓」門檻 45，但那時 OVR 中位數
  只有 37——**成功率 0%**，那個路口是假的，所有人都是被丟進青訓。
- **改動**：門檻拆成三層（青訓二隊 36／主場一隊 45／海外 47），選項依實際達標情況
  產生，打不到的層級不顯示。前三年可婉拒再練一年；第三年起必須決定；連 36 都摸不到
  可續打但最多到 22 歲。順手修掉二隊名稱的時代錯置（寫死名單 → 由當年真實一隊推導）。
- **量測**：離開業餘的年份分布（老手打法 300 段）第 1 年 3%／第 2 年 31%／
  第 3 年 52%／之後 14%。
- **狀態**：完成。測試新增 5 項檢查，全綠；瀏覽器實測 2013 年收到
  「橘子熊 Yoe Flash Wolves 二隊」邀約，時代正確。
- **已知限制**：`TEAMS_OVERSEAS` 仍是單一現代快照（2012 年也會看到 Gen.G／BLG 這類
  後來才成立的隊伍），這是設計文件既有的簡化，未動。

## 2026-08-11 — v3.1.0：起點改為「網咖盃賽」

- **方向**：S2（2012）的台灣沒有校隊、也沒有校際聯賽，「校園電競」這個起點是後來
  才有的東西，套在 2012 年開局上不成立。當年的業餘場景是網咖包台、網咖自辦盃賽、
  排位路人王，選手從網咖被看到再被找去試訓。
- **改動**：`LEAGUES.ACAD` → `LEAGUES.AMATEUR`（名稱「網咖盃賽」）、`stage` 同步改名、
  分區名「校園」→「業餘」、隊名改為網咖聯隊一類、新增 `AMATEUR_CUPS` 讓業餘階段的
  戰報掛上當年盃賽名稱、開場與路口敘述改寫。數值（par／min／games／薪資）全部未動。
  存檔 `SAVE_VERSION` 3 → 4（舊存檔的 `stage` 為 `'ACAD'`，直接失效重開）。
- **狀態**：完成。headless 測試全綠（新增 bucket 名稱與起始階段兩項檢查），
  瀏覽器實跑過完整生涯，結算表顯示「業餘」，無 console error。

## 2026-08-11 — v3.0.1：事件文本與論壇對話在地化

- **方向**：把固定的事件卡與退役留言從「制式敘述」改成更像 LOL 討論區（PTT／巴哈／
  虎噗）的語氣——事件卡加入版本答案、0/10/0、666、斗內、梗圖、宮鬥劇等 LOL 事件與梗，
  退役留言改成連串網民回覆，並放進盲僧、鱷魚、狐狸、VN、瑟雷西等英雄暱稱。
- **改動**：僅修改 `src/data/events.js` 的 `EVENT_CARDS` 敘述與 `FAN_QUOTES`；
  `id`／`name`／`kind`／`ability`／`flags` 完全未動，事件判斷與能力增減不受影響。
- **狀態**：完成。headless 回歸測試全綠。

## 2026-08-11 — v3.0.0：單檔 HTML 拆成分層 ES 模組，修掉一批致命錯誤

- **方向**：v2 是一個 987 行的 `index.html`，CSS／資料／規則／流程／DOM 全塞在同一個
  `<script>` 裡，而且實際上**在載入時就會崩潰**（`$` 定義成 `querySelector`，卻到處寫
  `$('log')` 少了 `#`）。目標是重新分層、讓規則可測，並把設計文件寫了卻沒實作的東西補齊。

- **改動**：
  - 架構拆成 `src/{core,data,engine,ui}`，依賴單向：`data ← engine ← ui`。零建置、原生 ESM。
  - 生涯流程由回呼金字塔改寫成 **generator 流程機**（`src/engine/game.js`），
    UI 透過 beat 協定（card／choice／alloc／checkpoint…）驅動。引擎完全不碰 DOM。
  - 新增 `tests/headless.mjs`：在 Node 裡跑 160 段完整生涯，驗種子決定論、不變式、
    評價分布、合成、版本落差方向、FA 分級、解散名單過濾。`node tests/headless.mjs`。
  - 修正的關鍵 bug（完整清單在 CHANGELOG）：載入即崩潰、`const` 重新賦值、
    每季旗標永不重置（季後賽冠軍／改寫史實／國際賽消耗）、版本補習方向寫反、
    海外選手 FA 被強制降級、史實解散可用時序漏洞繞過、「連續 3 次」不是連續、
    洗牌用隨機比較器破壞決定論、中路 OVR 漏算位置專屬能力。
  - 平衡：生涯評分重新校準（舊版人人「傳奇」）、年度獎項門檻收緊（舊版一段生涯堆 40 幾項榮譽）。
  - 新增：選手資料面板（隨時查能力／英雄池／版本落差／隊友／合約／素質）、存檔續玩、
    英雄池會成長、海外青訓路線真正可走、年度紀錄可折疊。
  - 文件：新增 `ARCHITECTURE.md`；改寫 `README.md`／`CHANGELOG.md`；
    `ESPORT-DESIGN.md` 更新數值並補〈規則對應表〉；`WIKI.md` 更新攻略。

- **更名整理**：移除多餘的 `yakyulife/` 巢狀資料夾（repo 內容移到根目錄，與 GitHub Pages
  實際服務的層級一致）、`YaKyoLife-WIKI.md` → `WIKI.md`、v2 改造計劃書移入 `docs/archive/`。
  GitHub repo 已由使用者改名為 `Vik1n9/esportlife`，本地 `origin` 已更新。

- **狀態**：完成。程式碼與文件都已就緒，headless 測試全綠，瀏覽器實跑過完整生涯
  （校園→青訓→主場→MSI→世界賽→退役結算→分享圖）無 console error。

- **部署**：已 commit 並 push 到 `Vik1n9/esportlife` 的 `main`。GitHub Pages 已啟用
  （來源 `main` 根目錄，強制 HTTPS），正式網址 <https://vik1n9.github.io/esportlife/>，
  線上實跑過完整生涯無 console error。舊網址 `leoggcat.github.io/yakyulife/` 是另一個
  帳號的舊部署，停留在 v2，不再更新。
  `Vik1n9/esportlife` 已解除 fork 關係（`fork: false`、`parent: null`），成為獨立專案。

- **下一步（未做，非阻塞）**：可再擴充的點——海外賽區的歷史解散事件、
  時代跨越時舊隊名的處置（例如球隊撐過賽區更名）、更多事件卡。
