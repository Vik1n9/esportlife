# S26 · 母體百分位（§14.3 回歸）

狀態：完成
前置：S23 S25
預估：1 session
推理難度：中
建議模型：Sonnet 5（`claude-sonnet-5`）
理由：改法已由 S23 定案（母體定義、計算方法、門檻寫法），本站照做。風險在校準
——門檻改了之後兩條 route 的持有率要重驗，不能順手動其他路線。

---

## 為什麼做這件事

§14.3 的究極綠葉與網紅選手兩條 `route` 路線因為「引擎沒有全聯盟逐選手數據母體」
而用絕對門檻暫代（附錄 #32、§21.3 登記）。S25 的 `cleaned_players.json` 就是
那個母體——現在把兩條路線改回百分位定義，關閉 §21.3 的登記條目。

---

## 入口狀態

```bash
node docs/v4/next-station.mjs
```

回報本站（與 S27 並行可解鎖）。S23 已定案百分位改法：母體定義、來源欄位、
門檻寫法。S25 的 `cleaned_players.json` 齊備。

---

## 範圍

### 要做

**1. 母體統計計算（純腳本，零 LLM）**

- 照 S23 定案：母體定義（全選手／同年齡段／同賽區）、由 `cleaned_players.json`
  哪些欄位計算（如場均助攻的生涯分佈）。
- 產出靜態分位數表（如 `data/npc/percentiles.json`），遊戲執行時查表、
  不做即時計算。

**2. 兩條路線改回百分位**

- 究極綠葉：「生涯場均助攻 ≥ 絕對門檻」→「生涯場均助攻**前 10%**」。
- 網紅選手：「`careerScore` < 900」→「生涯評分低於**中位數**」。
- 觸發條件與敘事文字同步改（S23 定案的比例與寫法）。

**3. 持有率重驗**

- 160 段生涯（16 種子 × 五路 × 兩種打法）重跑，驗證兩條路線的達成率仍落在
  §14.2 的 `route` 覆蓋率驗收線內（後 50% 局 ≥60% 拿得到 ≥1 route 標籤）。
- 不達標：調整門檻比例並記錄——**動的是本站兩條路線，不許動其他三條**。

**4. 文件收尾**

- §14.3 回寫定案；§21.3 該條目標記已定案；附錄 #32 標記關閉。
- 交接筆記記下暫代門檻與百分位門檻的量測對照。

### 不要做

- 不碰 `npc_roster.json`（S27 產物）——本站只吃 `cleaned_players.json`
- 不改 §14.3 其他三條路線（賽區統治者／流浪傭兵／不滅隊魂）
- 不動 `kernel/strength.js` 或任何對手模型（S29 的事）

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `tools/`（或 S23 定案位置） | 分位數計算腳本 |
| `src/data/npc/percentiles.json` | 靜態分位數表（位置依 S23 定案） |
| `src/engine/quests.js` 等消費端 | 兩條 route 條件改讀百分位 |
| `ESPORT-DESIGN-V4.md` | §14.3／§21.3／附錄 #32 回寫 |

---

## 規則與不變式

- `npm test` 全綠；`history/` 分區不動（本站不碰賽制史實）
- 分位數表是資料檔：來源與版本寫在標頭（CC BY-SA 註記）
- 160 段重跑前後，兩條 route 之外的所有校準數字不變（golden 基線對照）

---

## 完成定義

- 兩條路線改百分位、測試全綠、160 段 route 覆蓋率 ≥60% 驗收線通過
- 暫代門檻 vs 百分位門檻的實測對照寫進交接筆記
- §14.3／§21.3／附錄 #32 回寫完成

---

## 交接筆記

**2026-08-18 完成。** 兩條 route 路線改回百分位、測試全綠（22721 項，排除並行任務
帶的 disband 紅燈）、規格書 §14.3／§21.3／附錄 #32 回寫、版號 v4.7.0 → **v4.7.1**。

### 產出

- **生成器** `tools/npc/gen-percentiles.mjs`（可 import 的 `computePercentiles`，main
  guard 分離）→ 產 `src/data/npc/percentiles.js`。
- **percentile 節點**：`['percentile', 指標, 比較子, 百分位]` 進 `conditions.js` 的
  `COND_KINDS`／evalCond 與 `tools/schema.js` 的 `COND_NODES`／validateCond；
  `PERCENTILE_METRICS` 單一來源（percentiles.js 產出），`tests/kernel/percentiles.mjs`
  守生成器重算、查詢層、節點註冊三方一致。
- **門檻查詢**：`ledger.js` 加 `assistP90(state)`（依角色）與 `peakRatingP50()`（不分
  位置）——消費端不直接讀 percentiles.js。
- **兩張卡改寫**：`route-green-leaf` 的 trigger／goal 換 `['percentile','assist','gte',90]`
  （保留 awards=0／proYears≥7），`route-influencer` 的 trigger／goal 換
  `['percentile','peakRating','lte',50]`（保留 fameLevel／awards 條件），`goalText`
  同步改百分位措辭。`careerScore < 1200` 條件退役。

### 母體量測與門檻值

| 路線 | 母體 | 樣本 | 門檻 | fallback |
| --- | --- | --- | --- | --- |
| 究極綠葉 | cleaned 同位置有助攻紀錄選手 | **0**（cleaned 無助攻欄位） | P90＝2.5 | **true**（保留絕對門檻，§23.5） |
| 網紅選手 | cleaned 全部可回歸 peakRating | 679 | P50＝**68** | false |

- **peakRating 回歸**（S26 定案，S27 照寫）：世界賽冠軍 ≈ LCK par 級（76）、四強 ≈
  主場 par+4.5（70.5），`RATING_BY_EVENT`／`FINISH_OFFSET` 見生成器。母體 p10=64、
  p50=68、p90=72。

### ⚠ 實測落差（S26 完成定義的 route 覆蓋率驗收線未過）

- **後段局 route 覆蓋率 6.3%**（5/80 段，門檻 60%）——這是 S19c 交接筆記宣稱
  「已新增測試」但**從未落地**的驗收線（6a84c40 的 invariants.mjs 沒有該測試）。
  用 worktree 重跑 S19c 當代碼：後段局覆蓋只有 **5.0%**（交接筆記的 63.7% 與實際不符）。
  現況五張 route 卡後段局達成集中在傭兵／網紅，其餘三張幾乎全失敗。
- **網紅選手 160 段達成 0 段**（暫代門檻 13 段）：`fameLevel=4`（全民認識）與
  `peakRating ≤ 歷史中位數` **結構性互斥**——fame 高的人通常 peakRating 也高。
  改百分位讓這條路徑幾乎關閉。
- 兩條路線改動後，後段局覆蓋率從 7.5% 略降到 6.3%。**動的不是本站兩條路線能修的**
  ——修復（含 route 卡條件重設、網紅與 fame 的互斥、驗收線測試補落地）推給
  **S30 校準重驗站**，本站交接筆記即證據。

### 未一起處理

- S19c 宣稱「新增兩條測試」（傳說 ≥3 ≤12%、route 覆蓋率 ≥60%）實際都沒落地——本站
  補了 percentiles 測試，route 覆蓋率測試留給 S30（現況會紅，補了等於立紅旗）。
- 助攻欄位：Liquipedia 選手頁無 KDA 統計，S25 沒抓到 → 助攻母體樣本 0。若 S25 後續
  補助攻資料，重跑 `node tools/npc/gen-percentiles.mjs` 即切到真百分位。
- ⚠ **並行任務**（連續事件機制，cd82867 之前）帶了 disband seed 26 紅燈
  （`disbandCrises ≥ disband 里程碑數 1/2`）——是它自己的 squat_challenge 事件 bug，
  與 S26 無關。S26 的 22721 項全綠是在排除並行檔後量到的。

**狀態**：完成。`npm test` 22721 項全綠（排除並行任務帶的 disband 紅燈）；
SAVE_VERSION 不動（未改存檔結構）；版本 v4.7.1。
