/**
 * 六大屬性。玩家唯一直接經營的一層。
 *
 * 舊版讓玩家直接加點在九項「素質」上（體力／反應／操作／大局觀／對線／團戰／遊走／
 * 視野＋位置專屬）。九個滑桿每季來一次，決策沒有質變、只有量變，玩家很快就會用
 * 「哪個 OVR 權重高就餵哪個」把它變成打卡。素質不是不重要，是**不該由玩家直接管理**。
 *
 * 現在改成 D&D 的兩層：屬性（這個檔）是天賦與訓練的載體，只有六個；原本那九項降級
 * 成「技能」（`skills.js`），由屬性加權導出、不能直接加點。玩家面對六個決策，
 * 每個決策同時牽動好幾項技能——選擇變少，但每個選擇的份量變重。
 */

/** 屬性鍵的權威順序。面板、加點列、存檔都照這個順序走 */
export const ATTRS = ['vit', 'agi', 'awr', 'tec', 'syn', 'dec'];

export const ATTR_NAMES = {
  vit: '體能', agi: '靈巧', awr: '意識', tec: '技巧', syn: '默契', dec: '決斷',
};

export const ATTR_ABBR = {
  vit: 'VIT', agi: 'AGI', awr: 'AWR', tec: 'TEC', syn: 'SYN', dec: 'DEC',
};

/** 面板上給玩家的一句話說明。寫的是「這個屬性在峽谷裡長什麼樣」，不是形容詞堆疊 */
export const ATTR_DESC = {
  vit: '手腕與續航。撐得住整個賽季的訓練量，後期團戰手不會抖。',
  agi: '反應與手速。閃現躲技能、極限補刀、被抓時的第一時間反應。',
  awr: '地圖意識。小地圖、兵線、對方打野在哪，資訊進得來就活得下去。',
  tec: '機械技術。技能命中率、連招精度、英雄的熟練上限。',
  syn: '團隊協作。跟隊友的聯動、溝通、開團時有沒有人跟上。',
  dec: '決斷力。什麼時候開、什麼時候撤、大場面下敢不敢做那個決定。',
};

/**
 * 屬性硬上限（V4 §7.1）。
 *
 * 1–80 換成 0–100 是等比 ×1.25。刻度換算本身不會移動巔峰分布的相對位置（§7.1 有
 * 兩個成長模型跑同一條抽象生涯的驗算），所以換刻度不是「讓人變強」，只是換單位。
 *
 * ⚠ `神之領域` 舊有的「突破上限」效果已隨 S15b 作廢：0–100 的
 * 100 就是刻度本身的頂，沒有再高一階可言。潛力才是實務上的天花板——見下面的天花板
 * 係數——練得動 100 的人本來就不受這個常數限制。§10.2 那類「直接加評價／突破上限」
 * 的掛載禁令加值一律刪除（v4.3 決策 #44），新特質的效果由 S19a 重建。
 */
export const ATTR_CAP = 100;

/**
 * 潛力衰減係數（V4 §5.3 §7.1）——取代舊的成長成本階梯。
 *
 * 舊制是「玩家有訓練點、屬性越高每點越貴」（凸的成本階梯）；設施制是「訓練活動給
 * 固定的基礎成長值、越接近潛力天花板長得越少」。這是同一件事的兩面，但**換刻度
 * 不換模型會做出四不像**，所以這裡寫成乘法係數而不是成本：
 *
 *     潛力衰減係數 = 級距係數(現值) × 天花板係數(潛力 − 現值)
 *
 * v4.3（決策 #41）把級距從四段離散改成**連續冪函數**，消除舊制等比換算遺毒的斷崖
 * （§0.5 直覺檢驗）：級距係數(v) = max(0.05, (1 − v/100)^n)。`n` 是新增的校準旋鈕
 * （§21.2）——n 越大，貼著 100 時的衰減越狠。實測值見下方 `TIER_POWER` 的註解。
 */
/**
 * 級距冪 n（§7.1 決策 #41 的校準旋鈕）：級距係數 = max(TIER_FLOOR, (1 − v/100)^n)。
 *
 * S15b 實測取 **1.0**——這是舊四段離散級距（1/0.5/0.25/0.14）在中段的自然近似，也是
 * 校準時「先不動、用 GROWTH_BASE 收斂」的起點。n 越大貼著 100 衰減越狠；S15b 的收斂
 * 靠 GROWTH_BASE（3.9）而不是 n，因為曲線把巔峰釘在天花板，n 主要在影響「練得多接近
 * 100」，而 0.755 的驗收線是平均、對 n 不敏感。
 */
export const TIER_POWER = 1.0;

/** 級距係數的地板：貼著 100 時也不歸零（歸零會讓高屬性完全練不動，變成硬天花板） */
export const TIER_FLOOR = 0.05;

/**
 * 天花板係數 = `CEILING_FLOOR + (1 − CEILING_FLOOR) × min(max(潛力 − 現值, 0), 12) / 12`
 *
 * - **0.34 的由來**：舊制超過潛力上限之後成本 ×3（`OVER_POTENTIAL_MULTIPLIER`），
 *   倒數就是 1/3。所以潛力仍然是**軟**天花板——練得夠久還是過得去，只是很慢。
 * - **12 的由來**：0–100 刻度下單一屬性一年約漲 8–12 點，所以「最後一年的距離」才
 *   開始遞減。§7.1 實測把錐度起點從 8 掃到 20，打法差距只從 0.0155 動到 0.0172×上限。
 *
 * ⚠ 不要把 `CEILING_FLOOR` 改成 0（硬天花板）。§7.1 實測硬天花板要 1.85 倍的訓練
 * 預算才能把平均巔峰推回 0.755，而且低端尾巴被拉開——潛力會從「你要練多久」變成
 * 「你這局結束了」，那不是 §5.3 想要的曲線。
 */
export const CEILING_FLOOR = 0.34;
export const CEILING_TAPER = 12;

/**
 * 基礎成長值（§5.3 唯一刻意留到最後才收斂的旋鈕）。
 *
 * 設施制訓練是 S16，這一站還是舊的擲骰加點介面，所以這個常數暫時掛在投入端：
 * 每 1 點訓練成果乘上它，再走衰減係數換成屬性。
 *
 * 收斂方式照 §7.1 末段：固定級距冪 n、天花板係數與 7.3 的起始值，掃這個值讓 160 段
 * 樣本的「平均巔峰 ÷ 屬性上限」回到 **0.755**（S07 的實測基準）。
 *
 * ⚠ S15b（生命週期曲線）把它從 0.80 重校到 **3.9**，這不是通膨，是起始值改動的必然：
 * v4.3 的 §7.3 起始值讀 `effective_potential(16)`（≈ 潛力的 0.4 倍），成長空間從
 * 0.19×上限回到 0.46×上限——要練的距離是 S09 的 2.4 倍，需要的訓練預算自然跟著放大。
 * 而且曲線把巔峰釘在 `effective_potential(peak_age)`，兩種加點打法在夠長的生涯裡都會
 * 頂到同一道天花板——0.755 現在的意思是「人人幾乎練滿自己的潛力」，不是「加點拉開差距」
 * （後者由微基準守住，見 invariants 的 styleGap）。
 */
export const GROWTH_BASE = 3.9;

/*
 * 舊的 `OVER_POTENTIAL_MULTIPLIER`（超過潛力上限後成本 ×3）已經沒有對應的常數——
 * 它的角色被 `CEILING_FLOOR` 接走了：不再是「越線那一刻跳一階」，而是「越接近天花板
 * 衰減越兇」，貼著或超過時掉到 0.34（1/0.34 ≈ 2.94，與舊的 ×3 等價）。
 *
 * S07 交接筆記裡「故意弄壞 → 確認會紅」的配方要跟著換：原本寫
 * `OVER_POTENTIAL_MULTIPLIER 改 1`，現在等價的是 **`CEILING_FLOOR 改 1`**（天花板
 * 完全不打折）。
 */

/**
 * 潛力天花板的四段區間（V4 §7.1，舊值 ×1.25）。
 * 出生時 1 頂尖／1 優質／1 中上／3 平庸，隨機分派到六個屬性。
 */
export const POTENTIAL_BANDS = [
  [90, 100],   // 頂尖（舊 72–80）
  [80, 92],    // 優質（舊 64–74）
  [70, 85],    // 中上（舊 56–68）
  [58, 78],    // 平庸（舊 46–62）
];

/**
 * 職業第一年的起始屬性（V4 §7.3）。
 *
 * 寫成**潛力的比例**而不是固定區間：潛力是先分派的，若起始值走固定區間，一個
 * 「平庸 58」的屬性會在出生時就頂到天花板，第一年完全沒有成長空間。寫成比例則
 * 天花板越高、起始值越高、剩餘空間也越大，不會生出一出場就報廢的屬性。
 *
 * 0.80／0.70 的由來（量現行遊戲的職業首季）：主屬性實測 0.86 調低成 0.80，是為了
 * 讓 DEMO 第一年看得到成長；其餘實測 0.58 調高成 0.70，是因為 DEMO 跳過業餘期，
 * 新人不該一出場就帶著一個已經最佳化的畸形 build。
 *
 * ⚠ v4.3（§7.3）：起始值讀的是 `effective_potential(attr, 起始年齡)`（§7.2）而不是
 * 固定潛力——出生當下天花板隨年齡曲線移動（16 歲的 ceiling_curve ≈ 0.5），所以實際
 * 起始值是這裡的 k_i 再乘一個約 0.5 的曲線因子。k_i 本身經 S15b 重校（見交接筆記）。
 */
export const START_RATIO = { edge: 0.80, rest: 0.70, jitter: 0.03 };
