## [v4.9.0] - 2026-08-19

### 訓練選項六選一（S46，天組）

§5.2 的活動菜單自 S16 上線後一直是八項，規則彼此不同構——有的吃 2 個屬性、有的吃 3 個，
有三項完全不漲屬性。玩家每個月要在八個規則各異的選項之間比較三欄才選得下去。本站把菜單
收斂成 **6 個選項對應 6 個基礎屬性**，決策退化成「這個月要哪兩個屬性，體力還撐不撐得住」。

- **`engine/training.js`**：`TRAINING_ACTIVITIES` 改寫成**錯位輪替**——健身房
  （體能→靈巧）／團隊訓練賽（默契→決斷）／戰術覆盤（決斷→默契）／SOLO RANK
  （技巧→意識）／VOD 研究（意識→技巧）／休息（靈巧→體能），權重一律 .7／.3。
  六個主屬性互不重複、六個副屬性也互不重複，六屬性曝光完全均等。
  ⚠ `vod` 這個 id 指「戰術覆盤」，「VOD 研究」的 id 是 `study`（沿用舊 id 讓訓練卡
  標籤不必大搬）。
- **休息也給屬性成長**：不分成敗、不抽卡、不受傷、不動心理，倍率
  `restYieldMul(休息前的體力)` 落點 100→0.10、60→0.15、30→0.35、0→0.55。滿血休息
  ≈ 訓練月的 10%——曲線本身就是防濫用機制。
- **英雄池練習退場**：改成 SOLO RANK 的附帶欄位 `heroGames: 8`（舊值 16 的一半），
  隨成功係數縮放；大失敗係數 0 → 0 場，不必特判。`kind` 只剩 `train`／`rest`／`rehab`。
- **復健預防隱藏保留**：加 `menu: false` 退出玩家選單（復健年的自動流程仍走它），
  `recoveryOptions()` 非賽事期只剩休息。
- **`data/trainingCards.js`**：60 張不增不減，只搬標籤與改文案——12 張 `mechanics`
  改標 `study`、8 張 `heroes` 改標 `soloq`，文案裡的「復盤」統一成「覆盤」。原 `heroes`
  的 4 張兜底卡改標 `stage: ['PRO']` 歸進 PRO 池，兜底不變式（每活動 × 檔位恰好 1 張
  無條件卡）因此重新成立：兜底 20（5 活動 × 4 檔位）＋ PRO 24 ＋ AM2 8 ＋ 業餘 8。
- **`phases/month.js`**：休息現在會漲屬性，所以要印一張輕量結果卡（無檔位標籤、
  無卡片文案，只有體力回復量與屬性成長）；復健仍不印。
- **`tools/schema.js`**：`ACTIVITY_KEYS`／`ACTIVITY_LABELS` 改為從 `TRAINING_ACTIVITIES`
  導出，不再手抄——手抄的下場就是同檔 `SLOTS` 註解記著的那件事：編輯器答應得出來、
  引擎兌現不了，標到不存在活動的卡是永久抽不到的死卡。
- **測試**：新增錯位輪替不變式（主／副屬性集合各自蓋滿六屬性且主 ≠ 副）、休息成長隨
  體力遞減、編輯器與引擎同源三條；`npm test` 24058 項全綠（S36 為 23756 項）。
  `styleGap` 微基準門檻 0.9 → **0.85**（實測 87.5%）——曝光均等之後，屬性權重平坦的
  MID／ADC 上「選哪個活動」的差別變小，但平均領先反而從 0.137 升到 0.158。
- **量級漂移（交 S50）**：平均巔峰 ÷ 上限 **0.732 → 0.755**、體力節奏休息
  **29.6% → 26.6%**、透支 **27.2% → 24.1%**、間隔 **3.30 → 3.69**、傳奇率老手
  16.3% → 20.0%。依本站範圍 `TRAIN_YIELD` 一個字未動。

## [v4.8.5] - 2026-08-19

### 校準重驗：辛組收斂（S36，Phase 4）

S32–S35 動過數據生成分布、聲量／事件頻率與國際賽對手強度。本站重跑 160 段校準、
逐 commit 歸因，把 §24.4.2 寫死的量測清單逐條收斂。**結論是零常數變更**——四條
驗收線全部落在帶內，依規格「破帶才有授權」的判準，殘項與檢定四常數一律不動。

- **160 段重驗**（`tools/calibrate-160.mjs`）：國際賽局勝率 **53.5%**（錨
  53.1%±1pp 帶內）、頂端落差 **0.178**（門檻 0.08）、世界冠軍人均 **0.081**
  （＝S28 錨，逐顆落錨）、傳說持有 **23.8%**（15–25% 帶內）；體力節奏
  休息 29.6%／透支 27.2%（00b 基準 29.5%／27.0%）。
  `OPPONENT_SUPPORT_RESIDUAL` 維持 **16.5**、`DECIDER_CHECK` 四常數維持。
- **逐站歸因**：同一套腳本在 S31／S32／S33／S34／S35 五個 commit 各重跑一次。
  平均巔峰 74.19 → 73.19、職業年資 11.4 → 10.7、route 後 50% 覆蓋率 11.3% → 6.3%
  的位移**主因是共用人生流 `rng` 的樣本重排**（S32 一個平衡常數都沒動就掉到
  3.8%），可歸因於機制的只有 S34 的 fame 掛載（fame L3+L4 28.8% → 36.9%，
  `route-influencer` 5 → 12 段但全落前 50%）。
- **`state.deciderLog`（新）**：Bo5 高壓檢定的記帳讀點（場次／進第五局／雙邊
  衰減總量），校準腳本印在報表末段。`SAVE_VERSION` 維持 **28**（可選讀取）。
- **`tools/calibrate-160.mjs --intl`（新）**：大賽經驗加成診斷——p2 選隊池的
  原始 n 分布與各係數／上界組合的均值、sd、飽和率（靜態池量測，不跑生涯）。
- **`phases/playoff.js`**：刪掉 S20d 起掛著的 `clutch` TODO。實測結案為誤判——
  160 段有 **65 段取得** `clutch`，其中 63 段被四條史詩配方吃進 `fusedAway`，
  持有數低是合成消耗不是發放沒咬到。
- **`engine/opponents.js`**：`intlExpOf` 拆出 `intlYearsOf`（未夾取的原始 n）與
  具名常數 `INTL_EXP_COEF`／`INTL_EXP_CAP`，`isGlobalBossTeam` 改為匯出——
  診斷工具直接用同一份，不在 `tools/` 抄第二份走訪法（單一來源規則）；值未動。
  `teamCheckM` 檔頭「m=50 中性不衰減」更正——門檻是 60，
  `deciderCheckDecay(50) = 0.204`，不是零。
- **規格回寫**：§24.4.1（intlExp 池內 95.5% 飽和、上界 3.0 下鑑別度與錨點互斥的
  掃值表）、§24.4.2（四常數量測清單對照＋雙邊衰減不互抵的實測，淨 +0.125 偏玩家）、
  §24.2.4（K/D/A 量級約真實 0.45×，與 SUP 無關；K≠D 守恆差 11%）、§14.2
  （傳說／route 逐站歸因表）、§24.6 辛組收尾、附錄 #86。三條實測推翻的估算都是
  **規格決定不是校準旋鈕**，推導留在原節交規格站。

## [v4.8.4] - 2026-08-18

### 國際賽規則與特質連動（S35，Phase 4）

S34 把玩家數據併進國際賽池，但國際賽對手還是吃一般階梯、Bo5 沒有關鍵局壓力、
國際賽里程碑不發特質。本站把 §24.4 的四件 S31 定案落地——常數與落點照 S31，
本站不改量、只接線；偏移量測交 S36。

- **`engine/opponents.js`**：Global_Boss 合流——intl 作用域選池先收斂到
  「五人全為 fetch_priority 2」的隊（`globalBoss: true`），p2 池空落回全池
  （`globalBoss: false`）；`intlExpOf`（先發國際賽年數和，0.5×n 上限 3.0）、
  `regionPatchDebt`（四大賽區 0／HOME 1.5／其餘 2.5）、`teamCheckM`（五人
  psych 均值）；`intlMod = intlExp − debt` 進 `aggregateTeamStrength` 加項，
  聚合式形狀一個字沒動。
- **`engine/psych.js`**：Bo5 高壓檢定常數 `DECIDER_CHECK`（門檻 60、
  comp×0.6＋resl×0.4、衰減上界 3.0、指數 1.5）與 `deciderCheckM`／
  `deciderCheckDecay`。四常數歸 S36。
- **`kernel/series.js`**：`runSeries` 在 bo5 時算雙邊檢定，決勝局經新增
  `strengthShift` 參數走 `gameChance` 的戰力差加項——不進 §9.2 乘法鏈；
  0:2→3:2 翻盤記 `reverse_sweep` 計數（`EVENT_COUNT_KEYS` 新鍵）。
- **`engine/leagueSim.js`**：`recordIntlOpponentMicro`——實體化對手五人
  微觀逐局併進 `intl` 池（局型走 intl ×1.25 方差放大），補上 §24.4.3
  母體的 NPC 側。
- **`phases/{shared,seriesEvent,msi,worlds,playoff}.js`**：`opp`／`intl`
  參數穿透到五拍序列；Swiss 逐輪玩家微觀（S34 漏併修正）；舊制小組賽
  對手微觀（NPC 側新口）；戰報級距帶國際版（`intlBandNote`，母體 <20 不顯示）；
  Bo5 決勝局高壓敘事（只寫現象，測試守文字不洩漏 comp／resl／衰減值）。
- **`kernel/groups.js`**：`runGroup` 逐局附 `oppIndex`、`runSwiss` 逐輪附
  `opp`／`games`／`deaths`——供上層寫國際池。
- **條件語言**：`intlGames`（國際賽生涯累計局數）走 `ledger.js` 查詢層，
  `QUERIES`／`PREDICATES` 雙註冊；`reverse_sweep` 進 `EVENT_COUNT_KEYS`
  （`tools/schema.js` 同步）。
- **`data/epics.js`**：第 21 張傳說 `midseason_king`（季中王者；份量刻意走
  `intlRoll`／`worldsRoll`／`seriesDecider`，避開明星效應 6% 失控警報）；
  三個新 unique——`intl_dominator`（跨賽區高勝率）、`reverse_sweep_soul`
  （讓二追三記帳）、`intl_veteran`（intlGames≥50）。
- **`data/quests.js`**：`legend-midseason-king`（MSI 冠軍里程碑卡，發放口
  仍唯一走任務卡結算）。
- 量測對照（160 段，S34 → S35）：國際賽局勝率 56.0% → **53.5%**（錨帶內）、
  世界冠軍人均 0.119 → 0.081、有國際冠軍 22 → 14 段、傳說持有 21.9% →
  23.8%（帶內）；intlExp 實測對可實體化對手恆飽和 3.0（intlMod 淨 +3.0，
  登記 S36）；p2 落回路徑全年份掃描零命中。`npm test` 23754 項全綠。

## [v4.8.3] - 2026-08-18

### 玩家戰績接入與相對評價（S34，Phase 3）

S32／S33 把 NPC 的聯賽數據做出來了，但玩家的數據還在自己那條線，兩邊不通。
本站把玩家數據併入統一資料池，擴充 DPM 維度，接上動態評價基準與百分位謂詞。

- **`engine/state.js`**：`blankSeasonStat` 加 `DPM` 欄位；`SAVE_VERSION` 27 → 28。
- **`engine/season.js`**：`simulateSeason` 算 `DPM = DMG% × (TEAM_DPM_TOTAL/100)`
  （與 NPC 端共用同一個常數）；`mergeSplits`／`accumulate` 把 DPM 當比例
  處理。新增 `seriesMicroStats`：系列賽（季後賽／MSI／世界賽）的玩家六維，
  陣亡由呼叫端 `seriesDeaths` 帶入、不重算。
- **`engine/microStats.js`**：新增 `accumulateMicroTotals`，NPC 與玩家共用同一份
  累加邏輯（NPC 逐局呼叫、玩家一次寫入一批）。
- **`engine/leagueSim.js`**：`recordPlayerLeagueResult` 把玩家六維併進賽段
  `stats` 池（鍵 `PLAYER_STAT_ID`）；新增 `recordPlayerIntlMicroStats`（`intl`
  段唯一寫入口）。
- **`kernel/leagueStats.js`**（新）：`leaguePct` 節點的查詢層——指標白名單、
  `splitPercentile`／`leaguePercentile`／`intlPercentile`（§24.4.3 國際賽獨立
  基準）／`percentileBand`。母體 < 20 判 null，不勉強切分。
- **`engine/conditions.js`**／**`tools/schema.js`**：新條件節點
  `['leaguePct', 指標, 比較子, 百分位]`，讀當季模擬母體，與讀歷史史實母體的
  `percentile` 節點物理隔離（雙註冊）。
- **`phases/shared.js`**：`recordIntlSeries` 補 `rng` 參數，順手把國際賽系列賽
  的玩家微觀數據併進 `intl` 池。
- **`phases/playoff.js`**：賽段結算戰報附玩家 KDA 百分位級距描述，KDA／DPM
  越 P90 加聲量（不碰教練評價、薪資、獎項——§24.3.1 禁止事項）。
- **`ui/panel/team.js`**：新增「本賽段個人數據」與「國際賽數據」兩個 section。
- 新增 `tests/kernel/season.mjs`（10 項）、`tests/kernel/leagueStats.mjs`
  （18 項），`leagueSim.mjs`／`conditions.mjs` 各補若干。

## [v4.8.2] - 2026-08-18

### 微觀數據與排行榜落地（S33，Phase 2）

S32 只結算勝負，聯賽數據撐不起排行榜與 §24.3 的動態基準。本站把單局詳細數據
（K/D/A、DPM、CSM、VSPM）算出來，接上聯賽積分榜與個人數據榜。

- **新增 `src/engine/microStats.js`**：`generateMicroStats` 依 S31 定案的
  §24.2.3 公式生成 NPC 單局六維，綁定位置權重表與 tec/agi/awr（超過 60 的
  餘量 x⁺）；`gameTypeOf` 依宏觀強度差（夾取前）識別碾壓／均勢／爆冷三級局型，
  各帶 σM 與勝負方修正（§24.2.4）；`accumulateMicroStats`／`metricAverage`／
  `leaderboard` 是 `leaguePool.stats` 的寫入與查詢層（KDA 用總量比，不是逐場
  平均再平均）。
- **`data/skills.js`**：新增 `TEAM_DPM_TOTAL = 2400`（DPM＝傷害佔比%×24），
  與 `STAT_BASELINE` 同檔單一來源，S34 玩家端 DPM 換算共用。
- **`engine/leagueSim.js`**：`simulateLeagueMonth` 在同一個 Bo3 迴圈裡順手生成
  雙方五人微觀數據（不第二次配對）；`nonPlayerPool` 的真實 NPC 帶上
  tec/agi/awr（`NPC_BY_ID` 查表），合成匿名隊五個位置各配一名合成選手；
  新增 `statsFor` 查詢層。
- **`ui/panel/team.js`**：隊伍 tab 補上完整聯賽積分榜（不只板子上的名次數字）
  與各位置個人數據榜首（KDA／DPM／視野）。
- **`SAVE_VERSION` 未動**——`leaguePool.stats` 容器 S32 已建好，本站直接寫入。
- 新增 `tests/kernel/microStats.mjs`（27 項）：位置權重表、局型臨界值與 σM、
  夾取界永不突破、x⁺ 餘量、累加與 KDA 匯總語意、leaderboard 排序、整合
  （`simulateLeagueMonth` 寫進池）、同種子決定論。
- 新增 `tools/microstats-report.mjs`：分佈驗證量測工具（非遊戲機制），
  160 個賽段樣本量測結果見 `docs/v4/33-微觀數據與排行榜.md` 交接筆記。

## [v4.8.1] - 2026-08-18

### 模擬引擎與資料池落地（S32，Phase 1 宏觀模擬）

賽區內 NPC 隊伍間的對戰第一次真的被模擬：純數學批量結算逐月循環賽，寫進
`state.leaguePool`（S31 定案的 schema），接上 §22.1 的排名格。

- **新增 `src/engine/leagueSim.js`**：`simulateLeagueMonth` 逐月結算 NPC
  循環賽（隨機配對＋Bo3，`clamp(baseGameChance(強度差), 8, 92)`）；
  `recordPlayerLeagueResult` 併入玩家隊戰績（直接採用 `simulateSeason` 的
  `stat.W/L`，不二次模擬，§24.2.1 去重規則）；隊伍池不足 6 支時補合成匿名隊
  （`par±7`，無身分敘事），與 §23.7 同構。
- **單一來源整理**：`kernel/series.js` 拆出 `baseGameChance`（§11.1 斜率
  1.76，玩家系列賽與 NPC 背景模擬共用）；`engine/opponents.js` 拆出
  `aggregateTeamStrength`（§23.4 聚合式）與 `regionKeyOf`（賽區字串換算），
  `materializeOpponent` 與 `leagueSim.js` 兩個消費者共用同一份，不第三處手抄。
- **`ui/board.js`**：`#bd-rank` 接上真實積分榜（格式「名次/總隊數」），
  §21.3／§22.1 的「排名資料待接」備忘關閉。
- **`SAVE_VERSION` 26 → 27**：新增 `state.leaguePool`（只存當年＋前一年，
  §24.2.2 保留策略）。
- 新增 `tests/kernel/leagueSim.mjs`（15 項）：積分榜場次守恆（每場恰一勝
  一敗）、已排序、玩家戰績去重、賽段內隊伍組成固定、保留策略、業餘期不
  背景模擬、海外賽區同樣可用。

## [v4.8.0] - 2026-08-18

### 賽事模擬細部定案（S31，§24.2–24.6 與受影響節回寫）

辛組規格定案站：§24.1 三件事不動，細部規格全部寫死到「下一站照做」的程度。
純文件站——程式與測試未動（版號四份手抄本除外）。

- **數據池 schema（§24.2.1／24.2.2）**：`state.leaguePool` 頂層欄位——賽區
  循環賽逐局結算、逐月累計，只存匯總（賽段排名＋逐選手六維平均）與當年＋
  前一年；玩家隊戰績從 §11.3 結算入池、不做雙份模擬（去重規則）；NPC 隊
  強度複用 §23.4 聚合式與 §11.1 斜率 1.76，不另抄。S32 吃，bump
  `SAVE_VERSION`（26 → 27）。
- **微觀公式與方差（§24.2.3／24.2.4）**：NPC 六維綁 tec／agi／awr 與位置
  權重表（SUP 擊殺 0.3、助攻 1.4）；基線復用 `STAT_BASELINE` 單一來源；
  DPM＝傷害佔比 × 24（`TEAM_DPM_TOTAL` 2400，兩邊共用）；碾壓／均勢／爆冷
  三級方差（σM 1.3／1.0／1.5，國際賽再 ×1.25）。調參紀律寫死：只動方差、
  權重與局型修正，不動基線與夾取。S33 吃。
- **動態基準與謂詞擴充（§24.3.1／24.3.2）**：基準＝賽段累計窗口、一賽區一套；
  掛載層限戰報文本／fame（P90 越線 ＋3、每賽段至多一次）／條件式三處，禁止事項明列（不得進教練
  評價、薪資報價、獎項、成長——§10 定義邊界）；新節點 `leaguePct`（當季模擬
  母體 runtime 現算，母體 < 20 判 false）與既有 `percentile`（史實靜態表）由
  兩個節點物理隔離，兩套母體切分規則寫死。S34 實作。
- **Global_Boss 合流（§24.4.1）**：國際賽選池限 fetch_priority 2；大賽經驗
  加成 `min(3.0, 0.5 × 先發國際賽年數和)`、Region Patch Debt（四大賽區 0／
  HOME 四代 1.5／其餘 2.5）；`intlMod` 經 S29 預留 `intlBoost` 掛鉤進加項——
  不動公式形狀、不觸發 `OPPONENT_SUPPORT` 重推，均值淨移登記進 S36 量測清單。
- **Bo5 高壓檢定落點（§24.4.2）**：2:2 第五局檢定，`m = comp×0.6 + resl×0.4`，
  m < 60 衰減 `3.0 × (1−m/60)^1.5`；落點＝第五局限定隊伍強度加項、**不進
  §9.2 乘法鏈**（三條理由：常駐修正邊界、S12 重推判準不對稱、NPC 對稱生效
  互抵）；四常數歸 S36，量測清單寫死。
- **傳說／獨有連動（§24.4.4）**：里程碑 ↔ 謂詞對照表——讓二追三走
  `eventCount('reverse_sweep')`（既有節點、新計數鍵）、國際賽老將新增
  `intlGames` 謂詞（S35 雙註冊）；§18.3 解散改寫維持現狀（世界賽冠軍限定）。
- **受影響節回寫**：§11.3（入池唯一入口）、§12.3（聯賽百分位謂詞行）、
  §14.7（leaguePct 積木）、§15.1／15.2（級距描述與決勝局敘事邊界）、
  §20.2（leaguePool 欄位與版號註記）、§21.3（戰績排名備忘由 S32 關閉）、
  附錄 #80–#85。逐條消費站見新增 §24.6 消費表。

## [v4.7.5] - 2026-08-18

### 庚組校準收斂（S30，§23.4／§23.5／§14.2／§14.3）

S28（隊友池）與 S29（逐選手對手）改變了 §11.1 的輸入分佈，本站把受影響常數
重推收斂，160 段基線回到 S28 錨點，庚組收尾。

- **`OPPONENT_SUPPORT_RESIDUAL` 起始 3.5 → 定案 16.5**（`src/kernel/strength.js`）：
  3.5 只補了舊 9.0 推導三項裡的教練／體力一項，實測實體化對手平均比舊匿名弱
  13.14 點；掃值 13–18 對照後取 **16.5**——160 段國際賽局勝率 **53.1%**（＝S28
  錨）、頂端落差 **0.175**、平均巔峰 **74.19**（錨 74.16）、傳說持有 **18.1%**
  （15–25% 帶內）。判準→量測→結論全錄在 `kernel/strength.js` 檔頭。
- **網紅選手改走絕對門檻 fallback**（`src/data/quests.js`）：fame≥3 的 peakRating
  下限實測 71、p25 75–80，高於歷史母體 P90（72）——「fame 高且 ≤ 歷史中位數」
  零段可達（S26 點名的結構性互斥）。改 `peakRating ≤ 76`，收斂後 160 段達成 5 段。
  `percentile` 節點與 `percentiles.js` 保留（究極綠葉仍用 assist；peakRating 門檻
  留給 S31 當季模擬母體，§24.1）。
- **route 覆蓋率量測落地**（`tests/regression/invariants.mjs`）：後 50% 局
  **11.3%**（vs 60% 驗收線）。60% 線的「S19c 實測 63.7%」是幽靈量測——S26 用
  S19c 當代碼重跑只有 5.0%，該測試從未落地。量測打日誌不硬紅（同實體化率），
  補平庸局路線歸內容站。
- **`tools/calibrate-160.mjs`（新）**：160 段校準量測腳本——跑 AMATEUR 矩陣、印
  頂端落差／國際賽勝負／特質持有／route 覆蓋／實體化率／體力節奏等全部校準指標。
  S36 校準重驗同一套。
- **基線數字**：`npm test` **23447 項全綠**（基線 23675——校準後國際賽打得少，
  依樣本數迭代的檢查減少 228 項，非檢查被改成恆真）。頂端落差餘裕 0.060 →
  **0.095**。`SAVE_VERSION` 定案 **26**（S28→25、S29→26，本站未改存檔結構，
  舊存檔一律作廢的既有處置不變）。量測工具 `next-station.mjs` 回報庚組收尾。

## [v4.7.4] - 2026-08-18

### 逐選手對手模型上線（S29，§23.4）

對手從聚合數字變成有隊名有先發的 NPC 隊伍——階梯表仍是對手水準的唯一來源，
五人陣容是階梯水準的身分實體化。陣容缺口落回匿名對手（敘事不帶身分）。

- **`src/engine/opponents.js`**（新）：`teamsInYear` 依 career 年表與 `active_years`
  雙覆蓋建逐隊五人池（年代一致不變式長在入池條件裡）；選 carry 最接近階梯目標的
  隊；強度＝`P_carry × 0.60 + 其餘四人均值 × 0.40 + starTerm(carry, 對手主場聯賽
  par) + OPPONENT_SUPPORT_RESIDUAL`。
- **`src/kernel/strength.js`**：拆出 `OPPONENT_SUPPORT_RESIDUAL`（3.5 起始，S30
  擁有終值）與 `starTerm`（明星項純形式，兩邊共用）；匿名路仍走 `OPPONENT_SUPPORT` 9.0。
- **消費端**：季後賽／MSI／世界賽（含入圍、Swiss、地區資格賽）逐對手實體化，
  BO5 賽前敘事帶隊名與先發五人，小組卡列實體化隊名；常規賽維持匿名。
- **`src/data/npc/teamIds.js`**（新）：玩家可加入隊名→NPC team_id 對映，選隊池
  排除自己隊伍（聯賽內不自己打自己）。
- **不變式**：年代一致（硬紅）、缺口落回不帶身分（`tests/kernel/opponents.mjs`）；
  實體化率量測打日誌不硬紅（`regression/invariants.mjs`）。
- **存檔**：新增 `oppFaces` 計數，SAVE_VERSION 25 → 26。
- **基線偏移（交 S30）**：實體化對手平均比舊匿名弱 13.1 點（環節分解見交接筆記）
  → 國際賽局勝率 53.1%→63.0%、世界冠軍人均 0.081→0.381、頂端落差 0.172→0.140
  （比例制門檻全數未破，`npm test` 23675 項全綠）；實體化率 intl 37.6%／playoff
  25.4%，池太薄餵 S24 補抓。

## [v4.7.3] - 2026-08-18

### 隊友接 NPC 池（S28，§23.6）

隊友抽選從「12 個 ID 隨機抽 4 人」換成史實母體：職業期從 `src/data/npc/roster.js`
抽（年代×位置×賽區三道過濾，rating 讀 NPC 當年強度 `P_npc`，par±7 窗口放寬到
±14 不空手）；業餘期保留 par±7、名字改吃虛構名池；`MATE_NAMES` 退役。存檔結構
變動（`state.mates` 擴 `npcId/position`），SAVE_VERSION 24 → 25。

- **`src/engine/roster.js`**：`rollRoster` 依 stage 分流——PRO 走 `pickProMates`
  （NPC 池＋窗口＋合成兜底）、業餘走虛構名池；新增 `npcRatingInYear`（§23.3
  `peak.rating × ceiling_curve` 包絡）。
- **`src/data/npc/teamHistory.js`**（新，生成器 `gen-team-history-esm.mjs`）：戰隊
  縮寫→賽區的遊戲側單一來源，隊友抽選的賽區過濾與縮寫顯示讀它。
- **`src/data/mateNames.js`**（新）：虛構隊友名池（業餘期＋合成隊友取名）。
- **選手面板隊友卡**：職業期隊友顯示「名字（隊縮寫）·位置 rating」；心理六維與
  教練評價不顯示（§22.4 對 NPC 同適用）。
- **基線偏移**（交 S30）：平均巔峰 72.84→74.16、頂端落差 0.162→0.172（餘裕
  0.092）、世界賽冠軍人均 0.035→0.056、職業年資 12.4→11.4、傳說持有 23.1%→20.6%。

## [v4.7.2] - 2026-08-18

### NPC 名冊生成（S27）

庚組資料線的最後一站：把 `cleaned_players.json`（S25）的史實轉成 V4 的 NPC
結構，產出 `src/data/npc/roster.js`——S28（隊友）與 S29（逐選手對手）的輸入。

- **生成器 `tools/npc/gen-roster.mjs`**：確定性數學照 S23／S26 定案——`peak.rating`
  復用 S26 `peakRatingOf` 回歸（單一來源）；`attributes` 位置原型反映射
  （`55 + 40 × (w/w_max)` 再縮放使加權平均 = rating）；`lifecycle` 三參數範圍與
  `tags` 詞彙表補定 §23.3 三處「範圍見下表」缺表（回寫規格）。
- **生成範圍**：priority 1（台港澳全量 200）＋ priority 2（Worlds／MSI 參賽隊員＋
  四大賽區賽段冠軍隊員 738）＝ 938 筆；priority 3 不生成（S27 拍板）。
- **LLM 語意**：環境無 LLM API，比照 S25 先例——psych／traits／tags 由確定性規則
  產出，執行者對 7 筆知名台港澳選手做語意覆寫（`tools/npc/npc_overrides.json`
  快取）。重跑冪等（同輸入同輸出，shasum 不變）。
- **量測**：938 筆中 528 有 peak.rating（P50=68 與 S26 母體對齊）、663 有 position、
  410 無名次（peak null）。attributes 反映射 maxErr 0.29。`short_peak` 305 筆反映
  cleaned 資料涵蓋範圍短（資料限制，非真實曇花），記交接筆記。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.7.1 → v4.7.2（對既有章節的補充，§23.3 補缺表）。

## [v4.7.1] - 2026-08-18

### §14.3 兩條 route 路線改回百分位（S26）

究極綠葉與網紅選手原本用絕對門檻暫代（§14.3 附註 #32），因為引擎沒有逐選手
數據母體。S25 的 `cleaned_players.json` 就是那個母體，本站產靜態百分位表並把
兩張卡改回百分位定義。

- **新節點型別 `['percentile', 指標, 比較子, 百分位]`**：玩家某指標與歷史母體
  門檻比。`['percentile', 'assist', 'gte', 90]`＝生涯場均助攻 ≥ 同位置 P90；
  `['percentile', 'peakRating', 'lte', 50]`＝peakRating ≤ 歷史中位數。節點進
  `conditions.js` 的 `COND_KINDS`／evalCond 與 `tools/schema.js` 的
  `COND_NODES`／validateCond（兩張註冊表同步，`tests/kernel/conditions.mjs` 守）。
- **靜態門檻表 `src/data/npc/percentiles.js`**：`gen-percentiles.mjs` 產出（單一
  來源，`tests/kernel/percentiles.mjs` 過生成器實際運算式重算比對）。門檻常數由
  `ledger.js` 查詢層讀（`assistP90`／`peakRatingP50`），遊戲執行時零百分位運算。
- **母體與回歸**：助攻母體樣本 0（cleaned 目前無助攻欄位）→ 全位置 fallback 保留
  絕對門檻 2.5（§23.5 最小樣本規則）；peakRating 母體 679 人（>100）→ 非 fallback，
  P50 = 68。名次 → peakRating 回歸錨點（世界賽冠軍 ≈ LCK par 76）由 S26 定案，
  S27 照寫生成 NPC 時用。
- **實測落差（交接 S30）**：網紅選手改百分位後 160 段達成 0 段（暫代 13）——fame
  最高級與低 peakRating 結構性互斥；後段局 route 覆蓋率 6.3% 遠低於 §14.2 驗收線
  60%（此線在 S19c 交接筆記宣稱已新增測試、實際從未落地）。兩者都由 S30 重校。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.7.0 → v4.7.1（對既有章節的補充，§14.3 兩張卡＋§23.5 落地註記）。

## [v4.7.0] - 2026-08-17

### 連續事件：事件卡多了觸發旗標與觸發計數

事件之間原本只有 `chain`（同一回合立刻接演下一張卡）這一條路——那是三聯幅，不是
連續事件。事件一觸發後、隔幾個月、條件也滿足才輪到事件二，這種線講不出來；「同一件事
你已經幹過三次」也答不出來。§12.1 的四步演算法一步未改，補的是它讀得到的兩種軌跡。

- **觸發旗標**（`state.eventFlags`）：卡片結果／選項寫 `setFlags` 點亮、`clearFlags`
  熄滅，條件式用 `['eventFlag', 鍵]` 讀。跨月、跨賽季都記得。
- **觸發計數**（`state.eventCounts`）：每張卡出場自動 +1（鍵＝卡 id），另有卡片宣告的
  具名計數 `counters` 跨卡累加；條件式用 `['eventCount', 鍵, 比較子, 值]` 讀。
  「觸發 N 次後開新事件」與「觸發 N 次後授予特質」（§14.1 獨有特質的 `grantWhen`
  同一套條件式）因此寫得出來。
- **⚠ 連鎖必須收束**：條件卡不受防重擋，旗標亮著的期間讀它的卡月月都是事件一。
  多段連鎖由後段熄前段的旗標，一次性門檻寫成 `計數 ≥ N 且 not 已出過旗標`。編輯器
  擋下「沒有人熄得掉的正向旗標」，`tests/kernel/eventChain.mjs` 對整副牌驗四條不變式。
- **新內容 3 張**：挖角三部曲（`transfer_rumor`／`buyout_offer` 探風 →
  `tampering_meeting` 密會 → `tampering_leak` 密約曝光，中間夾具名計數 `poach_talk`
  的門檻）與計數門檻卡 `soloq_legend`（排位衝分出過 5 次才來，出過就上閂）。
- **編輯器同步**：條件積木多兩種節點，結果與選項多三欄（點亮／熄滅／計數 +1），
  鍵是自由字串，已用過的鍵在 datalist 給提示。
- **既有條件卡全數上閂**：`clip_meme`／`enemy_taunt`／`stream_meltdown`（條件是
  `持有 特質`，特質不會失去＝條件永遠成立）與 `slump`（`comp ≤ 40`，抗壓掉下去
  很少回來）本來會從條件成立的那個月起月月霸佔事件一——實測 240 個月裡
  `clip_meme` 144 次、`slump` 129 次、帶三特質的局 `enemy_taunt` 199 次。四張都上閂
  （`not 完成旗標` ＋ 每個選項點亮）。上閂後出卡分布轉平：最常出的卡只佔 6%，
  240 個月出 67 種卡（原本 19~50 種）。
- **閂有兩種壽命，差別只在鍵怎麼取名**：`xxx_done` 一次生涯一次（例：`soloq_legend`
  的里程碑），`annual_xxx_done` 一年一次——年初由 `engine/game.js` 的 `yearOpen`
  呼叫 `rearmAnnualEventFlags` 重新上膛。上述四張全走年度閂：季中低潮、梗圖爆紅
  這類事**每季都可能發生**，鎖成一輩子一次是把內容浪費掉，同年月月重播又成了背景音。
  ⚠ 重新上膛只清旗標不清計數——計數是生涯累計的軌跡，年度歸零會讓「觸發 N 次後」
  的門檻永遠到不了。
- **`tests/phases/amateur.mjs` 的「沒達標就沒有星探」改驗規則**：舊寫法假設低潛力
  的樣本兩年後仍在門檻下，但潛力只讓成長變貴、不封頂，事件卡的 ±1 給予照樣把他
  推過 AM2 的 45——內容一改就假紅。改成驗「收到邀約的那一刻評價確實過了門檻」。
- **SAVE_VERSION 23 → 24**：舊存檔缺兩欄會讓連鎖條件恆偽、計數從中途起算，故作廢。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.6.9 → v4.7.0（新增概念，§12.1／§12.2／§12.3 三節變動）。

## [v4.6.9] - 2026-08-17

### 手機版面：把高度還給事件文本區，字級整體放大

固定框架（v4.6.4）下每一列都在跟事件文本區搶高度，390×844 上事件卡常常只剩幾行。
四項一起處理，版面欄位（欄數、順序、決策槽固定高度規則）一律不動。

- **收合以拇指區為單位**（§22.2.1）：「收合選項」改為屬性條與決策槽一起收，
  390px 下一次讓出 413px；切換鈕永遠留在畫面上，新一輪選項自動展開。
- **分類篩選整列退場**（§22.1）：`#log-bar`（全部／賽事／訓練／事件／市場／結算
  chips ＋年度下拉）整列移除，省下一整列高度。年度跳轉改由年度分隔線折疊與
  寬螢幕左欄目錄承接；卡片仍帶 `data-kind`，要復活篩選不必回頭補資料。
- **事件聚焦**（§22.1，<720px）：卡牌問答期間自動收起賽區隊伍列、生涯任務列與
  月進度，事件文本區多出 70px；體力條與第一列不收，選定後立即復原。
- **字級 +1～1.5px**：卡片內文 14.5→16、選項 14.5→15.5、屬性數值 14→15.5、
  狀態帶各欄同步微幅放大。屬性條的潛力與蓄力**改為各佔一行**——擠成一行
  「潛100 ·蓄1.4」會把進度條軌道壓成 0px（改版前 320px 已經是 0），分行後
  320／360／390px 的軌道分別回到 9／23／33px，六格零溢出、零重疊。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.6.8 → v4.6.9。

## [v4.6.8] - 2026-08-16

### 結算圖：兩種 MVP 都列，榮譽計數併入生涯數據面板

- **`data/awards.js` 新檔**：年度個人獎項名目（例行賽 MVP／最佳新人／單殺王／
  全明星）收成單一來源，寫入端 `phases/seasonEnd.js` 的 `award()` 與讀取端結算圖
  共用同一份字串，跨檔不再各抄一份。附 `awardCount(state, name)`——依名目計數走
  `milestones` 帳本（`kind === 'award'` ＋ `text` 全等），不去 `honors` 解析顯示字串。
- **兩種 MVP 分開列**：`例行賽 MVP N 次`（一個聯賽一年只有一個人拿得到的年度獎）
  與 `單場 MVP N 次`（每場結算累加的 `stat.MVP`）是兩回事，結算圖兩格都出。
- **榮譽計數搬進右面板**：冠軍與 MVP 本來就是生涯數據的一部分，改接在「合計」列
  之下（面板內），不再於特質段之後另開一列——順帶把右面板原本的空白填滿。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.6.7 → v4.6.8。

## [v4.6.7] - 2026-08-16

### 結算圖版面修復＋回到原設計稿

`v4.6.5` 的轉播選手卡在 `v4.6.6` 出圖時整個下半張跑掉：`drawStatTable` 畫完
「合計」列的 KDA 之後，2D context 的 `textAlign` 停在 `'right'`，而個人特質、
榮譽、總薪資、生涯傳記全用裸 `fillText(line, PAD, y)`——於是每一段都被右對齊
到 x=40，字整片畫到畫布左邊界之外，玩家存下來的圖只剩每行最後幾個字。

- **落字單一入口**：新增 `text(c, s, x, y, font, color, align)`，font／色／對齊
  每次都設滿，不再繼承別人留下的 context 狀態。`drawRadar`／`drawStatTable`
  一併改走這個入口，裸 `fillText` 在這個檔案裡絕跡。
- **高度兩趟渲染**：舊版手抄一份高度算式跟繪製並行維護。改成先在暫存 context
  跑一次同一份 `paintCard` 拿末行基線，再開實際畫布畫第二次——手抄本移除。
- **回到設計稿版面**：雷達與數據表各自收進一張等高面板（圓角＋髮絲邊）；個人
  特質改成依五階配色的標籤，被合成掉的接在後面以灰字劃線（不再另開一行「合成
  離隊」）；榮譽段改成計數列（世界賽冠軍／MSI 冠軍／賽段冠軍／單場 MVP，零的
  不印）；生涯傳記上限 8 行。
- **數據表可讀性**：列距 28 → 34，分隔線改貼最後一列下緣、「合計」再讓開 12px。
- **新測試 `tests/kernel/shareCard.mjs`**：假 2D context 錄下每一筆落字，驗全部
  落在畫布內、兩趟渲染基線一致、關鍵字都印得出來。先以 v4.6.6 的寫法驗過紅。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.6.6 → v4.6.7。

## [v4.6.6] - 2026-08-17

### S45：alpha 內測完整版組裝（§19.4）

在既有 demo（PRO 起點三年期程）之外，組裝 alpha 完整版：AMATEUR 起點（16 歲
2012 網咖盃）一路打到退役，無年份上限。引擎零改動、SAVE_VERSION 不動——業餘期
與完整生涯本來就是 160 段測試的基線，這一站只補「瀏覽器入口 + 文案 + 存檔隔離」。

- **獨立目錄 `alpha/`**：`alpha/index.html` 複製自根目錄 `index.html`，改開場文案
  （2012 業餘期、完整生涯、ALPHA 內測標記），路徑指到 `../src/*`，檔頭標
  `window.__ESPORTLIFE_MODE__ = 'alpha'`。demo 根目錄入口不動。
- **`src/main.js` 讀模式**：`MODE`（預設 demo）→ `createState` 傳
  `stage: 'AMATEUR'`；存檔 namespace 跟著模式走。
- **存檔隔離**：`src/ui/storage.js` 的 key 加 namespace（`…v4.alpha` vs `…v4.demo`），
  兩邊不再互讀到對方存檔（起點語意不同）。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.6.5 → v4.6.6。

## [v4.6.5] - 2026-08-17

### S44：技能下拉說明＋結算圖轉播卡重設計（§22.3、§19.4）

純 UI 改版，不動任何遊戲機制與校準常數。

- **技能下拉**：選手 tab 技能欄比照屬性加 `▸` 下拉箭頭，展開顯示一句說明＋
  來源屬性；**不顯示權重數字**。說明文字 `SKILL_DESC` 收於 `data/skills.js`，
  來源屬性直接讀 `SKILL_WEIGHTS` 鍵不另存（單一來源規則）。
- **結算圖重設計**：轉播選手卡風格——左欄六維屬性雷達圖（Canvas 手繪，
  零依賴）、右欄各賽區生涯數據（出賽／K-D-A／KDA 比值＋生涯合計），
  下方依序放個人特質、頂級榮譽、生涯總薪資與生涯傳記（§15.5）。
- **帳本層新增 `kdaOf`／`careerKda`**：KDA 比值 (K+A)÷D 取一位小數，
  D=0 回 K+A 不炸；結算圖與 UI 讀帳本不直接除 raw 欄位。
- **升版**：package.json／APP_VERSION／規格書檔頭／CHANGELOG 四處同號
  v4.6.4 → v4.6.5（`tests/kernel/version.mjs` 守門）。
- **新增測試**：`tests/kernel/skillDescs.mjs`（說明覆蓋十二技能）、`kdaOf`／
  `careerKda` 單元進 `tests/kernel/ledger.mjs`。

## [v4.6.4] - 2026-08-17

### S43：固定框架與單一決策槽（§22.2／§22.2.1 修訂）

決策槽原本靠 `position:sticky;bottom:0` 假裝固定。sticky 的底部吸附需要包含塊在
元素下方還有剩餘高度，而 `#act` 是 `#app` 最後一個子元素、兩者底邊重合——travel
距離為零，等於完全沒作用：事件文本一長，選項就被推到摺線以下，玩家得捲回去找。

- **固定框架**：`#layout` 高度＝`100dvh`＋`overflow:hidden`，頁面本身不再捲動。
  `#log` 成為框內唯一的捲動窗格（`flex:1;min-height:0;overflow-y:auto`），狀態帶、
  屬性條、決策槽都是框內的 `flex:none` 子項，位置不隨文本長度改變。寬螢幕三欄同理：
  `#year-dir`／`#app`／`.panel-sheet` 各自吃滿框高、各自獨立捲動。
- **sticky 全數退場**：`#board`、`#year-dir`、`.panel-sheet` 不再需要 sticky，
  S42b 為了算 sticky 落點而加的 `--board-h` 與 `initBoardMetrics()` 一併移除
  （固定框架下欄高由 grid 列高決定，不必量測狀態帶）。
- **單一決策槽**：`slot:'inline'` 的卡牌選項改畫在決策槽，不再接在卡片下緣。
  卡片端保留兩個唯讀狀態——等待中掛 `.awaiting` 提示條並以 ↓ 指向下方，選定後
  就地留下「你選了 X」。`askChoice`／`askInline` 合流為同一個 `promptOptions`，
  差別只在有沒有錨點卡。
- **槽高改隨視窗縮放**：`--act-h: clamp(240px, 38dvh, 368px)`。固定框架下槽高吃掉
  的就是文本區高度，368px 定值會讓 844px 高的手機只剩 136px 可讀；改後約各分一半。
  §22.2.1 要的是同一視窗內不隨當月選項數跳動，不是全裝置同一個像素數。
- **捲動對象**：`scrollToBottom()` 改捲 `#log`（頁面不捲，捲 window 等於沒動作）。
- **日誌篩選 chips** 改單列橫捲＋右緣淡出，不再換行多吃一列高度。

規格同步：§22.2「卡牌覆寫」改寫為「卡牌問答」並說明為何收成單一操作點；
§22.2.1 增訂「固定框架」不變式。

## [v4.6.3] - 2026-08-17

### S42b：寬螢幕版面修正（§22.6，接續 S42）

S42 的三欄斷點只把 `#app` 推到第 2 欄、`#panel` 留在原地，兩者落在同一個 grid 格
互相疊字，右邊 340px 欄空著。本次修掉這條與另外三處版面偏差，規格未動。

- **三欄重疊**：`≥1024px` 補 `#panel{grid-column:3}`。原本 `#app` 與 `#panel` 同格
  疊放，選手資料蓋在敘事流上、事件文本左緣被截。
- **側欄 sticky 落點**：狀態帶實高約 165px（五列），CSS 卻硬寫 `top:56px`，左右欄
  一捲就滑進狀態帶底下。改由 `ui/board.js` 的 `initBoardMetrics()` 以
  `ResizeObserver` 量測 `#board` 實高寫回 `--board-h`，`#year-dir` 與 `.panel-sheet`
  的 `top`／高度都吃這個變數。
- **右欄不再跟著捲走**：`#panel` 補 `align-self:stretch`——`.panel-sheet` 的包含塊
  是 `#panel`，`#panel` 只有內容高時 sticky 沒有可走距離。
- **超寬螢幕欄距**：`#layout` 補 `max-width`（兩欄 940px／三欄 1180px），整個 grid
  置中。原本中欄在無限寬的 `1fr` 裡置中，2560px 下與側欄裂出 690px 空白。
- **中欄高度**：`#app` 補 `min-height:calc(100dvh - var(--board-h))`，讓決策槽回到
  欄底（豎屏靠 `#layout` 的 `min-height` 達成，grid 的 `align-items:start` 會失效）。
- **蓄力讀數**：選手 tab 的屬性列直接印浮點（`·蓄0.1376267417605339`）把整列擠成
  兩行，補上 `attrbar.js` 同一套 `num()` 格式化。

三條不變式重新驗證：決策動線不變、`#app` 維持 600px 行長上限、寬螢幕無額外資訊。

### S42：寬螢幕版面（§22.6）

壬組完工站。新增兩個斷點（≥720px 兩欄、≥1024px 三欄），手機豎屏版面完全不受影響。

- **DOM 重組**：新增 `#layout` 包裹 `#board`／`#app`／`#panel`，`#board` 移出 `#app`
  成為 `#layout` 直接子元素以橫跨整寬；移除假導覽列 `.site-nav`。
- **#panel 兩態**：窄螢幕（<720px）底部抽屜（`.open` 控制顯隱）；寬螢幕（≥720px）
  常駐右欄 340px（`position:static`，`display:block`，`.open` 不參與顯隱）。
  `refreshPanel`／`togglePanel` 改讀 `isPanelVisible()`（含 `matchMedia` 查詢），
  斷點跨越時由 `change` 事件觸發重繪。
- **年度目錄左欄**：新檔 `src/ui/yearDir.js`，≥1024px 時在 240px 左欄常駐顯示年度
  目錄（資料取用 `ui/log.js` 的 `yearEntries`，不重建一份）。
- **開場畫面**：≥720px 建角表單改雙欄（左說明／右表單）；favicon 對齊現行
  terracotta 品牌色。
- **分享圖**：移除從未載入的 `Cinzel`／`Rajdhani` 字型，改讀 `--ui`（Inter＋Noto Sans TC）；
  底色與標題色對齊現行色票。
- **鐵則註解**：`src/styles.css` 檔頭鐵則改寫為「窄螢幕抽屜／寬螢幕常駐」兩態。
- **三條不變式**：決策動線不變（#act 仍錨定在 #log 正下方）、文本行長不隨螢幕拉寬
  （#app 維持 max-width 600px）、寬螢幕不出現豎屏看不到的資訊。

## [v4.6.2] - 2026-08-17

### S41：事件日誌閱讀（分類／年度跳轉／五拍成組）

卡片分類（`kind` 欄位）：各 phase 檔透過 `kinded()` 工廠標示卡片類型
（`train`／`match`／`event`／`market`／`season`），91 個呼叫點零改動。
賽事五拍成組：`seriesEvent.js` 五拍帶 `series` 標識，`ui/log.js` 偵測連續同
`series` 的卡片收進 `.series-block`，跨過中間的 `choice`（備賽戰術走 `act` 槽）。
分類篩選：六個 chips（全部／賽事／訓練／事件／市場／結算），純 CSS 屬性選擇器
切換，不重繪、不重跑 generator。年度跳轉：年度分隔線登記進目錄，下拉選單跳轉。
生命週期卡（`game.js` 的出生／退役／結局）無 `kind`，任何篩選下保持可見。

## [v4.6.1] - 2026-08-16

### 壬組 S37：UI 版面規格增訂（純文件，除版號外程式與測試未動）

§22 於 v4.3.2 定案，主線 S01–S22 完工但**版面從未照做**。本站盤點缺口、把規格
細化到可施工粒度，並把實作拆成壬組 S38–S42 五站。

- **§22.1 主畫面骨架**：線框細化到可施工粒度；明訂體力條常駐、屬性條**帶蓄力
  第二刻度**、屬性條緊貼決策槽、賽事五拍**必須成組呈現**、狀態帶**不得出現任何
  技能值**；新增日誌檢視控制（分類篩選＋年度跳轉）。
- **§22.2 決策槽**：原三模式定案不動，補「為什麼分兩處」的理由、實作落點
  （`choice` beat 帶 `slot` 欄位，由發射端指定而非 UI 猜）與線框；新增 **§22.2.1
  決策槽版面不變式**（同槽互斥、位置與最小高度固定、選項須在選前呈現預期收益）。
- **§22.3 四 tab**：內容不動，補線框並明訂「四 tab 是並列檢視入口，不是單一長捲」。
- **§22.5**：屬性條補蓄力讀數；明訂體力條常駐於狀態帶第二列。
- **§22.6 寬螢幕版面（新增）**：手機豎屏仍是基準版面，寬螢幕只攤開資訊、不重排
  決策動線；三條不變式＝決策動線不變／文本行長不隨螢幕拉寬／寬螢幕不得出現豎屏
  看不到的資訊。**§22.4 資訊公開性一字未動**，在所有寬度一體適用。

盤點出的兩項現行偏差：**§22.2 卡牌覆寫整條未實作**（所有選項一律送底部決策槽）、
**§22.1「決策不提供技能讀數」現行違規**（狀態帶印本位置核心技能值）。

`DESIGN.md` 同步新增 Layout 斷點段與五個元件規格。`docs/v4/README.md` 新增壬組
S37–S42（全表 56 → 62 站），六份站說明書隨附。附錄新增 #75–#77。
除版號四份手抄本外程式與測試未動，`npm test` 19928 項基線不變
（版號一致性由 `tests/kernel/version.mjs` 守住）。

## [v4.6.0] - 2026-08-16

### 庚組 S23：NPC 規格細部定案（純文件，程式與測試未動）

§23 細部規格定案（規格書 23.3–23.8），S24–S30 全部「依 S23 定案」的欄位有了
回填來源；逐條定案與消費站對照見 §23.8 消費表。

- **NPC schema（23.3）**：遊戲資料一律 ESM `.js`（零建置無 JSON import），中間
  產物留 `tools/npc/`；`peak.rating` 是 NPC 強度唯一骨幹，不存 potential 與技能值；
  `fetch_priority 2`（Global_Boss）範圍定案。
- **逐選手對手模型（23.4）**：§11.1 階梯表仍是對手強度唯一來源，NPC 陣容是階梯
  水準的「身分實體化」；對手明星項顯式化，`OPPONENT_SUPPORT` 9.0 縮為教練／體力
  殘項（起始 3.5，終值 S30 重校）；新增三條不變式（年代一致／實體化率／缺口不帶身分）。
- **百分位回歸（23.5）**：究極綠葉吃同位置母體場均助攻 P90，網紅選手改讀
  `peakRating` 對歷史母體中位數；引擎讀靜態表，新增 `['percentile', …]` 謂詞
  （QUERIES／PREDICATES 雙註冊）。
- **隊友池 NPC 化（23.6）**：職業期改 NPC 池抽選（年代×位置×賽區），`MATE_NAMES`
  退役；業餘期留 par±7 機制改虛構名池；`state.mates` 擴充、SAVE_VERSION 歸 S28。
- **缺口備援（23.7）**：Liquipedia → Leaguepedia → synthetic 三級，逐筆標
  `data_source`；GPL 2012–2013 預估最薄。

受影響章節：§11.1（階梯表與明星項註記）、§14.3（暫代註記改定案指向）、§20.2
（隊友欄位寫回）、§21.3 與附錄 #32（條目標記定案，歷史登記保留）。附錄新增
#68–#73。程式與測試未動，`npm test` 19919 項基線不變（版號四份手抄本同號由
`tests/kernel/version.mjs` 守住）。

### 工作細則修訂（2026-08-16，不升版號）：S24 拆四站

S24 資料目錄與爬取依工作性質拆為四站（附錄 #74）：S24a 探勘與管線、S24b 台港澳
目錄與 raw、S24c 國際目錄、S24d 國際 raw。切割點＝決策密集 vs 執行密集，抓取量大
靠中斷續傳承接（執行時間不是站的工作量）；台港澳量小併一站，國際拆目錄／抓取兩站。
S25 前置改 S24d；規格 §23.2、`docs/v4/README.md` 狀態表同步。站切割屬工程細則、
與遊戲規則無關，版本維持 v4.6.0 不升——版號四份手抄本不動。程式與測試未動。

## [v4.5.3] - 2026-08-16

### V4 重建主線完成：月回合 × 設施制訓練 × 十二技能 × 教練評價（S01–S22，39 站）

> 版號與規格書 `ESPORT-DESIGN-V4.md` 共用同一套（AGENTS.md 版本編號規則），
> 故為 v4.5.3 而非 v5.0.0。工作說明書共 53 站，本節涵蓋主線 39 站；
> 庚組（S23–S30）與辛組（S31–S36）共 14 站施工未開始。

V4 重建同時翻掉四根互相咬死的柱子——回合粒度（年 → 月）、成長模型（擲骰加點 →
設施制）、評估層（OVR → 教練評價）、屬性刻度（1–80 → 0–100）——並在此之上把
賽制、內容與生涯收束全部重做。這是一份重建總結，逐站細節見各站交接筆記
（`docs/v4/`）與下方各條目。

**核心機制**
- **月回合制**：一年十二個月，每月選一個訓練活動；賽事月（季後賽／MSI／世界賽）
  不訓練、改走五拍賽事事件序列（賽前敘事→備賽戰術→戰術結算→比分→賽後敘事）。
  兩賽段年 7 個（MSI 年）／8 個（無 MSI 年）養成回合、三賽段年 6 個
- **設施制訓練**：八個訓練活動（個人機械訓練／強化訓練賽／戰術復盤／體能健身／
  排位衝分／英雄池練習／休息／復健預防），兩階段判定（成敗→檔位），結果由 60 張
  訓練事件卡決定；骰子加點協定整段退場（`phases/alloc.js` 刪除）
- **六屬性 0–100 ＋ 潛力衰減**：潛力仍是軟天花板，越接近長得越慢；刻度重校時
   per-point 係數全部除以 1.25（§17.2 的坑）
- **十二技能**：對線／操作／視野／節奏／支援／資源控制／會戰／開團／保護／邊線／
  轉線／走位，由屬性加權導出、玩家不能直接加點；每路核心 4 項＋次要 2 項
- **教練評價取代 OVR**：薪資／試訓／續約／FA 全部改讀教練評價（位置技能加權＋
  隱藏心理修正＋特質調整）；OVR 字樣從 UI 全面移除
- **隱藏心理六維**：抗壓／自信／動機／紀律／信任／韌性，不可見、只由事件卡改變，
  影響技能發揮穩定度（±15% 極限）與受迫／非受迫性失誤；聲量層保留為獨立可見層
- **體力系統**：0–100 資源，每月自然恢復 +10、訓練 −20~−30、休息 +50；低體力
  訓練成功率下滑、大失敗與受傷風險上升；體力進表現不進場次
- **生命週期曲線**：成長與衰退是同一條曲線的兩面，六屬性各有巔峰年齡與衰退速率
  參數（由種子生成）；固定年齡衰退表與退役硬上限作廢
- **市場淘汰制**：衰退曲線自然進行，自由市場無報價就只剩退役；退役走三層事件
  （五種特殊結局 → 四選項含「最後一搏」續命 → 結算）

**內容**
- 事件卡 26 → **86 張**（含 13 張條件卡，條件命中優先）；訓練事件卡 **60 張**；
  生涯任務卡 **25 張**（20 legend＋5 route）；扮演卡 23 張；天生特質池 5 條
- **統一條件語言**：任務卡／事件卡／特質授予／退役結局共用 `evalCond`
  s-expression，`whenHits` 已刪除；謂詞一律讀帳本查詢層（`ledger.js`）
- **四階特質重建**：S19a 把當時的 50 特質全部重定義（雙面性＋副作用＋互斥＋維持），
  擴充後現有 **72 條**（通用 30／稀有 8／史詩 11／傳說 20／獨有 3）；配方 18 條
  （稀有 8＋史詩 10，素材分池）；傳說 20 個改由生涯任務卡發放（不再由配方合成）；
  獨有階 3 條（老來俏／千錘百鍊／火炬手）
- **名次序位單一來源**：`formats/finishes.js`，三份手抄本退役——名次以機器可讀
  鍵入帳，不再解析顯示文字
- **世界賽衛冕者**：冠軍登記表 `titleHistory` 逐年連續，衛冕者佔淘汰賽席位、
  比同輪對手強 4 點，淘汰賽贏下授予獨有特質「火炬手」
- **生涯傳記**：退役時依事實拼接維基式五段總結，同一段生涯必定同一篇
- **生涯任務面板**：任務進度、期限與素材對玩家可見；失敗降階為生涯標籤

**DEMO 與起點**
- DEMO 起點：2015 年、19 歲出道（PRO 路線，跳過業餘期）；校準基線為 AMATEUR
  （2012、16 歲）雙軌制
- **DEMO 三年期程（S21b）**：§19 期程改 **36 個月＝三個賽季（2015–2017）**，
  期滿未觸發任何結局即以「DEMO 結束」比照退役結算；期程單一來源
  `src/engine/demo.js`；狀態列顯示「DEMO 14/36」。校準隨之調整：§7.2 衰退跟隨
  峰前改讀固定潛力（「19 歲衰退錯位」一併解掉）、§18.2 位置需求門檻補年輕項
  （22 歲前每年 −2）；最後一搏不再重跑同一年（`closeYear`）
- 測試由 8939 項成長到 **19919 項**；`golden.json` 決定論快照刪除，改為
  比例制平衡不變式（`regression/invariants.mjs`）
- `SAVE_VERSION` 9 → **23**（**舊存檔全部作廢**）
- 規格書 `ESPORT-DESIGN-V4.md` 升到 **v4.5.3**；工作說明書主線 S01–S22 共 **39 站**
  全部完成（全表 53 站，庚辛兩組 14 站未開工）
- **版號單一來源**：`APP_VERSION` 從 `main.js` 搬到 `src/version.js`（畫面右上角
  徽章與結算分享圖讀它），值由停滯的 v4.0.0 對齊到 **v4.5.3**；新增
  `tests/kernel/version.mjs` 守著四份手抄本——`package.json`／`APP_VERSION`／
  規格書檔頭／CHANGELOG 最新節必須同號
- 內容編輯器（`tools/`）：事件卡／訓練卡／任務卡／特質／配方／天生特質的表單
  編輯＋驗證，輸出貼回資料檔

**授權**
- S03–S05 淨室重寫全部完成，本專案不再含原作者程式碼（血緣表見
  `ARCHITECTURE.md` 九節）

### S20g 賽事成績帳本與衛冕者：對手表上姓名 — 2026-08-16

在「對手一直是純數字」的引擎裡插進第一個**對手身分**概念，讓 `火炬手`（S20c 留著
「待對手身分層」的那一條）終於拿得到。

- **冠軍登記表 `titleHistory`**：每年世界賽的冠軍進同一張表（玩家與 NPC 共用），
  玩家沒奪冠／沒參賽的年份也照樣登記（復健年亦然，不留空窗）。查詢層
  `reigningChampion`／`lastFinish` 從它導出
- **衛冕者賽路**：上屆冠軍佔一個淘汰賽席位，相遇機率八強 15%／四強 30%／決賽 60%，
  比同輪一般對手強 4 點。淘汰賽贏下 → 授予獨有特質 `torch_bearer`
  （200 段實測取得率 4.0%）
- **統一條件語言**：退役 `whenHits`，事件卡觸發條件改吃 `evalCond`（與任務卡同一套）。
  事件卡條件終於讀得到史詩／傳說特質；六大屬性補進謂詞表
- 世界冠軍人均 0.055 → 0.035（200 段 A/B），分布變動交給 S20d 校準
- `SAVE_VERSION` 20 → 21（**舊存檔作廢**）。`npm test` 19093 項全綠

### S20c 交接鏈修復：名次以鍵入帳，賽段冠軍終於算分 — 2026-08-15

三個靜默 bug 同屬一類——**查表的鍵必須與另一個檔產生的字串逐字相同，而沒有測試在守**。
本站不修字串，改成不再拿字串當鍵。

- **名次以機器可讀的鍵入帳**：新增 `src/data/formats/finishes.js` 作為名次的單一來源，
  里程碑帶 `event`／`finish` 欄位，查詢層不再解析顯示文字
  - 真實的**世界賽冠亞軍**在生涯查詢裡回「沒打過」——連帶世界賽冠軍反而開不了
    `legend-worlds-king` 那張傳說任務卡。修復
  - `MSI 止步` 在帳本回 99（等同沒打過）、在傳記回 4。修復
- **每一座賽段冠軍在生涯評分裡值 0 分**（該值 80）：關鍵字 `'季後賽冠軍'` 對不上實際
  產生的 `'2020 PCS 夏季賽冠軍'`。改讀計數器。實測 200 段生涯平均每段 +50 分，
  4% 的生涯上移一個等第（門檻重校留給下一站）
- **獨有特質（第五階）接線上線**：`老來俏`（30 歲後單季獲獎）與 `千錘百鍊`
  （生涯出賽 ≥ 1000 場）現在拿得到；`火炬手` 待對手身分層
- **事件卡的空頭承諾修掉**：`賽前互嗆`／`招牌被Ban` 兩張卡寫了特質解鎖旗標卻零給予
- **史實解散**：2016 年的台北暗殺星永遠不觸發（該隊名單只到 2014），改 2014
- 內部整理：四階特質的階名四套拼法收斂成一套、編輯器的時段標籤砍成引擎兌現得了的
  6 個、訓練卡池別由檔位導出、刪除死程式碼
- `SAVE_VERSION` 19 → 20（**舊存檔作廢**）。`npm test` 18932 項全綠

### 規格書 v4.1：生涯任務卡、替代性生涯路線、種子天生特質（僅文件） — 2026-08-13

`ESPORT-DESIGN-V4.md` 升到 v4.1，併入外部提出的增訂清單（可行性研究見
`docs/v4/08b-規格V4.1增訂.md`）。**這一版只改文件，程式與測試未動。**

- **§0.5 設計原則**：四項檢驗（直覺／防農／死路／心流），列進每一站的收尾檢查
- **§12.3 生涯任務卡**：跨回合、有期限的任務，`legend`／`route` 兩型共用一套 schema。
  傳說特質改由它發放，**不再由配方自動合成**（§14.2 的 20 組素材組合原地轉成觸發條件）
- **§14.3 替代性生涯路線**：五條非冠軍路線（賽區統治者／流浪傭兵／不滅隊魂／究極綠葉／
  網紅選手），終點是史詩特質＋生涯標籤，讓平庸局也有明確的 build 終點
- **§1.4／§9.1 種子天生特質**：種子決定 0／1／2 個（40／40／20%），初始心理六維必須與
  之一致；順帶給了 `鐵人` 一條來源（原本取得率 0%）
- **§15.5 生涯傳記生成**：單局結束時依事實拼接生涯總結文本
- 重建工作說明書 25 站 → 30 站（新增 S17a／S17b／S19d／S21a）

### 四階特質合成（LoL 道具合成概念）＋大量新事件 — 2026-08-12

把隱藏特質從「基礎→史詩」兩層拆成四階：**通用 → 稀有 → 史詩 → 傳說**，概念取自 LoL
道具合成（小件疊終極裝）。每一階都是困難取得的隱藏素質，持有正確組合時自動合成，越往上
疊生涯天花板越高；配方仍完全隱藏，靠社群探索。

**四階架構**
- `src/data/traits.js`：通用特質（新增 `肝帝`／`梗王`／`鏡頭感`／`守護者`）＋稀有特質
  （練功機器、流量密碼、明星選手、定海神針、元老、傳奇偶像、諧星、高冷守護）
- `src/data/epics.js`：史詩特質（沿用既有 10 個核心配方）＋傳說特質（不死魔王、弒神者、
  銅牆鐵壁、流量金身、一穿五、版本之神）
- 稀有與史詩素材刻意分屬「人格／媒體」與「競技表現」兩池、互不重疊，兩條線可以同時養，
  史詩的可達性維持原版水準（實測 160 段生涯：史詩 63/80、傳說 23/80、平均巔峰 OVR 59.9）
- 引擎 `checkFusions` 泛化為跨階消耗（`[tier, key]` 配方），`state` 新增 `rare`／`legendary`
  兩槽；面板與結算依四階配色顯示

**新事件卡（16 → 26 張）**
- 通宵練功、梗圖爆紅、開台首播、粉絲見面會、隊友甩鍋、極限搶龍、賽前互嗆、招牌被Ban、
  賽後金句、被釣魚——大量取材自 LoL 社群與實況圈梗（人形外掛、開秀、打野差距、心態炸裂、
  招牌角、金句、釣魚）

**事件邏輯修正**
- 選項可帶自己的 `on` 結果：像「暫時關台／休息／不看」這類跟卡片主軸相反的路，不再套用
  卡片的通用結果（例如關台的結果不再是「開台時間控制得宜」）。`streaming.close`、
  `solo_queue.stop`、`scrim.rest`、`endorsement.reject`、`vod_review.skip`、
  `roster_drama.avoid` 已補上各自的 good/bad。

### 九項素質 → 六屬性＋導出技能（D&D 兩層制）

九個滑桿每季來一次，決策沒有質變只有量變，玩家很快就會用「哪個 OVR 權重高就餵哪個」
把它變成打卡。素質不是不重要，是不該由玩家直接管理。

**兩層制**
- 新增 `src/data/attributes.js`：六大屬性 `體能 VIT／靈巧 AGI／意識 AWR／技巧 TEC／
  默契 SYN／決斷 DEC`，是玩家唯一直接經營的一層（`state.ability` → `state.attr`）
- 原九項素質＋位置專屬共 13 項降級成**技能**，移到 `src/data/skills.js`，由屬性加權
  導出（`SKILL_WEIGHTS`，每列和為 1.00），玩家不能直接加點
- 技能值與屬性同量級（1–80），OVR 尺度與舊版一致，賽區 par／勝率公式／薪資曲線都
  不必重算
- `src/engine/abilities.js` → `src/engine/attributes.js`；`ROLE_ATTR_WEIGHTS` 在載入時
  把兩層權重折疊成 5×6 表，OVR 一次內積算完，不繞技能

**位置身分**
- 技能刻意只掛 2～4 個屬性。第一版每項技能都沾到五、六個屬性，折疊後五路權重長得
  差不多（最扁的中路最高／最低只差 2.8 倍），位置身分被平均掉。寫純之後輔助的靈巧
  權重掉到 .028——靈巧對輔助幾乎沒有意義，這正是位置該有的樣子
- 出生時該路 OVR 最吃的兩個屬性額外 `+0..4`（`ROLE_START_EDGE`），天賦一出生就有
  位置味道。取代舊版寫死的「lane/op/tf +0..4」

**平衡**
- 成長門檻整體下移 4（66/58/50 → 62/54/46）。同一份訓練點分給六項而非九項，每項多吃
  1.5 倍，不補這一刀全體通膨：80 段實測巔峰 OVR 60.7 → 63.7、傳奇 5 段 → 14 段。
  下移後回到 59.9 / 5 段，老手打法的等第分布幾乎完全對回舊制
- 年齡衰退改打 `agi/vit/tec`，`awr/dec` 30 歲後仍可能續升——「老將靠意識吃飯」變成
  規則的直接後果，不是特例
- 事件卡結果欄位 `ability` → `attr`，18 張卡全部重新對應到屬性。一張卡動意識，玩家
  會同時在大局觀、視野、遊走三條技能上看到變化
- 冒煙測試的兩條「打法差距」不變式換了指標。舊版「新手打法 0 座傳奇、新手邊緣選手
  必須多於老手」有一半來自每路有 2/9 素質權重為 0（平均分配等於丟掉兩成的點）。
  六屬性對每路都有份量，加上權重和固定為 1、成本階梯是凸的，數學上平均分配拿到的
  就是平均值。改為守「傳奇仍罕見（新手不到老手三分之一）」與「老手平均巔峰 OVR
  至少高 1.5」

**介面**
- 加點列固定六列；面板拆成「屬性」（可加點、附潛力刻度與 VIT/AGI 縮寫）與
  「技能（位置）」（唯讀，只列該路有 OVR 權重的項目，由重到輕）

**存檔**
- `SAVE_VERSION` 6 → 7。舊存檔沒有 `attr`，一律作廢重開

### 授權與出處
- 新增 **MIT License**（`LICENSE`），並在 README 補上原始出處（YaKyuLife）與授權狀態說明。
  改寫前已向原作者申請授權，等待作者回覆後再補上原作者的版權署名。

## [v4.0.0] - 2026-08-12
### 賽制脫離棒球模型：MSI 是俱樂部賽事

專案的賽制骨架承襲自棒球生涯模擬，但 LoL 的賽制與過程差距極大。最刺眼的一處是
MSI——程式把它寫成「國家隊徵召」，玩家可以婉拒，打完還會累積傷病風險。MSI 是
俱樂部賽事，門票發給戰隊。追查下去共列出 12 項棒球殘留，橫跨整個年度流程。

**MSI**
- 資格從「個人 OVR ≥ 52 被徵召」改成「你的隊伍拿了賽段冠軍」（2023 起前兩名）。
  刪除 `MSI_CALLUP_OVR` / `msiEligible` / `msiForced` / `intlLock` 整套徵召邏輯
- 沒有出不出賽的選擇。決策點改成媒體日與決賽前的扮演路口
- 賽制逐輪出比分：2015–2022 四隊小組雙循環＋BO5 淘汰賽；2023 起雙敗淘汰，
  輸一場還能從敗部殺回來。舊版整個 MSI 只有一次擲骰
- 時序歸位到賽季中段（原本在年末，與世界賽並列）
- 刪除 `carryInjuryRisk`（WBC 投手手臂磨損模型）。替代後果是真實的那個：MSI 打完
  到下一賽段只有兩三週，賽區內其他隊多練一個版本
- MSI 冠軍為賽區多掙一張世界賽門票（2023 起的真實制度）
- 特質 `nationalace` 國家隊王牌 → `soloking` 賽區之光

**世界賽**
- 門票不再是擲骰。種子序在席位內直接晉級，最後一張打地區資格賽（BO5，即 LCK
  Regional Finals／LCS Gauntlet）。舊版算好種子序卻還要 `rng.chance(96/88/74/30)`，
  第一種子有 4% 機率拿不到門票
- 席位數隨時代（`data/regions/*.js`）。舊版寫死「主場賽區 2、其餘 4」，但
  2012–2014 的主場賽區只有一張外卡門票，LCK/LPL 也是 2023 年才從 3 席增為 4 席
- 種子序來源隨時代：2013–2022 冠軍點數制，2023 起改看最後一個賽段的季後賽名次。
  舊版把冠軍點數一路套用到 2025 年，但那套制度 2022 年就廢止了
- 賽程逐輪出比分：入圍賽 → 小組賽／Swiss → 八強／四強／決賽 BO5
- 實測世界賽冠軍從人均 0.83 座降到 0.09 座

**先發／板凳**
- 體力不再決定出賽場次（棒球的輪值模型）。LoL 是五個固定先發，隊伍打幾場你就打
  幾場；體力低表現成手感下滑、陣亡數上升
- 新增板凳機制：打不到隊伍平均、默契見底、傷癒回歸位子被頂、年紀大了想換血都會
  被下放。坐板凳沒有戰報也沒有季後賽，連續兩段會被明講
- 傷勢從「整季報銷、下季復健年」改成缺席週數：小傷 2–5 週、重傷 8–16 週，
  真的要動刀報銷整季的只佔重傷的 12%

**轉會窗口**
- 窗口每年都開，不再是「合約到期才進市場」。三拍：傳聞 → 官宣 → 落定
- 合約中途可被買斷，這是新的決策點
- 新增外援名額：各賽區每隊限 2 名，主場賽區出身的玩家簽進任何海外賽區都佔一個。
  不是數值不夠，是位子沒了

**種子的意義**
- 拆成兩條亂數流：種子只決定天賦（起始能力、潛力、性格底色、初始英雄池），人生
  每次開新局都不一樣。引擎本身仍可重現，隨機的是「誰來決定人生種子」
- 修掉 `createState` 把 `seed` 宣告兩次的 BUG——後者（世界賽種子序）覆蓋前者，
  出生種子從來沒有真的存進 state。種子序更名 `seedRank`

**架構**
- `world.js` 依實體重切成 `data/regions/`（一個賽區一個檔）與 `data/formats/`
- 年曆抽成 `data/formats/calendar.js` 一張表，`game.js` 從 1033 行降到 221 行，
  賽事各自住進 `phases/*.js`。新增一個賽事＝寫一個檔、註冊一行、年曆加一列
- 特質效果從十個引擎檔的 66 個 `if` 收進 `kernel/modifiers.js` 的四個查詢函式
- 抽出 `kernel/{series,groups,strength}`，新增小組循環與 Swiss
- 修掉 `diminish()` 的保底缺陷：任何 ≥1 的變動永遠至少推 +1，邊際遞減在極端值
  等於失效。修正後打過六次以上國際賽的人大心臟不再清一色 100

### 同批進來的前端 UI 重設計

這一版還包含一輪獨立進行的前端重設計（樣式表 v3.1 → v4.3）：

- 色階改以 tftactics.gg 的計算樣式為準：#0d202b／#102531／#123040，容器髮絲線
  #17313a 只用於容器與分隔
- 小控制無邊框、純填色 #123040、3px 圓角；容器 4px 近直角
- 動作藍 #227aad（hover #4080b0）、active 底線 terracotta #d47559、
  遊戲狀態金 #b89d27、標題亮青 #adf4ed
- 移除自製 SVG 背景（`assets/rift-bg.svg`）與 Cinzel 襯線展示字
- 新增 `DESIGN.md`（視覺規範）與 `PRODUCT.md`（產品定位）

> 註：這批 UI 檔案在合併時被掃進了 `6ec9575`（年曆抽成資料表）與 `3af552d`
> （世界賽重寫）兩個賽制 commit，不是獨立的 commit。要查 UI 的實際變動請直接
> `git diff c94276b..HEAD -- index.html src/styles.css assets/`，不要照 commit
> 訊息找。

**測試**
- `headless.mjs`（23KB 單檔）拆成 `tests/{kernel,phases,history,regression}/`，
  runner 自動掃描目錄
- 新增黃金種子快照：160 段生涯的 state 雜湊，純重構必須逐位元一致
- 新增 `history/` 分區：直接斷言史實（2020 無 MSI、2022 不得 Swiss、2025 不得用
  冠軍點數、席位數逐年）
- 檢查項數 4047 → 9400

## [v3.5.0] - 2026-08-11
### UI 重設計：簡約數據站風格

- 移除舊版裝飾語彙：金色漸層標題、光暈、角框、鑽石符號、襯線展示字與自製 SVG 背景。
- 新色階對齊 tftactics.gg 計算樣式：#0d202b／#102531／#123040，容器邊框 #17313a；
  動作藍 #227aad、遊戲狀態金 #b89d27、目前階段底線 terracotta #d47559。
- 字型統一為單一無襯線 Inter＋Noto Sans TC，數值用系統等寬字。
- 事件日誌與選手資料面板改為深色卡片，長文可讀性優先；全文對比 ≥ 4.5:1。
- chrome 結構同站內頁：toolbar＋nav 列；小控制無邊框純填色、3px 圓角；
  主按鈕藍底白字；標題 2px 藍底線。

## [v3.4.0] - 2026-08-11
### 賽段制、BO 系列賽、心理與性格軸

- **事件卡改成玩家決策**。舊版是引擎背著玩家擲一次 50/50 就把結果貼出來，
  一整段生涯能真正做決定的只有訓練加點與合約路口。現在每張卡先描述處境，
  再讓玩家選一條應對路線，選項本身決定成功率與數值幅度。安全牌換到的是低變異，
  代價是那條路不會覺醒任何隱藏素質。
- **賽段制**。依史實把一年拆成 1～3 個賽段：2012 單季 → 春／夏兩賽段 →
  2025 起三賽段；韓國 2012–2014 本來就是 OGN 冬／春／夏三季，歐洲 2023 起三賽段。
  每個賽段各自跑例行賽、季後賽、抽事件卡，場次按權重切分——變多的是決策點，
  不是比賽場數。全年冠軍點數決定世界賽種子序。
- **BO 系列賽**。季後賽從「一次擲骰吐冠軍」改成八強 BO3 → 四強 BO5 → 決賽 BO5
  逐局模擬，種子序決定從哪一輪打起，決勝局另外吃「大心臟」加成。
- **心理與性格軸**。新增五個維度（大心臟／隊友默契／自我／知名度／風評），
  跟能力值完全分開：訓練點碰不到，只能靠扮演卡累積。知名度與風評分成兩條軸，
  才表達得出「誇張言行同時收穫人氣與負評」。
- **扮演卡 18 張**，分賽前記者會／媒體採訪／休息室／教練談話／日常訓練五個時機。
  **不擲骰**——扮演不是賭博，選了就是那樣的人；隨機的只有外界反應，
  力道由知名度放大。新增 7 個性格特質與 2 個史詩合成。
- **下剋上劇本**。低種子序在季後賽與世界賽獲得加成，幅度由心理素質決定，
  且設有上限——要讓它「有機會」，不是「換個種子序就變強隊」。
- **更衣室與輿論的後果**。默契見底會被迫轉隊、風評見底會被球團切割，
  兩者都直接作廢合約並讓市場報價縮水；反過來知名度高則續約時球團被迫加薪留人。
  設三年冷卻，避免變成常態。

### 顯示
- 心理值**全程不對玩家揭露數字**。面板只給粗略標籤（「一上大場面就抖」「毀譽參半」），
  事件結算只給箭頭。攤成數字之後玩家就會開始最佳化它，扮演就沒了。

### 測試
headless 檢查由 12 項擴充到 19 項，新增：賽段結構隨史實演進、賽段權重總和為 1、
心理值不外洩數字、扮演卡不碰能力值、BO 系列比分自洽、下剋上加成有上下界、
球團切割有冷卻不會變常態。

### 相容性
- `SAVE_VERSION` 4 → 5，**舊存檔失效**。

## [v3.3.0] - 2026-08-11
### 介面重設計：官方配色 × 召喚峽谷意象

- **全面改用 Riot Games / LoL 官方配色**：深藍 `#0A1428`、近黑 `#010A13`、
  金 `#C89B3C`、亮金 `#F0E6D2`、深金 `#785A28`、青綠 `#0AC8B9`。
- **LoL 客戶端 UI 語彙**：金色 CTA 按鈕、六角水晶標誌、卡片金框＋四角飾、
  頂欄金色漸層邊線、能力條金色刻度。
- **字型**：標題改用 Cinzel + Noto Serif TC，正文/按鈕用 Rajdhani + Noto Sans TC。
- **開場畫面重構**：品牌標頭、主視覺、六角表單面板、金漸層標題。
- **自製原創背景** `assets/rift-bg.svg`：召喚水晶＋六角紋理＋地圖路線，
  完全原創、無外部依賴、可離線運作。
- 結算分享圖同步改用官方色票與字型。
- 開場頁新增 **Riot Legal Jibber Jabber 粉絲專案聲明**（授權政策要求）。

### 素材授權
- 依 Riot「Legal Jibber Jabber」政策，本專案為非營利粉絲致敬作品；
  未使用任何 Riot 旗下圖片（英雄美術圖、地圖、圖示、商標）。
  背景為自製 SVG，字型為 OFL 授權（Cinzel / Noto / Rajdhani）。

### 修正
- **數值字體加大**：頂欄狀態（歲／年份／綜合／生涯薪資）與能力值數字放大，
  訓練點剩餘數與骰子一併加大。
- **加點／減點更明顯**：訓練分配每列新增明顯的「＋／−」按鈕（46px 大按鈕），
  點「−」可退回該能力上一次投入；原「復原」改為「復原上一點」。

# 更新日誌 (Changelog)

## [v3.2.0] - 2026-08-11
### 業餘階段：達標就簽，不必熬滿三年

- **不再強制三年業餘**。只要綜合能力達標，隊伍隨時會找上門；三年期滿改成
  「必須做決定」的時點，而不是「才可以開始試訓」的時點。
- **門檻分成三層**：青訓二隊 36、主場一隊 45、海外賽區 47。
  舊版只給「投入主場賽區試訓」一個選項、門檻 45——但實測三年期滿時 OVR 中位數
  只有 37，那個選項的成功率是 **0%**，每個人都是走完一個假的路口再被丟進青訓。
  現在選項依實際達標情況產生，打不到的層級不會出現在畫面上。
- 前三年的邀約可以**婉拒**，留在業餘把數值養高再談，隔年只要仍達標就會再有人來。
- 連青訓門檻都摸不到時可以繼續打網咖盃，但最多撐到 22 歲。
- 實測離開業餘的年份分布（老手打法 300 段）：第 1 年 3%、第 2 年 31%、
  第 3 年 52%、之後 14%。

### 修正
- **二隊名稱時代錯置**：舊版寫死 `['閃電狼二隊', 'ahq Academy', 'PSG Talon Academy',
  '峽谷次級聯隊']`，於是 2013 年就會冒出 PSG Talon Academy——PSG Talon 2020 才成立。
  改為由「當年真實存在的一隊」推導（`橘子熊 Yoe Flash Wolves 二隊`），時代自動正確，
  也不必再維護第二份名單。

### 測試
新增：達標即被挖角、婉拒後隔年仍會被找上門、未達標不會有星探、
只夠青訓的實力不會看到一隊選項、二隊名稱不會時代錯置。

## [v3.0.1] - 2026-08-11
### 內容：事件文本與論壇對話在地化（參考 LOL 事件與梗）

* 重寫 `EVENT_CARDS` 的 good／bad 敘述，加入 LOL 玩家慣用語與網路梗
  （版本答案、版本逆子、0/10/0、666、斗內、梗圖、召喚峽谷、宮鬥劇、人機…），
  更貼近職業賽場與討論區風氣。
  **事件名稱、能力增減、旗標、kind 全部未動**，判斷邏輯不受影響。
* 重寫 `FAN_QUOTES` 為 PTT／巴哈／虎噗風格的論壇留言，加入英雄暱稱
  （盲僧、鱷魚、狐狸、VN、瑟雷西）讓退役留言更像真實討論串。

## [v3.0.0] - 2026-08-11
### 架構重寫：單檔 HTML → 分層 ES 模組

`index.html` 從 987 行縮成純畫面骨架，規則搬進 `src/engine/`、資料搬進 `src/data/`、
畫面搬進 `src/ui/`。零建置、零相依，GitHub Pages 直接可跑。詳見 `ARCHITECTURE.md`。

* 生涯流程由「回呼金字塔」改寫成 **generator 流程機**，一年的時間順序在程式碼裡由上而下讀得完。
* 引擎完全不碰 DOM → 新增 `tests/headless.mjs`，可在 Node 裡跑完整段生涯做回歸測試。
* 新增 `ARCHITECTURE.md`。
* **專案更名整理**：`YaKyoLife-WIKI.md` → `WIKI.md`；移除多餘的 `yakyulife/` 巢狀資料夾
  （repo 內容改為直接放在根目錄，與 GitHub Pages 實際服務的層級一致）；
  v2 時期的改造計劃書移入 `docs/archive/`。GitHub repo 本身的名稱未動。

### 修正：讓遊戲能跑

* **整個遊戲在載入時就會崩潰**。`$` 定義為 `querySelector`，但 `$('log')`、`$('start')`、
  `$('seed-show')`、`$('act')` 等呼叫都少了 `#`，第一次 `card()` 就丟 TypeError。
* `faFlow()` 對 `const stayM` 重新賦值（`stayM = Math.max(...)`），只要持有 `神主牌`
  或 `更衣室傳奇`，合約到期就必定拋 TypeError。而且 `stayM` 算完根本沒被用到。

### 修正：每季旗標沒有重置

* `champThisTeam`（季後賽奪冠）只在換隊時才清除 → 拿過一次冠軍，之後每年的世界賽出線
  機率都永久 +25%。
* `disbandAverted`（改寫史實）永不清除 → 生涯拿過任一座世界冠軍後，往後所有歷史解散
  事件全部自動免疫。
* `injNext`（國際賽消耗）設為 10 之後永不歸零 → 打過一次 MSI，受傷風險永久 +10%。
* 復健年（整季報銷）直接跳過合約結算 → 等於白送一年合約。

### 修正：規則寫反或形同虛設

* **版本補習的好結果反而加重懲罰**：好結果會 `metaCount += 1`，而懲罰正是由 `metaCount`
  推導出來的。改成獨立的「版本落差 `patchDebt`」，補習成功直接降低落差。
* **海外選手一進 FA 就被強制降級**：`faMarket()` 只從主場賽區 `teamListOf('HOME')` 產生報價，
  LCK/LPL 選手合約到期後收不到任何海外報價。改為依當下實力決定候選聯賽。
* **史實解散可以用時序漏洞繞過**：簽約發生在休賽期，但當年解散的戰隊沒有從名單移除，
  於是可以在 2019 年休賽期加入剛倒閉的閃電狼，之後那支隊伍永遠不會觸發解散。
* **列管徵召只鎖三年就永久失效**：`intlLock` 只記錄「第一次被徵召的年份」且從不更新。
* **「連續 3 次」拒絕享樂事件不是連續的**：計數器從不因失敗歸零。
* `單身` 年數同時在兩處累加，實際只需一半時間。
* 洗牌用 `sort(() => R() - 0.5)`，結果依賴 JS 引擎排序實作，跨瀏覽器破壞種子決定論。
  改用 Fisher-Yates。
* 中路 OVR 權重漏掉位置專屬能力 `刺客`，中路 OVR 結構性偏低。五路權重重新校正為總和 1.00。

### 修正：畫面

* 加點介面每次點擊都整排重繪，把剛顯示的「+N」提示立刻洗掉；且沒有任何地方顯示
  「下一點要花幾點」。現已常駐顯示成本與蓄力餘額。
* 潛力刻度線被 `overflow:hidden` 裁掉。
* 結算圖在 `fillText` 之後才設 `textAlign`，標題永遠對不齊；畫布高度寫死 420 但內容只
  畫到 200，下半張全白。改成先量後畫。
* 生涯數據表直接印出內部鍵值（`HOME`／`REG`／`AM`）。
* 開場畫面殘留棒球版的縫線圖示；`maximum-scale=1.0` 擋掉手機縮放。

### 平衡

* **生涯評分重新校準**。舊公式把累積 K／SOLO 直接乘上大係數，任何撐滿十幾季的選手都能
  破 2200，人人都是「傳奇」。新公式壓低「量」的權重、拉高榮譽與巔峰 OVR。
* **年度獎項門檻收緊**。舊版單殺王要求「單殺 ≥ 場次 ×1.2」，但上路／中路的單殺基線本來
  就是每場 1.2–1.3，等於年年必拿；一段生涯能堆出 40 幾項榮譽。改成與同位置基線比較並
  加上擲骰。
* 版本落差改為可被英雄池深度吸收（池深每多 1 隻抵銷 1 點），並設 0–10 上下界。
* 受傷、成長、衰退曲線維持設計文件數值，僅補上 `不老傳奇` 對反應／操作的二次減半。

### 新增：設計文件寫了但沒實作的部分

* **選手資料面板**（右上 ☰）：能力值與潛力刻度、英雄池與專精場次、目前版本與版本落差、
  隊友名單與教練加成、合約狀態、已覺醒的隱藏素質、榮譽紀錄。舊版只有在訓練加點那幾秒
  看得到能力值，英雄池與版本狀態全程沒有出口。
* **存檔續玩**：年初自動存檔，開場畫面出現「繼續上次的生涯」。
* **英雄池會成長**：出賽累積專精，練滿的英雄正式進入比賽池並回饋版本適應力。
* 海外青訓路線真正可走（舊版選了「直接投入海外賽區試訓」其實只是進主場青訓）。
* 新增事件卡：隊內矛盾、海外集訓。
* 新增合約選項：長約／短約的年限與年薪係數會隨表現浮動。
* 年度紀錄可折疊，長生涯不再是無盡卷軸。

## [v2.0.0] - 2026-08-11
### 重大改版：棒球 → LoL 電競生涯模擬
* 全新主題「電競人生」，位置改為五路（上路／打野／中路／射手／輔助）。
* 預設從 S2（2012）開始、16 歲；中度史實演進（GPL→LMS→PCS→LCP、MSI、世界賽 Swiss、薪資隨時代調整）。
* 能力系統重設計：8 共同能力＋位置專屬，位置加權 OVR。
* 新增英雄池（每路 8 隻）與版本 Meta 機制。
* 新增五路數據模擬（K/D/A、CS、視野、傷害、單殺、MVP）。
* 新增 MSI／世界賽（含 Swiss 賽制）與國際賽徵召。
* 新增老將長壽機制：衰退曲線、退役硬上限，可延後至 40。
* 新增歷史解散事件與改寫：解散隊奪世界冠可避免解散；未奪冠強制 FA；FA 不達標強迫退休。
* 新增隱藏特質與特質合成系統（完全隱藏，8 條合成配方）。
* 新增隊友與教練系統。
* 改寫成就、事件庫、命名（試訓／退役／榮譽殿堂）。
* 新增 `ESPORT-DESIGN.md` 並重寫 `README.md`、`YaKyoLife-WIKI.md`。

## [v1.3.3] - 2026-08-09
（棒球版最終更新，詳見 git 歷史。）
