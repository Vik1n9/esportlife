# v4.8.5 穩定點快照（V4 重建後）

> 2026-08-12 建立；2026-08-16 全文重寫；2026-08-17 版號更新至 v4.6.3（壬組 S42 完工）；
> 2026-08-18 庚組收尾（S30 校準重驗）：基線數字全面重量回寫；
> **2026-08-19 辛組收尾（S36 校準重驗）：基線重量、零常數變更**。V4 重建主線 39 站
> （S01–S22）走完，四根柱子全換；庚組 S23–S30、辛組 S31–S36 與壬組 S37–S42 完成。
> `npm test` 長到 **23756 項**。
>
> **這份是背景資料，不是工作指令。** 要開工請看
> [`README.md`](README.md) 的階段表與 [`00-共通規則.md`](00-共通規則.md)；
> 想知道「現在長什麼樣」讀這裡，想知道「每一站怎麼修」讀各站說明書。
> 本檔的讀者是**辛組**（S31–S36，賽事模擬）。

這份文件記錄 v4.5.3 的**現況基準**：現在站在哪裡、安全網真正守得住什麼、
量到的數字長什麼樣、哪裡還是空的。後續文件都以這份為基準；任何一項描述
與程式碼不符，是這份文件要改，不是程式碼要遷就它。

重寫的理由很實際：V4 重建一口氣翻掉四根互相咬死的柱子——回合粒度
（年 → 月）、成長模型（擲骰加點 → 設施制）、評估層（OVR → 教練評價）、
屬性刻度（1–80 → 0–100）——任一根單獨改都會讓其餘三根的校準失效，所以是
一次重建、沒有分批。重建前的快照講的那個「現在」已經不存在了，重新釘一次，
讓庚辛兩組有東西可以 diff。

---

## 一、基準座標

| 項目 | 值 |
|---|---|
| 版本 | `4.8.5`（軟體版號與規格書 `ESPORT-DESIGN-V4.md` 共用同一套，AGENTS.md 版本編號規則） |
| 存檔版本 | `SAVE_VERSION = 28`（v4.0.0 時代的存檔一律作廢；S28→25 隊友 NPC 引用、S29→26 `oppFaces` 計數、S32→27 `leaguePool`、S34→28 `blankSeasonStat.DPM`、S36 定案不再動） |
| 程式規模 | `src/` 99 個 `.js`、~16700 行；`src/styles.css` ~613 行；`index.html` ~146 行 |
| 開發者工具 | `tools/` 內容編輯器（事件卡／訓練卡／任務卡／特質／配方／天生特質）＋ `calibrate-160.mjs`（160 段校準量測，S30；`--intl`／`--influencer` 兩項診斷）＋ `microstats-report.mjs`（微觀分布，S33），零相依，獨立於遊戲 |
| 測試規模 | 48 個 suite ＋ runner ＋ harness（49 個 `.mjs`、~8100 行） |
| 建置 | 零建構、零相依。原生 ESM，GitHub Pages 直接服務 repo 根目錄 |
| 驗證指令 | `npm test`（＝`node tests/run.mjs`） |
| 驗證結果 | **23756 項檢查全綠**，約 14s |

Node 18+ 即可，不需要 `npm install`。版號四處手抄本（`package.json`／
`src/version.js` 的 `APP_VERSION`／規格書檔頭／`CHANGELOG.md` 最新節）由
`tests/kernel/version.mjs` 守著，必須同號。

---

## 二、結構上守住的東西

這些是 v4.5.3 的承重牆。改動如果會破壞其中任何一條，那不是「在穩定點上往前走」，
是要另外開一個穩定點。前六條承襲重建前（語義變了的會註明），後六條是重建立起來的新牆。

**① 依賴方向單向**：`data ← kernel ← phases ← engine ← ui`。
`engine/` 不 import `ui/`、不碰 `document`，所以整段生涯可以在 Node 裡跑完——
23756 項測試能存在，靠的就是這條。

**② 階段只匯出兩個東西**：`export const kind` 與 `export function* run(g, phase)`。
階段不 import `game.js`，沒有循環依賴。**新增一個賽事＝寫一個檔、註冊一行、年曆加一列。**
月回合的 `phases/month.js` 與五拍賽事序列 `phases/seriesEvent.js` 是同一個協定。

**③ 一年有哪些賽事是資料**：`data/formats/calendar.js` 一張表。月回合制後
`order = 月 × 10 + 檔位`；符號位置（`SPLIT_OPEN`／`MSI_SLOT`…）由
`engine/calendar.js` 依當年賽段數展開成實際月份——年份的形狀是算出來的，不是抄表。

**④ 賽區是實體**：一個賽區一個檔（`data/regions/*.js`），席位數、賽段數、外援名額、
隊名都住在同一個檔。賽段結構隨史實逐年演進（2015 春／夏 → 2025 三賽段）。

**⑤ 特質效果是資料**：`data/traits.js`／`epics.js` 自己宣告效果，消費端只問
`kernel/modifiers.js` 的 `bonus`／`factor`／`floorOf`／`capOf`／`flag`／
`lifecycleWindows`。四種運算刻意不合成單一 `apply()`。v4.3 起特質效果可自由掛載
在任一層（§14.4 掛載點全景表），效果鍵由編輯器（`tools/schema.js` 的 `EFFECT_KEYS`）
與引擎測試共同鎖定。

**⑥ 種子決定天賦，不決定人生**（語義變了）：出生流（玩家輸入的種子）決定起始屬性、
潛力天花板、天生特質（0／1／2 個）、心理底色、英雄池、業餘隊伍、生命週期參數；
人生流每局隨機抽。引擎本身仍是 `(出生種子, 人生種子, 選擇)` 的確定性函式——
測試靠它重現 160 段生涯。但**「同種子＝同一段人生」的保證已廢**（V4 §20.1 廢決定論），
`golden.json` 黃金種子快照隨之刪除——同種子可以反覆玩出不同的人生。

**⑦ 三層養成**（重建前是兩層）：玩家只經營六屬性（0–100）；十二技能是屬性加權
導出值，唯讀、不能直接加點；教練評價是評價層——薪資／試訓／續約／FA 全部讀它，
玩家看不見公式（`coachRating` 是內部值，UI 不得讀取）。位置身分靠技能權重保證，
不再靠屬性折疊。

**⑧ 條件語言唯一**：`engine/conditions.js` 的 `evalCond` s-expression——任務卡、
事件卡、特質授予、退役結局共用一套。⚠ 加謂詞要同時加進 `conditions.js` 的 `QUERIES`
與 `tools/schema.js` 的 `PREDICATES` 兩張註冊表，同一個 commit；脫節測試會直接抓。
`eventTrigger.js` 的 `whenHits` 已退役。

**⑨ 謂詞讀查詢層**：條件語言一律讀 `engine/ledger.js`（生涯軌跡帳本），不讀 raw
state 欄位。改一個欄位要改三個地方的坑，擋在查詢層。

**⑩ 一份資料一份來源**：名次序位 `data/formats/finishes.js`（三份手抄本已退役）、
版號 `src/version.js`、DEMO 期程 `engine/demo.js`（`DEMO_MONTHS`／`isDemo`…五個純函式）。
跨檔字串比對必須有測試，且測試要過寫入端的實際運算式。

**⑪ 內容由編輯器產**：事件卡／訓練卡／任務卡／特質／配方／天生特質一律用 `tools/`
表單編輯器產出（86 事件卡、60 訓練卡、25 任務卡、72 特質、18 配方全是它產的）。
欄位合法性的第一道閘在表單，第二道在測試。手寫資料檔可以，但要自備兩道閘的記憶。

**⑫ 傳說特質只由生涯任務卡發放**：不從配方合成、不從事件卡直接給。
20 張 `legend` 卡是傳說唯一發放口；5 張 `route` 卡給平庸局替代性生涯路線
（發史詩＋生涯標籤，不發傳說）。這是鐵則層級的決定——S07 的兩條傳奇不變式
全部經過這個發放口（S17b 生涯任務引擎）。

---

## 三、安全網覆蓋到哪裡

`tests/run.mjs` 自動掃描四個目錄，加 suite 不必改 runner；suite 間用 `shared`
共享冒煙跑出的生涯樣本（160 段只跑一次），用 `order` 排序保證樣本先生產。

| 分區 | suite 數 | 檢查數 | 內容 |
|---|---|---|---|
| `kernel/` | 25 | 17502 | BO 系列與種子序、小組賽與 Swiss、特質合成、條件語言、帳本與傳記、訓練事件卡、設施制、體力曲線、生命週期、心理界線、版號單一來源、逐選手對手（年代一致／聚合式／選隊）、隊友 NPC 池、母體百分位 |
| `phases/` | 8 | 1369 | 業餘起點與挖角、先發板凳與傷勢、自由市場與轉會窗口、賽事事件序列五拍、衛冕者、隊友抽選（S28） |
| `history/` | 5 | 351 | 史實斷言：2020 無 MSI、2022 不得 Swiss、2025 不得用冠軍點數、席位數逐年、解散與簽約名單過濾 |
| `regression/` | 5 | 4225 | 160 段生涯冒煙與評價分布、**比例制平衡不變式（測試網）**、DEMO 起點小矩陣、一段生涯年表、DEMO 模擬腳本端到端 |

### 平衡不變式取代了黃金種子快照

`regression/invariants.mjs` 是 V4 的安全網核心：它不錄快照（決定論已廢，錄不了），
改守「這個遊戲之所以是這個遊戲」的性質——**巔峰上界、打法差距、頂端才兌現、傳奇
稀有度、位置身分、合成消耗、潛力衰減、心理是放大器、明星效應上界**。門檻一律寫成
**比例**（÷ 屬性上限／÷ 可成長空間），換刻度不必重刷基準。⚠ 它擋得住「性質變了」，
擋不住「平衡變得更差」——好壞仍要靠分布數字與人的判斷。

鐵則二仍在：位置身分不准被平均掉——任兩路權重 L1 距離 ≥ 0.10、每路前二重屬性
合計 ≥ 0.45、靈巧對輔助 ≤ 0.06。

---

## 四、量到的基準（160 段生涯，S36 辛組收斂後）

改動之後這些數字會動，這裡記的是「現在是多少」，不是「應該是多少」。
冒煙矩陣＝16 組種子 × 五路 × 兩種打法（focus 集中／spread 平均）＝160 段。
量測工具 `tools/calibrate-160.mjs`（`--intl` 出大賽經驗加成診斷、`--influencer`
出網紅可達性診斷）。**S36 重驗結論是零常數變更**——四條驗收線全落帶內，
`OPPONENT_SUPPORT_RESIDUAL` 維持 S30 推的 16.5（判準→量測→結論見
`kernel/strength.js` 檔頭）、檢定四常數與 `STAT_BASELINE` 亦不動：

| 指標 | 值（S36） | 對照 S30 |
|---|---|---|
| 平均巔峰教練評價 | 73.19（÷上限 0.732；最高 95、最低 58） | 74.19 |
| 頂端落差 | 0.178（門檻 0.08，**餘裕 0.098**） | 0.175 |
| 傳奇率 | 老手 16.3%、新手 2.5% | 17.5%／0.0% |
| 傳說特質持有率 | 23.8%（15–25% 帶內；傳說 ≥3：16.9%——見第五節） | 18.1%／13.8% |
| 史詩特質持有率 | 55.6% | 51.2% |
| 平均職業年資 | 10.7 季 | 11.4 |
| 平均退役年齡 | 30.7 | 31.5 |
| 人均世界賽冠軍 | **0.081 座**（＝S28 錨，逐顆落錨；MSI 人均 0.244） | 0.037／0.188 |
| 國際賽局勝率 | **53.5%**（S28 錨 53.1%±1pp 帶內） | 53.1% |
| 退役年份 | 2017–2036 | 2017–2035 |

等第分布（老手｜新手，各 80 段）：傳奇 13｜2、歷史級 10｜10、優秀 21｜17、
稱職 25｜21、邊緣 11｜30。

體力節奏（18666 個月樣本）：休息 29.6%、透支月 27.2%——手感中點 52.5 與
體力修正係數 0.024 的判準（平均手感 ≈ 0.994／修正 ≈ 1.24 點）量在月內谷底，
辛組不碰體力經濟，常數未動。對手實體化率 intl 19.3%／playoff 21.0%（intl 自
S35 起是 p2 池口徑；門檻 90%／80% 等 S24 補抓資料後再議）。隊友真實 NPC 佔比
71.9%、rating 均值 65.9。Bo5 高壓檢定：1565 場 Bo5、進第五局 537 場（34.3%），
衰減均值我方 0.063／對手 0.188（淨 +0.125 偏玩家，見第五節）。

> ⚠ **這張表對「背景模擬加內容」不穩定**（S36 逐 commit 歸因）：S32 一個平衡
> 常數都沒動，只是把背景模擬掛上**共用人生流 `rng`**（§24.2.1 定案），巔峰就
> 掉 0.8、年資掉 0.5、route 後 50% 覆蓋率 11.3% → 3.8%——樣本組成重排而已。
> 往後動 `leagueSim` 的抽樣次數，這張表要整張重量。

四十段生涯帳本分布（S19c 校準 §14.3 門檻直接讀這裡）：打過國際賽 13/40、
平均總局數 43.5、teamHistory 平均 2.4（1–5）、disbandCrises 平均 0.7、
awards 平均 5.4（0–24）。

> ⚠ **冠軍人均**：世界賽 **0.081 ＝ S28 錨**（S30 時 0.037 未落錨，辛組後自己
> 走回來了）；MSI 0.244 vs 錨 0.131 仍高——160 段裡的冠軍是稀有事件（世界賽
> 13 座、MSI 39 座），樣本小且明星項的結構差不是單一常數能逐顆補回，穩定分布
> （勝率／巔峰／落差／持有率）全部落錨即視為收斂（S30／S36 同一條判讀）。
>
> ⚠ **頂端訊號仍薄**：頂端才兌現一條不變式扛著「頂端有沒有門檻」，餘裕 0.098。

---

## 五、現況全綠

23756 項全綠，無紅燈。S36 未改任何常數，檢查數與入站同數——本站新增的
`state.deciderLog` 不消耗 `rng`，樣本零位移。

要盯著的數（不是紅燈）：

- **傳說 ≥3 持有 16.9%**（§14.2 目標線 ≤12%）——S19c 之後就存在的漂移
  （S30 時 13.8%），辛組只是重排樣本。結構是「122 段一張沒有、38 名持有者裡
  27 名 ≥3」，修復要動任務卡體系，歸內容站。
- **route 後 50% 覆蓋率 6.3%**（驗收線 60% 是 S19c 幽靈量測——S26 重跑 S19c
  當代碼只有 5.0%）。量測已在 `regression/invariants.mjs` 落地（打日誌不硬紅）。
  S36 逐站歸因：S32 的 `rng` 重排就掉到 3.8%，S34 的 fame 掛載讓
  `route-influencer` 從 5 段漲到 12 段但全落前 50%。補後段局路線屬內容站。
- **Bo5 高壓檢定淨 +0.125 點偏玩家**（§24.4.2）——玩家 m 均 74.4（89.9% 零衰減）
  vs 對手 m 均 50.8（1.7% 零衰減），S31 寫的「兩邊各衰 ~0.2 互相抵消」不成立。
  四條驗收線都沒破，S36 依「破帶才有授權」不動門檻；改法（門檻 60 → 50）
  登記在 §24.4.2 的 S36 註記。
- **`intlExp` 池內 95.5% 打上界**（§24.4.1）——加成退化成常數 +3.0，「老將紅利」
  名存實亡；上界 3.0 之下鑑別度與局勝率錨互斥，唯一兩全解 0.25／4.0 是規格決定。
  診斷重跑：`node tools/calibrate-160.mjs --intl`。
- **K/D/A 顯示量級約真實聯賽 0.45×**（§24.2.4）——隊伍合計場均 5.2／5.8／9.8
  vs ~12／~12／~26；DPM／CSM／VSPM 三維正常。兩側對稱，可比性沒破；改基線表
  是規格決定。⚠ 附帶 K≠D 守恆差 11%。
- **實體化率 intl 19.3%／playoff 21.0%**（§23.4 門檻 90%／80%）——池薄
  （強度可算 NPC 389/938、完整五人隊 53 支），餵 S24 補抓，不是平衡紅燈。

~~`clutch` 持有 1/160~~ **已結案（S36）**：不是發放沒咬到——160 段有 65 段取得，
其中 63 段被四條史詩配方吃進 `fusedAway`。`phases/playoff.js` 的 TODO 已刪。

---

## 六、已知缺口（不阻塞，但別假裝不存在）

- **UI 沒有自動測試。** 23756 項全在引擎層；S21b 的瀏覽器驗證是 playwright 人工
  逐項跑。`src/ui/` 出的錯一項都擋不到。
- **英雄池與版本主題是現代快照**（`data/heroes.js`，每路 8 隻，同人引用），
  不隨時代回溯。
- **歷史解散事件是代表性子集**（7 條），海外隊只有兩條（IG／MAD Lions KOI），
  主場賽區為主。
- **出生流取數順序與規格書 §1.4 明文不符**：`state.js` 實際把業餘隊伍放在第 3 位
  （§1.4 寫的是第 7 步）。無功能影響（同種子仍同輸出），但移動會位移全部既有種子，
  160 段基線要全部重校——**刻意不動**，S28 隊友整合走人生流 `rng`、不碰出生流，
  重排只會讓 S30 校準對照更亂（S28 交接筆記），TODO 仍留在 `state.js`，往後
  哪一站真的要動出生流再一併對齊。規格書 §1.4 表不動（它是設計意圖）。
- **NPC 資料庫沒有更新機制**：新賽季資料如何增補（`tools/npc/` 管線重跑＋
  ESM 資料檔重產）庚組刻意沒做，流程與門檻見 S30 交接筆記。
- **160 段基線對背景模擬不穩定**：§24.2.1 定案「隨機數流走人生流」，`leagueSim`
  的抽樣次數一動，整張基準表就重排（S36 逐 commit 歸因）。要治本得把背景模擬
  移出人生流，屬推翻 §24.2.1 的規格決定。
- **route 後 50% 覆蓋率 6.3% vs 驗收線 60%**：驗收線的出處（S19c 實測 63.7%）
  是幽靈量測，補平庸局路線或重訂驗收線屬內容站（第五節）。
- **傳說 ≥3 持有 16.9% 超 ≤12% 目標線**（第五節）。
- **傳記第五段三年生涯一律落 `plain`**——S21a 領域，未處理。
- **六維過度副作用（§21.3）刻意沒做**，V4 完工後再議。
- **`WORKLOG` 記過但沒解的方向**：若想找回加點的份量，方向是讓評價對「尖峰」有
  非線性回報，不是加回死屬性。
- **平衡不變式擋得住「變了」，擋不住「變得更差」**——分布檢查與人的判斷仍是必要。

---

## 七、怎麼算「還在這個基準上」

往前走的時候，用這四條自檢：

1. **`npm test` 全綠**，而且檢查數只增不減（基準 23756）——減少通常代表某條檢查
   被改成恆真或比對到 `undefined`。⚠ 例外：校准站動對手／對手強度後，生涯打進
   深輪的段數改變，**依樣本數迭代**的檢查（帳本逐里程碑、不變式逐事件）會跟著
   樣本縮——S30 校準 −228 項屬此類（逐條查過，非恆真化）；引用此基準的站要
   先分辨這一類，不是見減就報警。
2. **平衡不變式守得住**：門檻是比例制，紅了就是性質變了——要放寬門檻，先拿
   校準證據說話，不要改數字繞過。
3. **文件與程式碼對得上**：改了賽制或改了檔名，同一個 commit 要把 `ARCHITECTURE`
   的目錄樹、`ESPORT-DESIGN-V4` 的規則對應表、`WIKI` 的攻略一起帶到；版號動了
   `tests/kernel/version.mjs` 守的四份手抄本會逼你。
4. **辛組的額外約束**：§23.1 的四件定案（一次到位、去姓名、Liquipedia 主源、
   LLM 只做語意離線快取）與 §24.1 的三件定案（背景模擬零 LLM、兩套百分位母體並存、
   Bo5 高壓檢定過乘法鏈）動工前不可推翻——庚組（S23–S30）已收尾，細部規格由
   S31 站定案後回寫 §24。

承重牆（第二節）要動的話，先開下一份快照，不要就地改。
