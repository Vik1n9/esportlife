# S25 · 資料清洗

狀態：未開始
前置：S24
預估：1 session
推理難度：中
建議模型：Sonnet 5（`claude-sonnet-5`）
理由：規則解析為主。LLM 只用於模糊比對且批次打包、結果落字典快取——極低 Token。
判斷點在實體對齊的別名映射，S23 的 `team_history.json` 是標準答案。

---

## 為什麼做這件事

raw_data 是 Wikitext／HTML，不能直接進參數生成。本站把非結構化文本轉成
`cleaned_players.json`：標準化基礎資料、生涯履歷、粗略賽事統計。S26（百分位母體）
與 S27（參數生成）都讀它，是資料線的分水嶺。

---

## 入口狀態

```bash
node docs/v4/next-station.mjs
```

回報本站為下一站。S24 的 `raw_data/`、`team_history.json`、`target_players.csv`
齊備，交接筆記有探勘結論。

---

## 範圍

### 要做

**1. 基礎欄位解析（規則制）**

- 解析 Infobox 與 Tournament Results 表格：出生年（不抓真實姓名，依 §23.1）、
  位置、所屬戰隊歷史、賽事名次。
- 依 S23 定案的 NPC schema 欄位對齊（schema 是權威，欄位對不上就回報）。

**2. 實體解析與標準化**

- 戰隊別名統一映射到 `team_history.json` 的 `team_id`（如 TPA／台北暗殺星／
  Taipei Assassins → 同一縮寫）。
- 模糊描述打包成批次（每次 10 筆）呼叫 LLM 輸出 `team_id` JSON 陣列；
  **映射結果寫入本地字典檔，後續查表不再呼叫 LLM**。
- 字典檔進 repo（S27 也用它）。

**3. 產出 `cleaned_players.json`**

- 每名選手一筆：標準化基礎資料、生涯履歷、Wiki 有提供的粗略賽事統計。
- 去重：同名 ID 跨年度合併成單筆生涯。

### 不要做

- **不做遊戲參數計算**（潛力、屬性、特質）——那是 S27
- 不抓真實姓名（§23.1）
- 清洗規則與 S23 schema 衝突時：**照 schema 改腳本**，不回頭改 schema
- 不碰 `src/` 與 `tests/`

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `tools/npc-clean/`（或 S23 定案位置） | 解析與清洗腳本 |
| `tools/npc-clean/team_alias.json` | 別名→team_id 字典（LLM 快取產物） |
| `tools/npc-clean/cleaned_players.json` | 標準化資料集 |

---

## 規則與不變式

- 同一段模糊描述查表命中即不呼叫 LLM（快取先於推理）
- 每筆 `player_id` 唯一；`team_id` 必須存在於 `team_history.json`
- 純工具站：`npm test` 前後完全不變

---

## 完成定義

- `cleaned_players.json` 覆蓋 `target_players.csv` 全部可解析選手；不可解析者
  有明確標記與原因（寫進交接筆記）
- 別名字典覆蓋率與 LLM 呼叫次數寫進交接筆記（快取效果可量測）
- 抽樣人工核對 10 筆（S23 定案時指定的抽樣規則）：欄位與史實一致

---

## 交接筆記

<!-- 做完由執行者回填：覆蓋率、LLM 呼叫量、不可解析清單、抽樣核對結果 -->
