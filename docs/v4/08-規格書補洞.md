# S08 · 規格書補洞

狀態：✅ 完成
前置：—（隨時可做，但**丙組全部依賴它**）
預估：1 session
推理難度：**高**
建議模型：Opus 5（`claude-opus-5`）
理由：這一站要定的是配方門檻、起始屬性值、明星效應的函數形式、聯賽 par 重校——
全部是「跑完 160 段生涯才知道對不對」的數值。這些數字被丙丁戊組直接引用，寫錯了
後面每一站都在錯的基礎上調。降級的風險是填出一堆看起來合理、實際上沒算過的數字。

---

## 為什麼做這件事

`ESPORT-DESIGN-V4.md` 有九個地方寫不下去或寫錯了。丙組（S09–S13）每一站都要引用
它的數值，所以要先補齊。

這一站**只改文件，不碰程式**。

---

## 入口狀態

```bash
head -20 ESPORT-DESIGN-V4.md
```

前 16 行是 LLM 殘骸（`<tool_call>` / `<function=create_file>` / `<parameter=path>`
＋對話開場白），正文從第 17 行的 `# 電競人生：LoL 職業選手生涯模擬` 才開始。

---

## 範圍

### 要做

逐項補完下面九個洞。每一項都要在規格書裡留下**可直接引用的數值或公式**，
不能只寫「待 playtest」——那等於把問題丟給丙組。

**洞 1 — 刪掉檔頭殘骸**

刪 `ESPORT-DESIGN-V4.md` 第 1–16 行。同時檔尾也檢查一下有沒有未閉合的
`</parameter>` 之類殘留。

**洞 2 — §14.2 配方表是空的**

現在只寫「配方需要重製」。S19c 全部卡在這。要產出的是完整的四階配方表：
通用 → 稀有 → 史詩 → 傳說。

⚠ **必讀** `docs/superpowers/specs/2026-08-12-four-tier-trait-synthesis.md`。那份記錄
了一個踩過的坑：第一版做成嚴格階梯（2 稀有 → 1 史詩）之後，史詩持有率從 71/80 掉到
10/80——因為稀有配方跟史詩配方**搶同一批稀缺的通用素材**。修正是把素材分兩池
（稀有吃「人格／媒體」類、史詩吃「競技表現」類，兩池不重疊）。新配方表要沿用這個
分池原則，否則會重蹈覆轍。

**洞 3 — §7 沒給職業第一年的起始屬性值**

現行 `src/engine/state.js` 是 1–80 刻度的 `birth.int(22, 34)`，加上該路權重最高的兩個
屬性 `+int(0,4)`。0–100 刻度＋§7.1 的潛力區間（平庸 58–78 到頂尖 90–100）之下，
V4 §19 的職業第一年新秀該從幾開始，要定。

考慮：DEMO 跳過業餘期，所以起始值代表的是「已經打進職業隊的新人」，不是 16 歲網咖
玩家。這個值直接決定第一年的成長空間與 §17.2 聯賽門檻的相對位置。

**洞 4 — §16.1 MSI 規則比程式退步**

規格書寫「春季季後賽冠軍取得資格」。程式 `src/data/formats/msi.js` 已經寫進：
2015 創辦、2020 COVID 停辦、2023 起主流賽區出兩隊（亞軍也去得了）、
「MSI 接在第幾個賽段之後」由賽區決定而非全球規則。

**以程式為準，改規格書。** 這一項是把已實作的正確史實回寫進文件。

**洞 5 — §18.1 與 §7.2 打架**

§18.1 寫「超過 24 歲後身體上的六圍衰減」，§7.2 寫「awr/dec 經驗型可續升（老將靠意識
吃飯）」。六圍全衰 vs 兩圍續升，二選一或寫清楚分界。

現行 `src/data/attributes.js` 的做法是 `DECLINE_ATTRS = ['agi','vit','tec']` 與
`AGING_GAIN_ATTRS = ['awr','dec']`（syn 兩邊都不列）。這個設計有理由，寫進規格書時
說明白。

**洞 6 — §9 心理層的市場面沒交代**

V4 §9 用競技心理六維取代舊的 nerve/chem/ego/fame/rep，但沒說薪資談判、轉會、戰隊
切割改讀什麼。**已經定案**（見 `docs/v4/README.md` 的「已定案的設計決定」）：

- 薪資／試訓／續約／FA 讀 §10.2 教練評價
- `fame`（聲量）保留為獨立層
- `chem` 併入 `trust`
- `ego` 不留維度，改由特質副作用承擔放大器角色
- `rep` 不留維度，切割判定改讀教練評價＋特質副作用

把這段回寫進 §9 與 §10。

**洞 7 — §11.1 明星效應只有一句話，＋ 對手強度要隨賽事等級拉高**

現在寫「玩家戰力遠超聯盟基準時給超線性加成」，沒有函數形式。要定：門檻從哪裡起算、
指數多少、上界在哪。

參考起點：`max(0, P − par)^1.35` 形式的加權項。但**要驗算**——這一項直接決定「強玩家
在弱隊能不能 carry」，太強會讓 w_p=0.60 的設計失效（玩家一個人打贏整隊），太弱則
§11.2 的「院長」特質沒有存在空間。

**同時要補的是另一半：對手強度隨賽事等級階梯式拉高。**

參考機制來自賽馬娘——後期賽事把門檻拉高，前期偷懶的配點在那裡才現形。常規賽門檻低，
練得糊也打得過；世界賽與 MSI 的對手是全聯盟頂端，位置戰力差幾點就會被吃掉，
甚至會被針對到打不過某些隊伍。

這件事**現在是勝率公式的副產品，不是被設計出來的機制**。S01 量到的實證：老手打法的
國際賽冠軍當量穩定是新手的 1.69～1.92 倍，而同樣兩批人的生涯評分中位數幾乎沒有差別
（三組種子裡有兩組是新手還高一點）。也就是說**篩選只發生在頂端**，這正是想要的，
但它目前是意外而非設計。

要定的：常規賽／季後賽／MSI／世界賽／生死戰各自的對手強度基準怎麼階梯，以及
「被針對」是否要做成獨立機制（例如對手會針對玩家最弱的技能）。S01 的新不變式
（`tests/regression/smoke.mjs` 的「加點在頂端才兌現」）就是在守這件事，
定案後那條門檻可以往上調。

**洞 8 — §10.2 教練評價的重校數值**

§10.2 給了公式但沒給數值。要重校的有：`LEAGUES[].par/min/g`、薪資曲線、
`mates[].ovr` 的生成區間、`opponentOvr`。§17.2 有一份「×1.25 等比草案」但自己註明
「正式需依新評估體系重調」。

⚠ 等比放大**不夠**——0–100 刻度下的成長曲線形狀跟 1–80 不同（§5.3 的潛力衰減取代了
凸的成本階梯），所以同一個相對位置對應的絕對值會偏移。要算，不要照抄 ×1.25。

**洞 9 — README 授權敘述過期**

§0.1 寫「未獲原作者授權」與現況一致，不必改。但 `README.md` 的「授權與出處」節仍寫
「等待作者回覆」。**這一項不在這站做**——記進 S06 的待辦即可（S06 已經有了，
確認一下就好）。

### 不要做

- **不要改任何 `src/` 或 `tests/` 檔案。** 這一站純文件
- **不要改 §16.1 對應的程式。** 洞 4 是改規格書遷就程式，方向不要搞反
- **不要把 §0.1 的授權敘述改掉**（它是對的）
- **不要新增 V4 沒有的系統。** 補洞是把寫不完整的地方寫完，不是加設計

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `ESPORT-DESIGN-V4.md` | 刪檔頭殘骸 ＋ 補八個洞（洞 9 歸 S06） |

只有這一個。

---

## 規則與不變式

- `npm test` 前後完全不變（沒碰程式）
- 每個補上的數值都要寫**推導或依據**，不能是憑空的數字。丙組的人會問「這個 62 是
  怎麼來的」
- 配方表要標明素材屬哪一池（人格／媒體 vs 競技表現），並在文件裡說明分池的理由
  （引用那份 spec 的實測數據）
- 補完之後 §21「待定與 Playtest 項目」要相應縮短——還留在那節的項目表示**刻意**
  留給 playtest，不是忘了寫

---

## 完成定義

```bash
head -5 ESPORT-DESIGN-V4.md
```

第一行是 `# 電競人生：LoL 職業選手生涯模擬 — 規格企劃書 v4.0（Rogue-like 重構版）`。

```bash
grep -n "待重定義\|配方需要重製\|待調\|TBD" ESPORT-DESIGN-V4.md
```

只剩下 §21 裡刻意保留的項目，八個洞對應的位置都不再出現。

```bash
npm test
```

全綠，項數與 S01 結束時相同。

人工檢查：拿 §14.2 的配方表，隨機挑三條配方，確認素材分池正確且沒有跨池搶素材。

---

## 交接筆記

只動了 `ESPORT-DESIGN-V4.md` 一個檔（＋本站說明書、`docs/v4/README.md` 狀態表、
`WORKLOG.md`）。`npm test` 前後都是 **10437 項全綠**，項數不變。

### 八個洞的落點（丙組要引用的數值都在規格書裡，這裡只寫「在哪一節」與「怎麼算的」）

| 洞 | 落在哪一節 | 關鍵結論 |
| --- | --- | --- |
| 1 檔頭殘骸 | — | 刪 1–16 行；檔尾乾淨，無未閉合標籤 |
| 2 配方表 | **§14.2** | 四階配方表全開：8 稀有 ＋ 10 史詩 ＋ **20 傳說**（史詩＋稀有＋生涯門檻） |
| 3 起始屬性 | **§7.3** | 起始值 ＝ 潛力 ×（主屬性 0.80／其餘 0.70）±0.03 |
| 4 MSI | **§16.1** | 以 `data/formats/msi.js` 為準改寫規格書 |
| 5 §18.1 vs §7.2 | **§7.2 ＋ §18.1** | 三衰（agi/vit/tec）、兩升（awr/dec）、一不動（syn）；30 歲起算 |
| 6 心理的市場面 | **§9.4 ＋ §10.2** | 順手把 S12 要的「十二技能 ↔ 六維」映射表補在 §9.2 |
| 7 明星效應 ＋ 對手階梯 | **§11.1** | `min(6.0, 0.06 × (P−par)^1.35)`；對手強度階梯表；「被針對」驗算後否決 |
| 8 重校數值 | **§17.2** | par/min 照 ×1.25 是對的；真正的坑是 per-point 係數要**除以** 1.25 |
| 9 README | — | S06 已經做完了（README 不再寫「等待作者回覆」），確認過，本站不動 |

### 三個要先知道的結論（會改變你對這批數字的預期）

**（一）`×1.25 等比草案`的 par／min 那一欄是對的，錯的是它沒寫到的東西。**

說明書警告「等比放大不夠，要算不要照抄」。算了：把舊成本階梯與新潛力衰減各自跑同一條
抽象生涯、各自把訓練預算調到「平均巔峰 ÷ 上限 = 0.755」之後，巔峰分位數 ÷上限 兩邊
對得上到小數第三位（p2 .707/.709 … p98 .830/.823）。**刻度換算不移動相對位置**，所以
落在分布同一個位置的門檻，絕對值就是等比放大。

真正會錯的是三類草案完全沒提的東西，全部寫進 §17.2：

1. **per-point 係數要除以 1.25**（勝率斜率 2.2→1.76、薪資 delta 0.05→0.04、
   數據生成一整排係數…）。刻度放大之後同一個相對差距會產出 1.25 倍的點數，
   係數不除就等於全部靈敏度上調兩成。
2. **`g`（單季場次）根本不該縮放**。草案把 36/48/60/66/70 乘成 45/60/75/82/88——
   場次是賽制事實，不會因為屬性改成 100 分制就多打 15 場。底薪同理。
3. **`delta` 是「高於所在聯賽 par 幾點」，不是「今年成長幾點」**（`delta = 評價 − par`）。
   我一開始也算錯方向。它是水準量，所以門檻要跟著 ×1.25（`delta ≥ 1` → `≥ 1.25`）。

**（二）⚠ 潛力衰減會吃掉約三成的「集中加點獎勵」——S09 的微基準餘裕會變薄。**

同一條抽象生涯上，集中 vs 平均的領先幅度：舊凸成本階梯 0.0236×上限，新潛力衰減
**0.0162×上限（−31%）**。原因是天花板係數會懲罰把單一屬性推到頂，而舊制只在超過潛力
之後才 ×3。現行遊戲的實測領先是 0.0464×上限（老手巔峰 62.26 對新手 58.55），照三成
折下來約 0.032，而 S07 微基準的門檻是 **0.0375×上限**（現行實測 0.0584）。

**紅了的修法寫在 §7.1：把天花板係數的底從 0.34 調到 0.5（實測回收約 15% 的差距），
不是去放寬 S07 的門檻。**

**（三）配方表有一半是死的，而且不是配方的錯，是素材拿不到。**

實測 160 段，五個通用特質實質上拿不到：**鐵人 0%（完全沒有來源）**、休息室黏著劑 0%、
全民偶像 0%（條件讀已廢除的 `rep`）、逆風翻盤 1.9%、神主牌 6.9%。連帶 4 條稀有配方
與 3 條史詩配方是死的（定海神針／元老／傳奇偶像／諧星、金剛不壞／奇蹟劇本／話題製造機
全部 0%）。

§14.2 因此多了兩條衛生規則：**死配方規則**（任一素材取得率 <15% 即視為死配方）與
**同池撞素材時取稀缺度乘積最低的那條**（諧星的兩個素材都很常見卻 0%，因為表的行序讓
明星選手／流量密碼先把素材吃掉了——那個取捨該由稀缺度決定，不該由行序決定）。

**這五個素材是 S18 的硬需求，不是「有空再說」**，已經寫進 §21.1。

### 給各站的直接指示

| 站 | 要看哪裡 |
| --- | --- |
| **S09** | §7.1 兩張係數表（級距 ×  天花板）、§7.3 起始值、§17.2 全表。**特別是上面第（二）點。** |
| **S10** | §8.1／§8.2 沒動。§9.2 的心理映射表用的是 §8.1 的十二個技能名，改名要同步 |
| **S11** | §10.2 心理修正項的公式與 ±3 上界、§11.1 明星效應三個參數、§17.2 全表 |
| **S12** | §9.2 的十二技能 ↔ 六維映射表（你要的那張，已經補上）、§9.4 的市場面對映、§10.2 心理修正只吃 drive/disc/comp |
| **S13** | §6.1／§6.2 的區間沒動，數值刻意留給你 playtest（§21.2） |
| **S18** | §21.1 點名的五個素材要補取得途徑。**這是 S19c 能不能成立的前提** |
| **S19b** | §14.1 定了獨有特質的規則（不可合成、不能當素材，來源只有生涯條件），目錄留給你；§14.2 的 20 條傳說配方已按原型排好，你只要命名並補副作用，**現有 6 條配方原樣保留** |
| **S19c** | §14.2 整節。傳說持有率目標 **28–35%**（現行 41.3%，要降）；20 條配方的素材配額與生涯門檻選單都附了實測命中率 |

### 刻意做的、超出九個洞的三件事

1. **§16.3 冠軍積分**改寫成已實作的史實制度（單一積分表 90/70/45/20/5，不是原本的
   春夏兩套）。理由：完成定義的 `grep 待調` 要求它不能留著，而「以程式為準」的處理
   方式與洞 4 同一套。
2. **§0.1 加了一段現況補記**（承襲程式碼已在 S03–S05 淨室重寫完成）。說明書寫「不要把
   §0.1 的授權敘述改掉」，所以原本那三條**一字未動**，只在後面加註。這是回答 S06 交接
   筆記裡明確留給 S08 的問題（「§0.1 是否要隨 README 一起調整」）。**不同意的話刪掉那
   一段引用塊就好，不影響其他節。**
3. **§21 開頭加了一張「原本留在這裡、現在定在哪」的對照表**，並把已定案的項目移出。
   還留在 §21 的都是刻意留給 playtest 的。

### 驗算腳本

都是丟棄式的（跑在 scratchpad，沒有進 repo），要重跑的話重點是四支：

| 驗算 | 做法 |
| --- | --- |
| 刻度換算不移動相對位置 | 兩個成長模型跑同一條抽象生涯，各自把預算二分搜尋到平均巔峰÷上限 = 0.755，比分位數 |
| 集中 vs 平均的差距 | 同上，但同時跑 focus／spread 兩種政策，比 (焦點−平均)÷上限 |
| 起始值 0.80／0.70 | 生成 600 段，量位置加權起始評價與第一年成長 |
| 素材稀缺度與門檻選單 | `playMatrix` 跑 160 段，數每個特質的 `state.traits`／`fusedAway` 命中，再與各候選門檻取聯合命中率 |
