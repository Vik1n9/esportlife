# S28 · 引擎整合：隊友與戰隊

狀態：完成
前置：S23 S27
預估：1 session
推理難度：中
建議模型：Sonnet 5（`claude-sonnet-5`）
理由：抽選規則 S23 已定案，本站是接線。要小心的不是設計，是不動到
`rollRoster` 現有的校準依賴（隊友 rating 進 §11.1 團隊強度）。

---

## 為什麼做這件事

現在隊友是 `MATE_NAMES` 12 個 ID 隨機抽 4 人、rating 隨機配（`src/engine/roster.js:49`），
戰隊顯示名來自 `data/regions/*`。S27 的 `npc_roster.json` 是史實母體——本站把
隊友抽選、戰隊縮寫與選手面板接上它。

---

## 入口狀態

```bash
node docs/v4/next-station.mjs
```

回報本站（與 S29 並行可解鎖，各自讀 `npc_roster.json`）。S23 已定案隊友池
抽選規則（年代過濾、位置、與 `rollRoster` 的取代／共存關係、`MATE_NAMES` 去留）。

---

## 範圍

### 要做

**1. 隊友抽選接 NPC 池**

- 依 S23 定案：抽選規則（年代／位置／賽區過濾）、rating 來源（NPC 屬性導出
  或照舊隨機配）、`MATE_NAMES` 去留。
- 保留 `rollRoster` 的呼叫端契約——`market.js`、`phases/*` 的呼叫不破。
- 隊友存 NPC 引用（如 `mate: { id, name, rating }` → `{ npcId, ... }`，依 S23
  schema）時，`SAVE_VERSION` 要不要 bump 依 S23 定案。

**2. 戰隊縮寫顯示**

- `team_history.json` 的縮寫接進戰隊顯示鏈（戰報、選手面板、主畫面狀態帶
  §22.1 的「賽區・隊伍名・戰績排名」）。

**3. 選手面板**

- §22.3 選手 tab 的隊友資訊若讀 NPC 資料（如隊友的能力概況），依 S23 定案
  決定揭露範圍——**隱藏心理與教練評價的揭露禁令（§22.4）對 NPC 同樣適用**。

### 不要做

- 不動 `kernel/strength.js` 與任何對手強度模型（S29）
- 不做 160 段校準重驗（S30）——但若隊友 rating 分佈因抽選規則改變，
  在交接筆記記下觀測值，供 S30 參考
- 不推翻 S23 的抽選規則定案

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `src/engine/roster.js` | 隊友抽選接 NPC 池 |
| `src/data/teams.js` | `MATE_NAMES` 去留（依 S23 定案） |
| 戰隊顯示鏈（`src/phases/*`、面板） | 縮寫顯示 |
| `tests/` | 抽選規則測試、既有測試同步 |

---

## 規則與不變式

- `npm test` 全綠；`history/` 分區不動
- 隊友 ID 一律來自 `npc_roster.json`（或 S23 定案的混合規則）
- §22.4 揭露禁令不因 NPC 資料破例

---

## 完成定義

- 隊友抽選跑 NPC 池、戰隊縮寫全鏈顯示正確
- `npm test` 全綠（既有 160 段 smoke 不改種子跑得通；若基線偏移，記錄數字交 S30）
- 選手面板顯示依 S23 定案

---

## 交接筆記

**2026-08-18 完成。** 版號 v4.7.2 → **v4.7.3**（Z 級，使用者拍板；規格書只加
§23.6 落地註記）；`SAVE_VERSION` 24 → **25**（`state.mates` 擴 `npcId/position`，
S23 定案「SAVE_VERSION 歸 S28 bump」）。`npm test` **22817 項全綠**（新增
`tests/phases/roster.mjs` 27 項）。

### 做了什麼

1. **隊友抽選接 NPC 池（`src/engine/roster.js`）**：`rollRoster` 依 stage 分流——
   PRO 走 `pickProMates`（位置以外的四位置各一；年代 `active_years` 覆蓋當季年份
   ×賽區 `teamRegionOf` × `peak/birth_year` 非缺；rating＝`npcRatingInYear`
   （`peak.rating × ceilingCurve(lifecycle, 年齡)`，§23.3），`par±7` 窗口篩選、
   依序放寬 ±10 → ±14，全空落**合成隊友**（`npcId: null`、虛構名池取名、
   `rng.int(par−7, par+7)`——與舊 rollRoster 分布同構）→ **永不空手**。
   業餘期（AMATEUR／AM2）par±7 機制原樣＋虛構名池。信任重置／教練／mateMorale
   邏輯原樣保留（S23.6 明令不動）。
2. **`MATE_NAMES` 退役（`src/data/teams.js`）**：十二名真實選手 ID 進 NPC 池當
   `player_id`。業餘期與合成隊友改吃新建的 `src/data/mateNames.js` 虛構名池
   （24 個非史實綽號）。
3. **`src/data/npc/teamHistory.js`（新）**：由生成器 `tools/npc/gen-team-history-esm.mjs`
   從 `team_history.json` 產（277 隊，標頭 CC BY-SA），`teamRegionOf(teamId)` 給
   賽區過濾（HOME 玩家吃 GPL/LMS/PCS/LCP 四代，海外吃 LCK/LPL/LEC/LCS 同值）、
   `teamDisplayName(teamId)` 給縮寫顯示。**S29 可直接用**（對手隊顯示與賽區判定）。
4. **選手面板隊友卡（`ui/panel/team.js`）**：職業期隊友顯示「名字（隊縮寫）·位置
   rating」（`mateTeamId`＋`teamDisplayName`＋`ROLE_NAMES`）；§22.4 對 NPC 同適用
   ——心理六維與教練評價不顯示，rating 是 S23.6 schema 明列的可見欄位。
5. **ARCHITECTURE.md**：目錄樹補 `data/npc/` 與 `mateNames.js`、統計刷 94 檔／
   15293 行（S27 時未刷，順手補）。

### 基線偏移（S28 完成定義要求記錄，交 S30）

160 段（AMATEUR 起點，16 seed × 5 路 × 2 打法）實測，隊友 rating 來源從均勻
`par±7` 換成 NPC 池（窗口內篩選）後：

| 指標 | 00b 快照（S21b） | S28 後 | 方向 |
|---|---|---|---|
| 平均巔峰教練評價 | 72.84 | **74.16** | +1.32 |
| 頂端落差（有冠 vs 無冠） | 0.162 | **0.172**（17/143 段） | +0.010，餘裕 0.092 |
| 世界賽冠軍人均 | 0.035 | **0.056** | +0.021 |
| 平均職業年資（proYears） | 12.4 | **11.4** | −1.0 |
| 平均退役年齡 | 32.3 | **31.4** | −0.9 |
| 傳說特質持有率 | 23.1% | **20.6%** | −2.5pp |
| 傳奇率 | 老 15%／新 2.5% | 老 15.0%／新 2.5% | 不變 |

「隊友變強」→ 平均巔峰與國際賽兌現率上升；職業年資反而降（更高壓的環境早退役？）。
**S30 要拿這張表當對照**。所有平衡不變式 `npm test` 全綠（頂端落差 0.172 > 0.08 門檻）。
⚠ 別急著推常數——S23.6 校準護欄：偏了在 S27 映射層調，不動 `matesAverage`。

DEMO 100 段（PRO 起點）：第一年存活、期滿 ≥ 60 等門檻全綠（`npm test` 無紅燈）。

### 縮寫顯示的取捨（規格 §23.3「regions 無對應隊才顯示縮寫」）

玩家所屬隊名來自 `data/regions/*`（中文，如「閃電狼」），永遠有對應 → 狀態帶與
選手面板維持在地名，沒加縮寫。縮寫的落點是**隊友卡**（NPC 有 `team_id`）。規格
「縮寫顯示」在 S28 的完整意義＝建立 `teamHistory.js` 查詢層＋隊友卡接上；S29
對手實體化時隊名顯示直接吃同一層。戰報卡（phases 敘事）維持中文隊名——敘事文本
的縮寫屬顯示風格，非資料層職責。

### 未一起處理

- **出生流重排（§1.4 對齊）**：00b 已知缺口點名「S28 隊友與戰隊整合會重排出生流
  接入 NPC 池時一併對齊」——但 S28 隊友抽選完全不碰出生流（`rollRoster` 走人生流
  `rng`），重排只會位移全部既有種子天賦、讓 S30 的基線對照更混亂，**刻意沒做**。
  `state.js:110-113` TODO 保留。
- **戰報卡縮寫**：如上，留給敘事風格決策，不在本站。
- **OCR 審查閘門**：本環境 `ocr` LLM 未設定，比照 S25/S26/S27 往例記入交接筆記，
  下次有環境時補審（`node scripts/station-review.mjs --station 28`）。
- 00b 快照的基線數字沒動（S30 重校後才回寫）。

### OCR 委託審查補記（3 條，皆已修）

1. `src/data/mateNames.js`：Aegis／Relic 撞 `NPC_ROSTER` 的 `player_id`（身分漂移，
   S23.6 明言虛構名不能再兼真實選手）→ 換 Basalt／Runic，並加「虛構名池不與任何
   NPC player_id 重疊」的檢查守住（medium）。
2. `src/ui/panel/team.js`：未使用的 `teamDisplayName` import → 移除（low）。
3. `package.json`：結尾缺換行 → 補回（low）。

**狀態**：完成。`node docs/v4/next-station.mjs` 回報 S29 為下一站（55／63 完成）。
