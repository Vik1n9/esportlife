# S17a · 生涯軌跡帳本

狀態：✅ 完成（S17a，2026-08-15）
前置：S12
預估：1 session
推理難度：中
建議模型：Sonnet 5（`claude-sonnet-5`）
理由：判斷成本中等——欄位要記什麼是被 V4 §14.3 與 §15.5 直接點名的，答案唯一。難的
不是設計而是**掛載點要找對**：同一件事（例如「進了世界賽四強」）在 `phases/worlds.js`
與 `phases/msi.js` 各有一條路徑，漏掉一條帳本就少記一半。

> ⚠ **v4.3 增訂**（§20.2）：state 新增 `lifecycle`／`activeEffects`／`traitGraph` 三欄
> ——那是 S15b／S16／S19a 的產出，不是本帳本。本帳本的記帳欄位（§14.3 五格）不變，
> 但「退役／解散／降級」的記帳掛載點要跟著 S15b 的市場淘汰走——退役不再是
> `game.js` 的年度硬上限檢查，而是 `transfer.js` 的無報價→退役路徑。

---

## 為什麼做這件事

V4.1 增訂了兩個系統，它們要的資料是同一批：

- **§14.3 替代性生涯路線**：五條 `route` 路線的觸發條件裡，有四格 `state` 現在沒有。
- **§15.5 生涯傳記生成**：拼接文本的事實來源。

現況的 `state` 只記得了「總數」，記不得「過程」：

| 要什麼 | 現在有什麼 | 差在哪 |
| --- | --- | --- |
| 國際賽勝率 | `intlAppearances`、`worldsWins`、`msiPodiums` | 只有**出場次數與名次**，沒有勝負場 |
| 歷任戰隊 | `team`（現在這支）、`teamYears` | 換過哪幾支查不到 |
| 降級危機次數 | `disbandThreat`（布林） | **每季重置**，累計不下來 |
| 個人獎項數 | `honors[]`（字串陣列） | 要靠 `includes('MVP')` 撈，脆弱 |
| 關鍵事件 | `recentEvents`（暫存） | 只留最近幾張，不是生涯紀錄 |

所以這一站先把帳本記起來，**S17b（生涯任務引擎）與 S21a（生涯傳記）才有東西可讀**。
它是那兩站的共同前置，單獨做的價值就在這裡——兩站都不必自己去改 `phases/`。

---

## 入口狀態

```bash
npm test
```

全綠，S12 已完成。

```bash
grep -rn "intlAppearances\|disbandThreat\|honors.push\|teamYears" src/
```

會指到 `phases/msi.js`、`phases/worlds.js`、`phases/split.js`、`phases/transfer.js`、
`phases/seasonEnd.js`、`engine/market.js`、`engine/game.js`。**掛載點就是這幾處**。

---

## 範圍

### 要做

**必讀規格**：V4 §14.3（五條路線各讀哪一格）、§15.5（傳記的資料來源限定）、
§20.2（state 結構）。

**1. `src/engine/state.js` 新增帳本欄位**

```js
// 生涯軌跡帳本（V4 §14.3／§15.5）。與既有的總數欄位分開：
// 總數回答「拿了幾個」，帳本回答「怎麼走到這裡的」
intlRecord: { W: 0, L: 0 },   // 國際賽系列賽的勝負場（MSI ＋ 世界賽）
teamHistory: [],              // [{team, league, fromYear, toYear, firstSeasonRating, teamAvgRating}]
disbandCrises: 0,             // 生涯累計的降級危機／重建期次數
awards: 0,                    // 個人獎項數（MVP／最佳新人／單殺王／全明星）
milestones: [],               // [{year, kind, text}] 給 §15.5 拼接用的事實流
```

`SAVE_VERSION` 13 → **14**。

**2. 在既有掛載點記帳**

| 檔案 | 記什麼 |
| --- | --- |
| `phases/msi.js`、`phases/worlds.js` | `intlRecord` 的勝負場；晉級／淘汰寫進 `milestones` |
| `engine/market.js`、`phases/transfer.js` | 換隊時關掉上一筆 `teamHistory` 並開新的一筆；被開除／解散寫進 `milestones` |
| `engine/game.js` | `disbandThreat` 由 false→true 的那一次 `disbandCrises += 1`（**不是每季都加**） |
| `phases/seasonEnd.js`、`phases/split.js` | `awards += 1` 與現有的 `honors.push` 同一個地方，不另外撈字串 |

**3. `teamHistory` 的首季評價**

「流浪傭兵」要「每次轉會後首季教練評價 > 隊伍平均」。轉會後第一個賽季結束時，把
該季的教練評價與隊伍平均一起寫進當筆 `teamHistory`。隊伍平均讀 `state.mates`——
`engine/mental.js` 已經有隊友強度，不要新造一套。

**4. 純讀取的查詢函式**

`src/engine/ledger.js`（新檔）：`intlWinRate(state)`、`assistsPerGame(state)`、
`distinctTeams(state)`、`longestTenure(state)`。純函式，吃 state 回傳數字。
S17b 與 S21a 都讀它，**條件判斷不准直接翻 `state` 的原始欄位**——那樣改一次欄位要
改三個地方。

### 不要做

- **不要做任務卡的觸發判斷**（S17b）。這一站只記帳，不判斷
- **不要做傳記文本**（S21a）
- **不要動既有的總數欄位**（`intlAppearances`、`worldsWins`……）。帳本是**加上去**的，
  不是取代——`engine/career.js` 的評分還在讀那些
- **不要用字串比對重算獎項**。`awards` 在 `honors.push` 的同一行加，不要事後 `includes`
- 不要碰 `positionPower` 的任何乘法項（S12 交接筆記第一節）

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `src/engine/state.js` | 五個新欄位 ＋ `SAVE_VERSION` → 14 |
| `src/engine/ledger.js` | **新檔**，純查詢函式 |
| `src/phases/msi.js`、`worlds.js` | 記 `intlRecord` 與 milestone |
| `src/phases/transfer.js`、`src/engine/market.js` | 記 `teamHistory` |
| `src/phases/seasonEnd.js`、`split.js` | 記 `awards` |
| `src/engine/game.js` | 記 `disbandCrises` |
| `tests/kernel/ledger.mjs` | **新檔**，查詢函式的單元測試 |

---

## 規則與不變式

- S07 不變式全綠。這一站**只加欄位不改判定**，任何一條紅了就是記帳寫進了判定路徑
- `intlRecord.W + intlRecord.L` 應該與國際賽實際打的場數對得上（測試要驗）
- `teamHistory.length` 與 `firedTimes ＋ 主動轉會次數` 對得上
- `disbandCrises` 單季最多加 1

---

## 完成定義

```bash
npm test
```

全綠。

```bash
node -e "
import('./tests/lib/harness.mjs').then(async h => {
  /* 跑 40 段生涯，印帳本的分布 */
})"
```

交接筆記要列出：40 段生涯裡 `intlRecord` 的平均場數、`teamHistory` 長度分布、
`disbandCrises` 分布、`awards` 分布。**S19c 校準 §14.3 的絕對門檻時直接讀這幾個分布**
——「究極綠葉的場均助攻門檻」要定在哪，答案在這裡而不是拍腦袋。

---

## 交接筆記

<!-- 做完由執行者回填 -->

**狀態**：✅ 完成。`npm test` **15187 項全綠**（基線 14314 ＋ 新增
`tests/kernel/ledger.mjs` 873 項）；`SAVE_VERSION` 17 → **18**（舊存檔作廢）。

**實作對照說明書**（掛載點與說明書的兩處出入，後續站照實際程式讀）：

- `intlRecord` 記**局**勝負不是系列勝負：小組／Swiss 循環的 `wins/losses` 與每個
  BO5 系列的 `mine/theirs` 都累進（`phases/shared.js` 的 `recordIntlSeries`／
  `recordIntlGroup`）。§14.3「賽區統治者」的勝率語感是局勝率，且完成定義要求
  「W+L 與實際打的場數對得上」——記局才對得上。
- **`teamHistory` 統一掛在 `engine/market.js` 的 `signContract`**（所有換隊的唯一
  入口：買斷、FA、試訓、業餘轉職業全走它），不是散在 transfer.js。`changedTeam`
  時關上一筆（`toYear = state.year`）開新筆；**只在 `stage === 'PRO'` 記**——業餘
  ／青訓的簽約不算職業效力（§14.3 是職業路線）。續約（同隊）不開筆。
- 說明書寫的 `phases/split.js` 記 `awards` 是舊資訊——個人獎項（MVP／最佳新人／
  單殺王／全明星）全在 `seasonEnd.js` 的 `awards()`，`split.js` 只有賽段例行數據。
  4 個 `honors.push` 統一改走 `award(state, text)` helper：同處 `awards += 1`、
  記 `milestone {kind:'award'}`。**賽段冠軍／國際賽名次不算個人獎項**（那是團隊
  榮譽，「究極綠葉」要求獎項數 = 0 時團隊榮譽不該擋路）。
- `disbandCrises` 在 `game.js` 的 `yearOpen`，**`!state.disbandThreat` 才加**
  （false→true 那次），單季最多加 1，連兩年流言只算一次危機。
- 首季評價補寫在 `seasonEnd.js` PRO 結算：最後一筆 `toYear === null &&
  firstSeasonRating === null && fromYear !== state.year`（排除簽約當季）時補
  `effectiveCoachRating` 與 `mates` 平均。⚠ 復健年（skipSeason）的 seasonEnd 直接
  return，當筆首季評價會拖到下一季才補；若期間轉會，該筆 `firstSeasonRating` 留
  null——S17b 判「流浪傭兵」時把 null 當「不成立」即可，不必補救。
- `ledger.js` 四個查詢：`intlWinRate` 回 0–100 百分比（沒打過國際賽回 0）、
  `assistsPerGame` 跨分區 `ΣA ÷ ΣG`、`distinctTeams` 數不同隊名、`longestTenure`
  用 teamHistory（未完成筆算到今年）。S17b／S21a 一律讀這裡，不准翻原始欄位。

**40 段實測分布**（seed `ledger-0..39`、MID/TOP 交替、strategy first＋focus；
S19c 校準 §14.3 門檻直接讀這組，別拍腦袋）：

- `intlRecord`：5/40 段打過國際賽（國際賽門票是稀有事件，引擎現況如此），
  平均總局數 **16.0**、平均勝場 7.6 → 樣本勝率約 47%。
- `teamHistory` 長度分布：1 隊 15 段／2 隊 14 段／3 隊 10 段／5 隊 1 段
  （平均 2）。⚠ **40 段只有 1 段摸到「流浪傭兵」的 4 隊門檻**——S19c 定門檻時
  若維持 ≥4，實際達標率極低；要放寬或靠 S18 的事件卡拉轉會頻率。
- `disbandCrises`：0 次 19 段／1 次 17 段／2 次 4 段（平均 0.6）。「不滅隊魂」
  的 ≥2 次約 10% 達標，剛好貼心流檢驗的 60% 線之下，S19c 注意。
- `awards`：0 項 15 段／1 項 10 段／2 項 2 段／3 項 5 段／4+ 項 8 段（平均 1.9）。
  「究極綠葉」要獎項 = 0：37.5% 生涯符合，母體夠大。
- `milestones` 記帳量：move 38 筆、fired 6 筆、disband 25 筆／40 段。

**S17b 開工要帶走的**：

1. `intlWinRate` 低於 40% 的選手（賽區統治者）＝國際賽打得少或輸得多，和
   `splitTitles ≥ 3` 是互斥導向的組合——驗收時確認沒有同段同時中兩條。
2. `longestTenure` 用 teamHistory 算，跟既有 `teamYears`（現在這隊的連續年數）不同
   源——「不滅隊魂」讀 `teamHistory`（生涯最長），不是 `teamYears`。
3. 帳本只加欄位不動判定，S07 不變式原樣全綠；`tests/kernel/ledger.mjs` 的 40 段
   不變式（awards 一致性、teamHistory 結構、milestone 與記帳點對得上）是帳本的
   安全網，S17b 改判定的時候別把它弄紅。
