# S19c · 合成配方重製

狀態：未開始
前置：S19b
預估：1 session
推理難度：**高**
建議模型：Opus 5（`claude-opus-5`）
理由：配方門檻直接決定四階特質各自的持有率，也就直接決定傳奇稀有度。這個專案**已經
在這裡踩過一次坑**（史詩持有率從 71/80 掉到 10/80），而且錯誤要跑完 160 段才看得
出來。降級的風險是重蹈覆轍。

---

## 為什麼做這件事

V4 §14.2 只寫了「配方需要重製」，是個空的。

現行 24 條配方是為舊體系設計的（掛 OVR、沒有副作用、素材是舊的 26 通用特質）。
S19a 補了副作用、S19b 把傳說擴到 20——配方必須跟著重做，否則 14 個新傳說沒有任何
取得途徑。

V4 §14.1 的合成規則：

- 通用 → 稀有 → 史詩 → 傳說，逐階往上
- 取得特質後**自動檢查配方，命中即合成**
- 合成後**低一階的會被消耗掉**
- 被消耗的低階特質**不可再取**
- UI 不揭露配方，僅氛圍敘事，玩家靠 WIKI/社群探索

V4 §13.3 第三層約束：**跨配方共用元件**——低階特質是多條高階配方的共同材料，
合成消耗後不可再取 → 選一條就鎖掉其他。這是 build 分歧的來源。

---

## 入口狀態

```bash
npm test
```

全綠，S19b 已完成（傳說 20 個，全部有副作用）。

```bash
node -e "import('./src/data/epics.js').then(m=>console.log('現有配方',m.FUSIONS.length))"
```

印出 24。

**必讀**：`docs/superpowers/specs/2026-08-12-four-tier-trait-synthesis.md`
與 S08 在規格書 §14.2 補的配方表。

---

## 範圍

### 要做

**必讀規格**：V4 §14（特質合成全節）、§13.3（三層約束）。

**1. ⚠ 先讀踩過的坑**

`docs/superpowers/specs/2026-08-12-four-tier-trait-synthesis.md` 記錄：

> 第一版把「稀有 → 史詩」做成嚴格階梯（2 稀有 → 1 史詩、史詩再往上疊），結果 160 段
> 生涯的史詩持有率從基準 71/80 掉到 10/80、平均巔峰 OVR 從 60.5 掉到 57.5——因為稀有
> 配方跟史詩配方**搶同一批稀缺的通用素材**，稀有先檢核就把史詩的素材吃光了。
>
> 修正：稀有吃「人格／媒體」類通用，史詩吃「競技表現」類通用，**兩池不重疊**。
> 如此稀有與史詩能同時成立、互不搶素材；傳說＝史詩＋稀有，素材必然分屬兩池。

實測基準（160 段）：

| 指標 | 值 |
| --- | --- |
| 平均巔峰 OVR | 59.9 |
| 史詩持有率 | 63/80 |
| 稀有持有率 | 69/80 |
| 傳說持有率 | 23/80 |

**分池原則要沿用。** S08 在規格書 §14.2 定的配方表應該已經標明素材分池——照它做。
若沒標，停下來寫進交接筆記。

**2. 重寫 `FUSIONS`**

現有的資料結構已經支援跨階素材（`need: [[tier, key], ...]`，
見 `src/engine/progression.js` 的 `checkFusions`），**不必改架構**，只換配方內容。

要涵蓋：
- 通用 → 稀有（吃人格／媒體池）
- 通用 → 史詩（吃競技表現池）
- 史詩 ＋ 稀有 → 傳說（20 條，每個傳說一條）

**3. 跨配方共用元件（§13.3 第三層）**

刻意讓某些低階特質同時是多條配方的材料。玩家選了一條就鎖掉其他——這是 build 分歧
的機制來源。

⚠ 但**共用不能造成搶素材**（那是上面那個坑）。安全的做法：**池內共用，跨池不共用**。
同一池裡的配方可以搶同一個素材（那是取捨），但稀有池與史詩池之間絕不重疊。

**4. 平衡：20 個傳說不能人人拿到**

傳說從 6 個變 20 個，如果配方門檻不變，傳說持有率會暴增。門檻要跟著收緊，
把持有率壓回 23/80 附近（或 S08 定的目標值）。

**5. 「合成後低階不可再取」的驗證**

`state.fusedAway` 機制已經存在（`src/engine/progression.js`）。20 條新傳說配方
上線後要確認它仍然成立——被消耗的特質不會被事件卡重新解鎖。

**6. UI 不揭露配方（§14.1）**

確認 `src/ui/panel.js`／`src/ui/summary.js` 沒有任何地方列出配方。
合成發生時只給氛圍敘事（`fusionBeats` 已經是這樣做的）。

### 不要做

- **不要新增特質**（S19a/S19b 已定案）
- **不要改 `checkFusions` 的架構。** 跨階素材的資料結構已經對了
- **不要在 UI 揭露配方**
- **不要讓稀有池與史詩池的素材重疊**
- **不要為了讓某條配方好合成而放寬 S07 的傳奇稀有度門檻**

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `src/data/epics.js` | `FUSIONS` 重寫 |
| `src/data/traits.js` | 素材分池的標記（若需要） |
| `tests/kernel/traits.mjs` | 分池不重疊、共用元件、消耗後不可再取的檢查 |
| `ESPORT-DESIGN-V4.md` | §14.2 回寫最終配方表 |

---

## 規則與不變式

S07 不變式全綠。這一站是**整條線最容易破壞平衡的地方**，四項都要盯：

- 老手打法：傳奇不得超過三成
- 新手打法：傳奇要罕見（不到老手的三分之一）
- 老手平均巔峰至少高 1.5
- 五個等第都出現得到

加上四階持有率（跟那份 spec 的基準比，允許偏移但要說得出理由）：

| 指標 | 基準 |
| --- | --- |
| 稀有持有率 | 69/80 |
| 史詩持有率 | 63/80 |
| 傳說持有率 | 23/80 |

**分池不重疊**要做成測試：掃過所有配方，稀有配方的素材集合與史詩配方的素材集合
交集為空。

---

## 完成定義

```bash
npm test
```

全綠。

```bash
node -e "import('./src/data/epics.js').then(m=>{const r=m.FUSIONS.filter(f=>f.outTier==='rare').flatMap(f=>f.need.map(n=>n.join(':')));const e=m.FUSIONS.filter(f=>f.outTier==='epic').flatMap(f=>f.need.map(n=>n.join(':')));console.log('交集',r.filter(x=>e.includes(x)))})"
```

輸出 `交集 []`（欄位名依實作調整）。

```bash
node tests/run.mjs traits
```

全綠。

交接筆記要有：四階持有率的實測值、與基準的對照、門檻怎麼調的。

---

## 交接筆記

<!-- 做完由執行者回填 -->
