# S22 · 文件同步

狀態：未開始
前置：S21
預估：1 session
推理難度：低
建議模型：Haiku 4.5（`claude-haiku-4-5-20251001`）
理由：純文件更新，事實來源都在前面各站的交接筆記裡。沒有設計判斷。

---

## 為什麼做這件事

V4 重建走完之後，所有面向玩家與開發者的文件都過期了。README 現在寫的東西**大部分
是錯的**：

> 每季三階段：**季初訓練**（擲骰加點）→ **賽季** → **休賽期**
> 22 歲前累積 5 次擲出「6」可覺醒隱藏素質
> 開局選擇 ID 與位置，可輸入自訂種子碼（**同種子＋同選擇＝同一段人生**）

三條全錯：擲骰加點沒了（S16）、決定論廢了（S09）、季初訓練變月回合（S14）。

---

## 入口狀態

```bash
npm test
```

全綠，S21 已完成（DEMO 跑得完，瀏覽器實測過）。

```bash
ls docs/v4/*.md
```

每份說明書的「交接筆記」都已回填——**那些是這一站的事實來源**。

---

## 範圍

### 要做

**1. `README.md`**

- 玩法說明：擲骰加點 → 訓練活動選單；年回合 → 月回合；OVR → 十二技能
- 「同種子＋同選擇＝同一段人生」→ 種子只決定出生天賦（V4 §1.4）
- 「22 歲前累積 5 次擲出 6」→ 沒有骰子了
- **測試指令改對**：現在寫 `node tests/headless.mjs`，正確是 `npm test`
  （＝`node tests/run.mjs`）
- 授權與出處：S06 應該已經改過，確認一致

**2. `WIKI.md`**（攻略）

251 行，內容全部基於舊系統。要重寫的部分：
- 六屬性的刻度（0–100）與潛力
- 十二技能與位置權重
- 訓練活動菜單與體力管理
- 隱藏心理六維（**攻略可以講機制，但不要洩漏具體數值**——V4 §14.1 的精神是
  「UI 不揭露配方，玩家靠 WIKI/社群探索」，所以 WIKI 是玩家寫的東西，
  官方文件保持神秘）
- 特質與合成：**不要列出配方表**

**3. `ARCHITECTURE.md`**

- 目錄結構：新增 `engine/{training,stamina,psych,eventTrigger}.js`、
  `data/reputation.js`、`phases/{month,seriesEvent}.js`；移除 `phases/alloc.js`
- 主迴圈說明：年回合 → 月回合
- Beat 協定：`{type:'alloc'}` 沒了，換成訓練活動選單的 choice
- **保留 S02 建立的「## 程式碼血緣」節**，把處置欄更新成已完成

**4. `CHANGELOG.md`**

新增 v5.0.0（或依你的版號慣例）一節，記 V4 重建的全部改動。
素材來自各站的交接筆記。

**5. `WORKLOG.md`**

在最上方加一筆總結整個 V4 重建（格式見 `~/.claude/worklog.md`）。
記的是意圖與脈絡：為什麼重建、四根柱子怎麼換、踩到什麼、哪些刻意沒做。

**6. `package.json`**

版號更新。確認 `test:golden` script 已在 S09 移除。

**7. `ESPORT-DESIGN.md`（v3）標記為已取代**

檔頭加一行指向 `ESPORT-DESIGN-V4.md`。**不要刪**——它記錄了一段設計歷史。

**8. `docs/v4/README.md` 狀態表全部標「完成」**

### 不要做

- **不要改任何 `src/` 或 `tests/` 檔案。** 這一站純文件。發現程式跟文件對不上，
  **以程式為準改文件**；若判斷是程式錯了，寫進交接筆記，不要自己改
- **不要在 WIKI 洩漏合成配方**（V4 §14.1）
- **不要刪 `ESPORT-DESIGN.md`**
- **不要動授權敘述**（S06 已定案）

---

## 要動的檔案

| 檔案 | 動作 |
| --- | --- |
| `README.md` | 玩法、測試指令、種子說明 |
| `WIKI.md` | 攻略全面更新 |
| `ARCHITECTURE.md` | 目錄、主迴圈、beat 協定、血緣表處置欄 |
| `CHANGELOG.md` | 新增 V4 重建一節 |
| `WORKLOG.md` | 總結一筆 |
| `package.json` | 版號 |
| `ESPORT-DESIGN.md` | 檔頭標記已取代 |
| `docs/v4/README.md` | 狀態表全完成 |

---

## 規則與不變式

- `npm test` 前後完全不變（沒碰程式）
- **README 裡的每個指令都要真的能跑**。逐條執行過
- **文件裡不再出現**：擲骰、加點、OVR、決定論、「同種子同人生」、
  `tests/headless.mjs`
- ARCHITECTURE 的目錄樹與實際 `src/` 一致

---

## 完成定義

```bash
npm test
```

全綠。

```bash
grep -rn "擲骰\|加點\|OVR\|headless.mjs\|同種子＋同選擇" README.md WIKI.md ARCHITECTURE.md
```

輸出為空（或只剩刻意保留的歷史敘述，例如「舊版是擲骰加點」）。

```bash
git diff --stat -- src/ tests/
```

輸出為空。

人工檢查：照 README 的說明從零開始跑一次（起伺服器、開頁面、玩幾個月、跑測試），
每一步都符合文件描述。

---

## 交接筆記

<!-- 做完由執行者回填。V4 重建到此結束，記下還留著沒做的事（正式版的業餘期、多年生涯擴充等） -->
