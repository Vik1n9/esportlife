/**
 * 天生特質池（V4 §1.4／§14.1）。純資料，無邏輯。
 *
 * 天生特質是種子（§1.4）的產物：出生時擲骰決定 0／1／2 個（40／40／20%），
 * 除了重選種子沒有其他取得管道。它是**通用特質的一個子集**，不是第六階——
 * 條目本體住在 `data/traits.js` 的 `BASE_TRAITS`（這樣 lookupTrait／合成／UI
 * 全部認識它），這裡只宣告「哪些 key 是天生來源」與每個條目的 `mentalBias`。
 *
 * 池的規則（§14.1，v4.2 硬上限）：
 * - **只收基礎人格／體質類**，不收競技表現類。「天生會打團」推不出來，「天生抗壓」
 *   推得出來——§0.5 直覺檢驗。
 * - **池內特質總數 ≤ 5**。§1.4 給出期望 0.8 個／種子，均勻抽取下單一條目取得率
 *   是 `0.8 ÷ N`：N = 5 → 16%（過 §14.2 死配方規則的 15% 下限），N = 6 → 13.3%
 *   破線。池開得越大每一個就越拿不到，與直覺相反，所以寫成硬上限。
 * - **每一個都必須指派合成池**（persona 或 performance，§14.2）——沒有指派等於
 *   在合成樹上是死的（死路檢驗）。
 * - 人格類 → persona 池；體質類 → performance 池。
 *
 * `mentalBias {dim, dir}`（§9.1）：初始心理六維的微調不得與天生特質矛盾。帶
 * mentalBias 的條目，出生時對應維度改抽有向區間（正向 +6～+10／負向 −10～−6），
 * 不再對稱 jitter；體質類不宣告（它動的是受傷率，不是心理）。池內 mentalBias 的
 * 維度不得重複——否則同一個種子抽到同維度的正負兩條，初始值就不知道該聽誰的。
 *
 * ⚠ v4.3 增訂：具體條目（名稱／效果數值）之後由特質編輯器（S18a，§14.7）重定義，
 * 這裡的 5 條是機制先行用的初始內容——池的 schema（≤5、雙池、mentalBias、副作用）
 * 是硬規則，條目內容不是。
 */
export const INNATE_POOL = [
  { key: 'iron', mentalBias: null },                    // 體質類：受傷率（performance）
  { key: 'glass', mentalBias: null },                   // 體質類：純副作用條目（performance）
  { key: 'innateCalm', mentalBias: { dim: 'comp', dir: 1 } },   // 人格類：抗壓正向
  { key: 'innateLeader', mentalBias: { dim: 'trust', dir: 1 } }, // 人格類：信任正向
  { key: 'nightOwl', mentalBias: { dim: 'disc', dir: -1 } },     // 人格類：紀律負向
];
