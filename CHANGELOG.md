## [v4.6.4] - 2026-08-17

### S43：固定框架與單一決策槽（§22.2／§22.2.1 修訂）

決策槽原本靠 `position:sticky;bottom:0` 假裝固定。sticky 的底部吸附需要包含塊在
元素下方還有剩餘高度，而 `#act` 是 `#app` 最後一個子元素、兩者底邊重合——travel
距離為零，等於完全沒作用：事件文本一長，選項就被推到摺線以下，玩家得捲回去找。

- **固定框架**：`#layout` 高度＝`100dvh`＋`overflow:hidden`，頁面本身不再捲動。
  `#log` 成為框內唯一的捲動窗格（`flex:1;min-height:0;overflow-y:auto`），狀態帶、
  屬性條、決策槽都是框內的 `flex:none` 子項，位置不隨文本長度改變。寬螢幕三欄同理：
  `#year-dir`／`#app`／`.panel-sheet` 各自吃滿框高、各自獨立捲動。
- **sticky 全數退場**：`#board`、`#year-dir`、`.panel-sheet` 不再需要 sticky，
  S42b 為了算 sticky 落點而加的 `--board-h` 與 `initBoardMetrics()` 一併移除
  （固定框架下欄高由 grid 列高決定，不必量測狀態帶）。
- **單一決策槽**：`slot:'inline'` 的卡牌選項改畫在決策槽，不再接在卡片下緣。
  卡片端保留兩個唯讀狀態——等待中掛 `.awaiting` 提示條並以 ↓ 指向下方，選定後
  就地留下「你選了 X」。`askChoice`／`askInline` 合流為同一個 `promptOptions`，
  差別只在有沒有錨點卡。
- **槽高改隨視窗縮放**：`--act-h: clamp(240px, 38dvh, 368px)`。固定框架下槽高吃掉
  的就是文本區高度，368px 定值會讓 844px 高的手機只剩 136px 可讀；改後約各分一半。
  §22.2.1 要的是同一視窗內不隨當月選項數跳動，不是全裝置同一個像素數。
- **捲動對象**：`scrollToBottom()` 改捲 `#log`（頁面不捲，捲 window 等於沒動作）。
- **日誌篩選 chips** 改單列橫捲＋右緣淡出，不再換行多吃一列高度。

規格同步：§22.2「卡牌覆寫」改寫為「卡牌問答」並說明為何收成單一操作點；
§22.2.1 增訂「固定框架」不變式。

## [v4.6.3] - 2026-08-17

### S42b：寬螢幕版面修正（§22.6，接續 S42）

S42 的三欄斷點只把 `#app` 推到第 2 欄、`#panel` 留在原地，兩者落在同一個 grid 格
互相疊字，右邊 340px 欄空著。本次修掉這條與另外三處版面偏差，規格未動。

- **三欄重疊**：`≥1024px` 補 `#panel{grid-column:3}`。原本 `#app` 與 `#panel` 同格
  疊放，選手資料蓋在敘事流上、事件文本左緣被截。
- **側欄 sticky 落點**：狀態帶實高約 165px（五列），CSS 卻硬寫 `top:56px`，左右欄
  一捲就滑進狀態帶底下。改由 `ui/board.js` 的 `initBoardMetrics()` 以
  `ResizeObserver` 量測 `#board` 實高寫回 `--board-h`，`#year-dir` 與 `.panel-sheet`
  的 `top`／高度都吃這個變數。
- **右欄不再跟著捲走**：`#panel` 補 `align-self:stretch`——`.panel-sheet` 的包含塊
  是 `#panel`，`#panel` 只有內容高時 sticky 沒有可走距離。
- **超寬螢幕欄距**：`#layout` 補 `max-width`（兩欄 940px／三欄 1180px），整個 grid
  置中。原本中欄在無限寬的 `1fr` 裡置中，2560px 下與側欄裂出 690px 空白。
- **中欄高度**：`#app` 補 `min-height:calc(100dvh - var(--board-h))`，讓決策槽回到
  欄底（豎屏靠 `#layout` 的 `min-height` 達成，grid 的 `align-items:start` 會失效）。
- **蓄力讀數**：選手 tab 的屬性列直接印浮點（`·蓄0.1376267417605339`）把整列擠成
  兩行，補上 `attrbar.js` 同一套 `num()` 格式化。

三條不變式重新驗證：決策動線不變、`#app` 維持 600px 行長上限、寬螢幕無額外資訊。

### S42：寬螢幕版面（§22.6）

壬組完工站。新增兩個斷點（≥720px 兩欄、≥1024px 三欄），手機豎屏版面完全不受影響。

- **DOM 重組**：新增 `#layout` 包裹 `#board`／`#app`／`#panel`，`#board` 移出 `#app`
  成為 `#layout` 直接子元素以橫跨整寬；移除假導覽列 `.site-nav`。
- **#panel 兩態**：窄螢幕（<720px）底部抽屜（`.open` 控制顯隱）；寬螢幕（≥720px）
  常駐右欄 340px（`position:static`，`display:block`，`.open` 不參與顯隱）。
  `refreshPanel`／`togglePanel` 改讀 `isPanelVisible()`（含 `matchMedia` 查詢），
  斷點跨越時由 `change` 事件觸發重繪。
- **年度目錄左欄**：新檔 `src/ui/yearDir.js`，≥1024px 時在 240px 左欄常駐顯示年度
  目錄（資料取用 `ui/log.js` 的 `yearEntries`，不重建一份）。
- **開場畫面**：≥720px 建角表單改雙欄（左說明／右表單）；favicon 對齊現行
  terracotta 品牌色。
- **分享圖**：移除從未載入的 `Cinzel`／`Rajdhani` 字型，改讀 `--ui`（Inter＋Noto Sans TC）；
  底色與標題色對齊現行色票。
- **鐵則註解**：`src/styles.css` 檔頭鐵則改寫為「窄螢幕抽屜／寬螢幕常駐」兩態。
- **三條不變式**：決策動線不變（#act 仍錨定在 #log 正下方）、文本行長不隨螢幕拉寬
  （#app 維持 max-width 600px）、寬螢幕不出現豎屏看不到的資訊。

## [v4.6.2] - 2026-08-17

### S41：事件日誌閱讀（分類／年度跳轉／五拍成組）

卡片分類（`kind` 欄位）：各 phase 檔透過 `kinded()` 工廠標示卡片類型
（`train`／`match`／`event`／`market`／`season`），91 個呼叫點零改動。
賽事五拍成組：`seriesEvent.js` 五拍帶 `series` 標識，`ui/log.js` 偵測連續同
`series` 的卡片收進 `.series-block`，跨過中間的 `choice`（備賽戰術走 `act` 槽）。
分類篩選：六個 chips（全部／賽事／訓練／事件／市場／結算），純 CSS 屬性選擇器
切換，不重繪、不重跑 generator。年度跳轉：年度分隔線登記進目錄，下拉選單跳轉。
生命週期卡（`game.js` 的出生／退役／結局）無 `kind`，任何篩選下保持可見。

## [v4.6.1] - 2026-08-16

### 壬組 S37：UI 版面規格增訂（純文件，除版號外程式與測試未動）

§22 於 v4.3.2 定案，主線 S01–S22 完工但**版面從未照做**。本站盤點缺口、把規格
細化到可施工粒度，並把實作拆成壬組 S38–S42 五站。

- **§22.1 主畫面骨架**：線框細化到可施工粒度；明訂體力條常駐、屬性條**帶蓄力
  第二刻度**、屬性條緊貼決策槽、賽事五拍**必須成組呈現**、狀態帶**不得出現任何
  技能值**；新增日誌檢視控制（分類篩選＋年度跳轉）。
- **§22.2 決策槽**：原三模式定案不動，補「為什麼分兩處」的理由、實作落點
  （`choice` beat 帶 `slot` 欄位，由發射端指定而非 UI 猜）與線框；新增 **§22.2.1
  決策槽版面不變式**（同槽互斥、位置與最小高度固定、選項須在選前呈現預期收益）。
- **§22.3 四 tab**：內容不動，補線框並明訂「四 tab 是並列檢視入口，不是單一長捲」。
- **§22.5**：屬性條補蓄力讀數；明訂體力條常駐於狀態帶第二列。
- **§22.6 寬螢幕版面（新增）**：手機豎屏仍是基準版面，寬螢幕只攤開資訊、不重排
  決策動線；三條不變式＝決策動線不變／文本行長不隨螢幕拉寬／寬螢幕不得出現豎屏
  看不到的資訊。**§22.4 資訊公開性一字未動**，在所有寬度一體適用。

盤點出的兩項現行偏差：**§22.2 卡牌覆寫整條未實作**（所有選項一律送底部決策槽）、
**§22.1「決策不提供技能讀數」現行違規**（狀態帶印本位置核心技能值）。

`DESIGN.md` 同步新增 Layout 斷點段與五個元件規格。`docs/v4/README.md` 新增壬組
S37–S42（全表 56 → 62 站），六份站說明書隨附。附錄新增 #75–#77。
除版號四份手抄本外程式與測試未動，`npm test` 19928 項基線不變
（版號一致性由 `tests/kernel/version.mjs` 守住）。

## [v4.6.0] - 2026-08-16

### 庚組 S23：NPC 規格細部定案（純文件，程式與測試未動）

§23 細部規格定案（規格書 23.3–23.8），S24–S30 全部「依 S23 定案」的欄位有了
回填來源；逐條定案與消費站對照見 §23.8 消費表。

- **NPC schema（23.3）**：遊戲資料一律 ESM `.js`（零建置無 JSON import），中間
  產物留 `tools/npc/`；`peak.rating` 是 NPC 強度唯一骨幹，不存 potential 與技能值；
  `fetch_priority 2`（Global_Boss）範圍定案。
- **逐選手對手模型（23.4）**：§11.1 階梯表仍是對手強度唯一來源，NPC 陣容是階梯
  水準的「身分實體化」；對手明星項顯式化，`OPPONENT_SUPPORT` 9.0 縮為教練／體力
  殘項（起始 3.5，終值 S30 重校）；新增三條不變式（年代一致／實體化率／缺口不帶身分）。
- **百分位回歸（23.5）**：究極綠葉吃同位置母體場均助攻 P90，網紅選手改讀
  `peakRating` 對歷史母體中位數；引擎讀靜態表，新增 `['percentile', …]` 謂詞
  （QUERIES／PREDICATES 雙註冊）。
- **隊友池 NPC 化（23.6）**：職業期改 NPC 池抽選（年代×位置×賽區），`MATE_NAMES`
  退役；業餘期留 par±7 機制改虛構名池；`state.mates` 擴充、SAVE_VERSION 歸 S28。
- **缺口備援（23.7）**：Liquipedia → Leaguepedia → synthetic 三級，逐筆標
  `data_source`；GPL 2012–2013 預估最薄。

受影響章節：§11.1（階梯表與明星項註記）、§14.3（暫代註記改定案指向）、§20.2
（隊友欄位寫回）、§21.3 與附錄 #32（條目標記定案，歷史登記保留）。附錄新增
#68–#73。程式與測試未動，`npm test` 19919 項基線不變（版號四份手抄本同號由
`tests/kernel/version.mjs` 守住）。

### 工作細則修訂（2026-08-16，不升版號）：S24 拆四站

S24 資料目錄與爬取依工作性質拆為四站（附錄 #74）：S24a 探勘與管線、S24b 台港澳
目錄與 raw、S24c 國際目錄、S24d 國際 raw。切割點＝決策密集 vs 執行密集，抓取量大
靠中斷續傳承接（執行時間不是站的工作量）；台港澳量小併一站，國際拆目錄／抓取兩站。
S25 前置改 S24d；規格 §23.2、`docs/v4/README.md` 狀態表同步。站切割屬工程細則、
與遊戲規則無關，版本維持 v4.6.0 不升——版號四份手抄本不動。程式與測試未動。

## [v4.5.3] - 2026-08-16

### V4 重建主線完成：月回合 × 設施制訓練 × 十二技能 × 教練評價（S01–S22，39 站）

> 版號與規格書 `ESPORT-DESIGN-V4.md` 共用同一套（AGENTS.md 版本編號規則），
> 故為 v4.5.3 而非 v5.0.0。工作說明書共 53 站，本節涵蓋主線 39 站；
> 庚組（S23–S30）與辛組（S31–S36）共 14 站施工未開始。

V4 重建同時翻掉四根互相咬死的柱子——回合粒度（年 → 月）、成長模型（擲骰加點 →
設施制）、評估層（OVR → 教練評價）、屬性刻度（1–80 → 0–100）——並在此之上把
賽制、內容與生涯收束全部重做。這是一份重建總結，逐站細節見各站交接筆記
（`docs/v4/`）與下方各條目。

**核心機制**
- **月回合制**：一年十二個月，每月選一個訓練活動；賽事月（季後賽／MSI／世界賽）
  不訓練、改走五拍賽事事件序列（賽前敘事→備賽戰術→戰術結算→比分→賽後敘事）。
  兩賽段年 7 個（MSI 年）／8 個（無 MSI 年）養成回合、三賽段年 6 個
- **設施制訓練**：八個訓練活動（個人機械訓練／強化訓練賽／戰術復盤／體能健身／
  排位衝分／英雄池練習／休息／復健預防），兩階段判定（成敗→檔位），結果由 60 張
  訓練事件卡決定；骰子加點協定整段退場（`phases/alloc.js` 刪除）
- **六屬性 0–100 ＋ 潛力衰減**：潛力仍是軟天花板，越接近長得越慢；刻度重校時
   per-point 係數全部除以 1.25（§17.2 的坑）
- **十二技能**：對線／操作／視野／節奏／支援／資源控制／會戰／開團／保護／邊線／
  轉線／走位，由屬性加權導出、玩家不能直接加點；每路核心 4 項＋次要 2 項
- **教練評價取代 OVR**：薪資／試訓／續約／FA 全部改讀教練評價（位置技能加權＋
  隱藏心理修正＋特質調整）；OVR 字樣從 UI 全面移除
- **隱藏心理六維**：抗壓／自信／動機／紀律／信任／韌性，不可見、只由事件卡改變，
  影響技能發揮穩定度（±15% 極限）與受迫／非受迫性失誤；聲量層保留為獨立可見層
- **體力系統**：0–100 資源，每月自然恢復 +10、訓練 −20~−30、休息 +50；低體力
  訓練成功率下滑、大失敗與受傷風險上升；體力進表現不進場次
- **生命週期曲線**：成長與衰退是同一條曲線的兩面，六屬性各有巔峰年齡與衰退速率
  參數（由種子生成）；固定年齡衰退表與退役硬上限作廢
- **市場淘汰制**：衰退曲線自然進行，自由市場無報價就只剩退役；退役走三層事件
  （五種特殊結局 → 四選項含「最後一搏」續命 → 結算）

**內容**
- 事件卡 26 → **86 張**（含 13 張條件卡，條件命中優先）；訓練事件卡 **60 張**；
  生涯任務卡 **25 張**（20 legend＋5 route）；扮演卡 23 張；天生特質池 5 條
- **統一條件語言**：任務卡／事件卡／特質授予／退役結局共用 `evalCond`
  s-expression，`whenHits` 已刪除；謂詞一律讀帳本查詢層（`ledger.js`）
- **四階特質重建**：S19a 把當時的 50 特質全部重定義（雙面性＋副作用＋互斥＋維持），
  擴充後現有 **72 條**（通用 30／稀有 8／史詩 11／傳說 20／獨有 3）；配方 18 條
  （稀有 8＋史詩 10，素材分池）；傳說 20 個改由生涯任務卡發放（不再由配方合成）；
  獨有階 3 條（老來俏／千錘百鍊／火炬手）
- **名次序位單一來源**：`formats/finishes.js`，三份手抄本退役——名次以機器可讀
  鍵入帳，不再解析顯示文字
- **世界賽衛冕者**：冠軍登記表 `titleHistory` 逐年連續，衛冕者佔淘汰賽席位、
  比同輪對手強 4 點，淘汰賽贏下授予獨有特質「火炬手」
- **生涯傳記**：退役時依事實拼接維基式五段總結，同一段生涯必定同一篇
- **生涯任務面板**：任務進度、期限與素材對玩家可見；失敗降階為生涯標籤

**DEMO 與起點**
- DEMO 起點：2015 年、19 歲出道（PRO 路線，跳過業餘期）；校準基線為 AMATEUR
  （2012、16 歲）雙軌制
- **DEMO 三年期程（S21b）**：§19 期程改 **36 個月＝三個賽季（2015–2017）**，
  期滿未觸發任何結局即以「DEMO 結束」比照退役結算；期程單一來源
  `src/engine/demo.js`；狀態列顯示「DEMO 14/36」。校準隨之調整：§7.2 衰退跟隨
  峰前改讀固定潛力（「19 歲衰退錯位」一併解掉）、§18.2 位置需求門檻補年輕項
  （22 歲前每年 −2）；最後一搏不再重跑同一年（`closeYear`）
- 測試由 8939 項成長到 **19919 項**；`golden.json` 決定論快照刪除，改為
  比例制平衡不變式（`regression/invariants.mjs`）
- `SAVE_VERSION` 9 → **23**（**舊存檔全部作廢**）
- 規格書 `ESPORT-DESIGN-V4.md` 升到 **v4.5.3**；工作說明書主線 S01–S22 共 **39 站**
  全部完成（全表 53 站，庚辛兩組 14 站未開工）
- **版號單一來源**：`APP_VERSION` 從 `main.js` 搬到 `src/version.js`（畫面右上角
  徽章與結算分享圖讀它），值由停滯的 v4.0.0 對齊到 **v4.5.3**；新增
  `tests/kernel/version.mjs` 守著四份手抄本——`package.json`／`APP_VERSION`／
  規格書檔頭／CHANGELOG 最新節必須同號
- 內容編輯器（`tools/`）：事件卡／訓練卡／任務卡／特質／配方／天生特質的表單
  編輯＋驗證，輸出貼回資料檔

**授權**
- S03–S05 淨室重寫全部完成，本專案不再含原作者程式碼（血緣表見
  `ARCHITECTURE.md` 九節）

### S20g 賽事成績帳本與衛冕者：對手表上姓名 — 2026-08-16

在「對手一直是純數字」的引擎裡插進第一個**對手身分**概念，讓 `火炬手`（S20c 留著
「待對手身分層」的那一條）終於拿得到。

- **冠軍登記表 `titleHistory`**：每年世界賽的冠軍進同一張表（玩家與 NPC 共用），
  玩家沒奪冠／沒參賽的年份也照樣登記（復健年亦然，不留空窗）。查詢層
  `reigningChampion`／`lastFinish` 從它導出
- **衛冕者賽路**：上屆冠軍佔一個淘汰賽席位，相遇機率八強 15%／四強 30%／決賽 60%，
  比同輪一般對手強 4 點。淘汰賽贏下 → 授予獨有特質 `torch_bearer`
  （200 段實測取得率 4.0%）
- **統一條件語言**：退役 `whenHits`，事件卡觸發條件改吃 `evalCond`（與任務卡同一套）。
  事件卡條件終於讀得到史詩／傳說特質；六大屬性補進謂詞表
- 世界冠軍人均 0.055 → 0.035（200 段 A/B），分布變動交給 S20d 校準
- `SAVE_VERSION` 20 → 21（**舊存檔作廢**）。`npm test` 19093 項全綠

### S20c 交接鏈修復：名次以鍵入帳，賽段冠軍終於算分 — 2026-08-15

三個靜默 bug 同屬一類——**查表的鍵必須與另一個檔產生的字串逐字相同，而沒有測試在守**。
本站不修字串，改成不再拿字串當鍵。

- **名次以機器可讀的鍵入帳**：新增 `src/data/formats/finishes.js` 作為名次的單一來源，
  里程碑帶 `event`／`finish` 欄位，查詢層不再解析顯示文字
  - 真實的**世界賽冠亞軍**在生涯查詢裡回「沒打過」——連帶世界賽冠軍反而開不了
    `legend-worlds-king` 那張傳說任務卡。修復
  - `MSI 止步` 在帳本回 99（等同沒打過）、在傳記回 4。修復
- **每一座賽段冠軍在生涯評分裡值 0 分**（該值 80）：關鍵字 `'季後賽冠軍'` 對不上實際
  產生的 `'2020 PCS 夏季賽冠軍'`。改讀計數器。實測 200 段生涯平均每段 +50 分，
  4% 的生涯上移一個等第（門檻重校留給下一站）
- **獨有特質（第五階）接線上線**：`老來俏`（30 歲後單季獲獎）與 `千錘百鍊`
  （生涯出賽 ≥ 1000 場）現在拿得到；`火炬手` 待對手身分層
- **事件卡的空頭承諾修掉**：`賽前互嗆`／`招牌被Ban` 兩張卡寫了特質解鎖旗標卻零給予
- **史實解散**：2016 年的台北暗殺星永遠不觸發（該隊名單只到 2014），改 2014
- 內部整理：四階特質的階名四套拼法收斂成一套、編輯器的時段標籤砍成引擎兌現得了的
  6 個、訓練卡池別由檔位導出、刪除死程式碼
- `SAVE_VERSION` 19 → 20（**舊存檔作廢**）。`npm test` 18932 項全綠

### 規格書 v4.1：生涯任務卡、替代性生涯路線、種子天生特質（僅文件） — 2026-08-13

`ESPORT-DESIGN-V4.md` 升到 v4.1，併入外部提出的增訂清單（可行性研究見
`docs/v4/08b-規格V4.1增訂.md`）。**這一版只改文件，程式與測試未動。**

- **§0.5 設計原則**：四項檢驗（直覺／防農／死路／心流），列進每一站的收尾檢查
- **§12.3 生涯任務卡**：跨回合、有期限的任務，`legend`／`route` 兩型共用一套 schema。
  傳說特質改由它發放，**不再由配方自動合成**（§14.2 的 20 組素材組合原地轉成觸發條件）
- **§14.3 替代性生涯路線**：五條非冠軍路線（賽區統治者／流浪傭兵／不滅隊魂／究極綠葉／
  網紅選手），終點是史詩特質＋生涯標籤，讓平庸局也有明確的 build 終點
- **§1.4／§9.1 種子天生特質**：種子決定 0／1／2 個（40／40／20%），初始心理六維必須與
  之一致；順帶給了 `鐵人` 一條來源（原本取得率 0%）
- **§15.5 生涯傳記生成**：單局結束時依事實拼接生涯總結文本
- 重建工作說明書 25 站 → 30 站（新增 S17a／S17b／S19d／S21a）

### 四階特質合成（LoL 道具合成概念）＋大量新事件 — 2026-08-12

把隱藏特質從「基礎→史詩」兩層拆成四階：**通用 → 稀有 → 史詩 → 傳說**，概念取自 LoL
道具合成（小件疊終極裝）。每一階都是困難取得的隱藏素質，持有正確組合時自動合成，越往上
疊生涯天花板越高；配方仍完全隱藏，靠社群探索。

**四階架構**
- `src/data/traits.js`：通用特質（新增 `肝帝`／`梗王`／`鏡頭感`／`守護者`）＋稀有特質
  （練功機器、流量密碼、明星選手、定海神針、元老、傳奇偶像、諧星、高冷守護）
- `src/data/epics.js`：史詩特質（沿用既有 10 個核心配方）＋傳說特質（不死魔王、弒神者、
  銅牆鐵壁、流量金身、一穿五、版本之神）
- 稀有與史詩素材刻意分屬「人格／媒體」與「競技表現」兩池、互不重疊，兩條線可以同時養，
  史詩的可達性維持原版水準（實測 160 段生涯：史詩 63/80、傳說 23/80、平均巔峰 OVR 59.9）
- 引擎 `checkFusions` 泛化為跨階消耗（`[tier, key]` 配方），`state` 新增 `rare`／`legendary`
  兩槽；面板與結算依四階配色顯示

**新事件卡（16 → 26 張）**
- 通宵練功、梗圖爆紅、開台首播、粉絲見面會、隊友甩鍋、極限搶龍、賽前互嗆、招牌被Ban、
  賽後金句、被釣魚——大量取材自 LoL 社群與實況圈梗（人形外掛、開秀、打野差距、心態炸裂、
  招牌角、金句、釣魚）

**事件邏輯修正**
- 選項可帶自己的 `on` 結果：像「暫時關台／休息／不看」這類跟卡片主軸相反的路，不再套用
  卡片的通用結果（例如關台的結果不再是「開台時間控制得宜」）。`streaming.close`、
  `solo_queue.stop`、`scrim.rest`、`endorsement.reject`、`vod_review.skip`、
  `roster_drama.avoid` 已補上各自的 good/bad。

### 九項素質 → 六屬性＋導出技能（D&D 兩層制）

九個滑桿每季來一次，決策沒有質變只有量變，玩家很快就會用「哪個 OVR 權重高就餵哪個」
把它變成打卡。素質不是不重要，是不該由玩家直接管理。

**兩層制**
- 新增 `src/data/attributes.js`：六大屬性 `體能 VIT／靈巧 AGI／意識 AWR／技巧 TEC／
  默契 SYN／決斷 DEC`，是玩家唯一直接經營的一層（`state.ability` → `state.attr`）
- 原九項素質＋位置專屬共 13 項降級成**技能**，移到 `src/data/skills.js`，由屬性加權
  導出（`SKILL_WEIGHTS`，每列和為 1.00），玩家不能直接加點
- 技能值與屬性同量級（1–80），OVR 尺度與舊版一致，賽區 par／勝率公式／薪資曲線都
  不必重算
- `src/engine/abilities.js` → `src/engine/attributes.js`；`ROLE_ATTR_WEIGHTS` 在載入時
  把兩層權重折疊成 5×6 表，OVR 一次內積算完，不繞技能

**位置身分**
- 技能刻意只掛 2～4 個屬性。第一版每項技能都沾到五、六個屬性，折疊後五路權重長得
  差不多（最扁的中路最高／最低只差 2.8 倍），位置身分被平均掉。寫純之後輔助的靈巧
  權重掉到 .028——靈巧對輔助幾乎沒有意義，這正是位置該有的樣子
- 出生時該路 OVR 最吃的兩個屬性額外 `+0..4`（`ROLE_START_EDGE`），天賦一出生就有
  位置味道。取代舊版寫死的「lane/op/tf +0..4」

**平衡**
- 成長門檻整體下移 4（66/58/50 → 62/54/46）。同一份訓練點分給六項而非九項，每項多吃
  1.5 倍，不補這一刀全體通膨：80 段實測巔峰 OVR 60.7 → 63.7、傳奇 5 段 → 14 段。
  下移後回到 59.9 / 5 段，老手打法的等第分布幾乎完全對回舊制
- 年齡衰退改打 `agi/vit/tec`，`awr/dec` 30 歲後仍可能續升——「老將靠意識吃飯」變成
  規則的直接後果，不是特例
- 事件卡結果欄位 `ability` → `attr`，18 張卡全部重新對應到屬性。一張卡動意識，玩家
  會同時在大局觀、視野、遊走三條技能上看到變化
- 冒煙測試的兩條「打法差距」不變式換了指標。舊版「新手打法 0 座傳奇、新手邊緣選手
  必須多於老手」有一半來自每路有 2/9 素質權重為 0（平均分配等於丟掉兩成的點）。
  六屬性對每路都有份量，加上權重和固定為 1、成本階梯是凸的，數學上平均分配拿到的
  就是平均值。改為守「傳奇仍罕見（新手不到老手三分之一）」與「老手平均巔峰 OVR
  至少高 1.5」

**介面**
- 加點列固定六列；面板拆成「屬性」（可加點、附潛力刻度與 VIT/AGI 縮寫）與
  「技能（位置）」（唯讀，只列該路有 OVR 權重的項目，由重到輕）

**存檔**
- `SAVE_VERSION` 6 → 7。舊存檔沒有 `attr`，一律作廢重開

### 授權與出處
- 新增 **MIT License**（`LICENSE`），並在 README 補上原始出處（YaKyuLife）與授權狀態說明。
  改寫前已向原作者申請授權，等待作者回覆後再補上原作者的版權署名。

## [v4.0.0] - 2026-08-12
### 賽制脫離棒球模型：MSI 是俱樂部賽事

專案的賽制骨架承襲自棒球生涯模擬，但 LoL 的賽制與過程差距極大。最刺眼的一處是
MSI——程式把它寫成「國家隊徵召」，玩家可以婉拒，打完還會累積傷病風險。MSI 是
俱樂部賽事，門票發給戰隊。追查下去共列出 12 項棒球殘留，橫跨整個年度流程。

**MSI**
- 資格從「個人 OVR ≥ 52 被徵召」改成「你的隊伍拿了賽段冠軍」（2023 起前兩名）。
  刪除 `MSI_CALLUP_OVR` / `msiEligible` / `msiForced` / `intlLock` 整套徵召邏輯
- 沒有出不出賽的選擇。決策點改成媒體日與決賽前的扮演路口
- 賽制逐輪出比分：2015–2022 四隊小組雙循環＋BO5 淘汰賽；2023 起雙敗淘汰，
  輸一場還能從敗部殺回來。舊版整個 MSI 只有一次擲骰
- 時序歸位到賽季中段（原本在年末，與世界賽並列）
- 刪除 `carryInjuryRisk`（WBC 投手手臂磨損模型）。替代後果是真實的那個：MSI 打完
  到下一賽段只有兩三週，賽區內其他隊多練一個版本
- MSI 冠軍為賽區多掙一張世界賽門票（2023 起的真實制度）
- 特質 `nationalace` 國家隊王牌 → `soloking` 賽區之光

**世界賽**
- 門票不再是擲骰。種子序在席位內直接晉級，最後一張打地區資格賽（BO5，即 LCK
  Regional Finals／LCS Gauntlet）。舊版算好種子序卻還要 `rng.chance(96/88/74/30)`，
  第一種子有 4% 機率拿不到門票
- 席位數隨時代（`data/regions/*.js`）。舊版寫死「主場賽區 2、其餘 4」，但
  2012–2014 的主場賽區只有一張外卡門票，LCK/LPL 也是 2023 年才從 3 席增為 4 席
- 種子序來源隨時代：2013–2022 冠軍點數制，2023 起改看最後一個賽段的季後賽名次。
  舊版把冠軍點數一路套用到 2025 年，但那套制度 2022 年就廢止了
- 賽程逐輪出比分：入圍賽 → 小組賽／Swiss → 八強／四強／決賽 BO5
- 實測世界賽冠軍從人均 0.83 座降到 0.09 座

**先發／板凳**
- 體力不再決定出賽場次（棒球的輪值模型）。LoL 是五個固定先發，隊伍打幾場你就打
  幾場；體力低表現成手感下滑、陣亡數上升
- 新增板凳機制：打不到隊伍平均、默契見底、傷癒回歸位子被頂、年紀大了想換血都會
  被下放。坐板凳沒有戰報也沒有季後賽，連續兩段會被明講
- 傷勢從「整季報銷、下季復健年」改成缺席週數：小傷 2–5 週、重傷 8–16 週，
  真的要動刀報銷整季的只佔重傷的 12%

**轉會窗口**
- 窗口每年都開，不再是「合約到期才進市場」。三拍：傳聞 → 官宣 → 落定
- 合約中途可被買斷，這是新的決策點
- 新增外援名額：各賽區每隊限 2 名，主場賽區出身的玩家簽進任何海外賽區都佔一個。
  不是數值不夠，是位子沒了

**種子的意義**
- 拆成兩條亂數流：種子只決定天賦（起始能力、潛力、性格底色、初始英雄池），人生
  每次開新局都不一樣。引擎本身仍可重現，隨機的是「誰來決定人生種子」
- 修掉 `createState` 把 `seed` 宣告兩次的 BUG——後者（世界賽種子序）覆蓋前者，
  出生種子從來沒有真的存進 state。種子序更名 `seedRank`

**架構**
- `world.js` 依實體重切成 `data/regions/`（一個賽區一個檔）與 `data/formats/`
- 年曆抽成 `data/formats/calendar.js` 一張表，`game.js` 從 1033 行降到 221 行，
  賽事各自住進 `phases/*.js`。新增一個賽事＝寫一個檔、註冊一行、年曆加一列
- 特質效果從十個引擎檔的 66 個 `if` 收進 `kernel/modifiers.js` 的四個查詢函式
- 抽出 `kernel/{series,groups,strength}`，新增小組循環與 Swiss
- 修掉 `diminish()` 的保底缺陷：任何 ≥1 的變動永遠至少推 +1，邊際遞減在極端值
  等於失效。修正後打過六次以上國際賽的人大心臟不再清一色 100

### 同批進來的前端 UI 重設計

這一版還包含一輪獨立進行的前端重設計（樣式表 v3.1 → v4.3）：

- 色階改以 tftactics.gg 的計算樣式為準：#0d202b／#102531／#123040，容器髮絲線
  #17313a 只用於容器與分隔
- 小控制無邊框、純填色 #123040、3px 圓角；容器 4px 近直角
- 動作藍 #227aad（hover #4080b0）、active 底線 terracotta #d47559、
  遊戲狀態金 #b89d27、標題亮青 #adf4ed
- 移除自製 SVG 背景（`assets/rift-bg.svg`）與 Cinzel 襯線展示字
- 新增 `DESIGN.md`（視覺規範）與 `PRODUCT.md`（產品定位）

> 註：這批 UI 檔案在合併時被掃進了 `6ec9575`（年曆抽成資料表）與 `3af552d`
> （世界賽重寫）兩個賽制 commit，不是獨立的 commit。要查 UI 的實際變動請直接
> `git diff c94276b..HEAD -- index.html src/styles.css assets/`，不要照 commit
> 訊息找。

**測試**
- `headless.mjs`（23KB 單檔）拆成 `tests/{kernel,phases,history,regression}/`，
  runner 自動掃描目錄
- 新增黃金種子快照：160 段生涯的 state 雜湊，純重構必須逐位元一致
- 新增 `history/` 分區：直接斷言史實（2020 無 MSI、2022 不得 Swiss、2025 不得用
  冠軍點數、席位數逐年）
- 檢查項數 4047 → 9400

## [v3.5.0] - 2026-08-11
### UI 重設計：簡約數據站風格

- 移除舊版裝飾語彙：金色漸層標題、光暈、角框、鑽石符號、襯線展示字與自製 SVG 背景。
- 新色階對齊 tftactics.gg 計算樣式：#0d202b／#102531／#123040，容器邊框 #17313a；
  動作藍 #227aad、遊戲狀態金 #b89d27、目前階段底線 terracotta #d47559。
- 字型統一為單一無襯線 Inter＋Noto Sans TC，數值用系統等寬字。
- 事件日誌與選手資料面板改為深色卡片，長文可讀性優先；全文對比 ≥ 4.5:1。
- chrome 結構同站內頁：toolbar＋nav 列；小控制無邊框純填色、3px 圓角；
  主按鈕藍底白字；標題 2px 藍底線。

## [v3.4.0] - 2026-08-11
### 賽段制、BO 系列賽、心理與性格軸

- **事件卡改成玩家決策**。舊版是引擎背著玩家擲一次 50/50 就把結果貼出來，
  一整段生涯能真正做決定的只有訓練加點與合約路口。現在每張卡先描述處境，
  再讓玩家選一條應對路線，選項本身決定成功率與數值幅度。安全牌換到的是低變異，
  代價是那條路不會覺醒任何隱藏素質。
- **賽段制**。依史實把一年拆成 1～3 個賽段：2012 單季 → 春／夏兩賽段 →
  2025 起三賽段；韓國 2012–2014 本來就是 OGN 冬／春／夏三季，歐洲 2023 起三賽段。
  每個賽段各自跑例行賽、季後賽、抽事件卡，場次按權重切分——變多的是決策點，
  不是比賽場數。全年冠軍點數決定世界賽種子序。
- **BO 系列賽**。季後賽從「一次擲骰吐冠軍」改成八強 BO3 → 四強 BO5 → 決賽 BO5
  逐局模擬，種子序決定從哪一輪打起，決勝局另外吃「大心臟」加成。
- **心理與性格軸**。新增五個維度（大心臟／隊友默契／自我／知名度／風評），
  跟能力值完全分開：訓練點碰不到，只能靠扮演卡累積。知名度與風評分成兩條軸，
  才表達得出「誇張言行同時收穫人氣與負評」。
- **扮演卡 18 張**，分賽前記者會／媒體採訪／休息室／教練談話／日常訓練五個時機。
  **不擲骰**——扮演不是賭博，選了就是那樣的人；隨機的只有外界反應，
  力道由知名度放大。新增 7 個性格特質與 2 個史詩合成。
- **下剋上劇本**。低種子序在季後賽與世界賽獲得加成，幅度由心理素質決定，
  且設有上限——要讓它「有機會」，不是「換個種子序就變強隊」。
- **更衣室與輿論的後果**。默契見底會被迫轉隊、風評見底會被球團切割，
  兩者都直接作廢合約並讓市場報價縮水；反過來知名度高則續約時球團被迫加薪留人。
  設三年冷卻，避免變成常態。

### 顯示
- 心理值**全程不對玩家揭露數字**。面板只給粗略標籤（「一上大場面就抖」「毀譽參半」），
  事件結算只給箭頭。攤成數字之後玩家就會開始最佳化它，扮演就沒了。

### 測試
headless 檢查由 12 項擴充到 19 項，新增：賽段結構隨史實演進、賽段權重總和為 1、
心理值不外洩數字、扮演卡不碰能力值、BO 系列比分自洽、下剋上加成有上下界、
球團切割有冷卻不會變常態。

### 相容性
- `SAVE_VERSION` 4 → 5，**舊存檔失效**。

## [v3.3.0] - 2026-08-11
### 介面重設計：官方配色 × 召喚峽谷意象

- **全面改用 Riot Games / LoL 官方配色**：深藍 `#0A1428`、近黑 `#010A13`、
  金 `#C89B3C`、亮金 `#F0E6D2`、深金 `#785A28`、青綠 `#0AC8B9`。
- **LoL 客戶端 UI 語彙**：金色 CTA 按鈕、六角水晶標誌、卡片金框＋四角飾、
  頂欄金色漸層邊線、能力條金色刻度。
- **字型**：標題改用 Cinzel + Noto Serif TC，正文/按鈕用 Rajdhani + Noto Sans TC。
- **開場畫面重構**：品牌標頭、主視覺、六角表單面板、金漸層標題。
- **自製原創背景** `assets/rift-bg.svg`：召喚水晶＋六角紋理＋地圖路線，
  完全原創、無外部依賴、可離線運作。
- 結算分享圖同步改用官方色票與字型。
- 開場頁新增 **Riot Legal Jibber Jabber 粉絲專案聲明**（授權政策要求）。

### 素材授權
- 依 Riot「Legal Jibber Jabber」政策，本專案為非營利粉絲致敬作品；
  未使用任何 Riot 旗下圖片（英雄美術圖、地圖、圖示、商標）。
  背景為自製 SVG，字型為 OFL 授權（Cinzel / Noto / Rajdhani）。

### 修正
- **數值字體加大**：頂欄狀態（歲／年份／綜合／生涯薪資）與能力值數字放大，
  訓練點剩餘數與骰子一併加大。
- **加點／減點更明顯**：訓練分配每列新增明顯的「＋／−」按鈕（46px 大按鈕），
  點「−」可退回該能力上一次投入；原「復原」改為「復原上一點」。

# 更新日誌 (Changelog)

## [v3.2.0] - 2026-08-11
### 業餘階段：達標就簽，不必熬滿三年

- **不再強制三年業餘**。只要綜合能力達標，隊伍隨時會找上門；三年期滿改成
  「必須做決定」的時點，而不是「才可以開始試訓」的時點。
- **門檻分成三層**：青訓二隊 36、主場一隊 45、海外賽區 47。
  舊版只給「投入主場賽區試訓」一個選項、門檻 45——但實測三年期滿時 OVR 中位數
  只有 37，那個選項的成功率是 **0%**，每個人都是走完一個假的路口再被丟進青訓。
  現在選項依實際達標情況產生，打不到的層級不會出現在畫面上。
- 前三年的邀約可以**婉拒**，留在業餘把數值養高再談，隔年只要仍達標就會再有人來。
- 連青訓門檻都摸不到時可以繼續打網咖盃，但最多撐到 22 歲。
- 實測離開業餘的年份分布（老手打法 300 段）：第 1 年 3%、第 2 年 31%、
  第 3 年 52%、之後 14%。

### 修正
- **二隊名稱時代錯置**：舊版寫死 `['閃電狼二隊', 'ahq Academy', 'PSG Talon Academy',
  '峽谷次級聯隊']`，於是 2013 年就會冒出 PSG Talon Academy——PSG Talon 2020 才成立。
  改為由「當年真實存在的一隊」推導（`橘子熊 Yoe Flash Wolves 二隊`），時代自動正確，
  也不必再維護第二份名單。

### 測試
新增：達標即被挖角、婉拒後隔年仍會被找上門、未達標不會有星探、
只夠青訓的實力不會看到一隊選項、二隊名稱不會時代錯置。

## [v3.0.1] - 2026-08-11
### 內容：事件文本與論壇對話在地化（參考 LOL 事件與梗）

* 重寫 `EVENT_CARDS` 的 good／bad 敘述，加入 LOL 玩家慣用語與網路梗
  （版本答案、版本逆子、0/10/0、666、斗內、梗圖、召喚峽谷、宮鬥劇、人機…），
  更貼近職業賽場與討論區風氣。
  **事件名稱、能力增減、旗標、kind 全部未動**，判斷邏輯不受影響。
* 重寫 `FAN_QUOTES` 為 PTT／巴哈／虎噗風格的論壇留言，加入英雄暱稱
  （盲僧、鱷魚、狐狸、VN、瑟雷西）讓退役留言更像真實討論串。

## [v3.0.0] - 2026-08-11
### 架構重寫：單檔 HTML → 分層 ES 模組

`index.html` 從 987 行縮成純畫面骨架，規則搬進 `src/engine/`、資料搬進 `src/data/`、
畫面搬進 `src/ui/`。零建置、零相依，GitHub Pages 直接可跑。詳見 `ARCHITECTURE.md`。

* 生涯流程由「回呼金字塔」改寫成 **generator 流程機**，一年的時間順序在程式碼裡由上而下讀得完。
* 引擎完全不碰 DOM → 新增 `tests/headless.mjs`，可在 Node 裡跑完整段生涯做回歸測試。
* 新增 `ARCHITECTURE.md`。
* **專案更名整理**：`YaKyoLife-WIKI.md` → `WIKI.md`；移除多餘的 `yakyulife/` 巢狀資料夾
  （repo 內容改為直接放在根目錄，與 GitHub Pages 實際服務的層級一致）；
  v2 時期的改造計劃書移入 `docs/archive/`。GitHub repo 本身的名稱未動。

### 修正：讓遊戲能跑

* **整個遊戲在載入時就會崩潰**。`$` 定義為 `querySelector`，但 `$('log')`、`$('start')`、
  `$('seed-show')`、`$('act')` 等呼叫都少了 `#`，第一次 `card()` 就丟 TypeError。
* `faFlow()` 對 `const stayM` 重新賦值（`stayM = Math.max(...)`），只要持有 `神主牌`
  或 `更衣室傳奇`，合約到期就必定拋 TypeError。而且 `stayM` 算完根本沒被用到。

### 修正：每季旗標沒有重置

* `champThisTeam`（季後賽奪冠）只在換隊時才清除 → 拿過一次冠軍，之後每年的世界賽出線
  機率都永久 +25%。
* `disbandAverted`（改寫史實）永不清除 → 生涯拿過任一座世界冠軍後，往後所有歷史解散
  事件全部自動免疫。
* `injNext`（國際賽消耗）設為 10 之後永不歸零 → 打過一次 MSI，受傷風險永久 +10%。
* 復健年（整季報銷）直接跳過合約結算 → 等於白送一年合約。

### 修正：規則寫反或形同虛設

* **版本補習的好結果反而加重懲罰**：好結果會 `metaCount += 1`，而懲罰正是由 `metaCount`
  推導出來的。改成獨立的「版本落差 `patchDebt`」，補習成功直接降低落差。
* **海外選手一進 FA 就被強制降級**：`faMarket()` 只從主場賽區 `teamListOf('HOME')` 產生報價，
  LCK/LPL 選手合約到期後收不到任何海外報價。改為依當下實力決定候選聯賽。
* **史實解散可以用時序漏洞繞過**：簽約發生在休賽期，但當年解散的戰隊沒有從名單移除，
  於是可以在 2019 年休賽期加入剛倒閉的閃電狼，之後那支隊伍永遠不會觸發解散。
* **列管徵召只鎖三年就永久失效**：`intlLock` 只記錄「第一次被徵召的年份」且從不更新。
* **「連續 3 次」拒絕享樂事件不是連續的**：計數器從不因失敗歸零。
* `單身` 年數同時在兩處累加，實際只需一半時間。
* 洗牌用 `sort(() => R() - 0.5)`，結果依賴 JS 引擎排序實作，跨瀏覽器破壞種子決定論。
  改用 Fisher-Yates。
* 中路 OVR 權重漏掉位置專屬能力 `刺客`，中路 OVR 結構性偏低。五路權重重新校正為總和 1.00。

### 修正：畫面

* 加點介面每次點擊都整排重繪，把剛顯示的「+N」提示立刻洗掉；且沒有任何地方顯示
  「下一點要花幾點」。現已常駐顯示成本與蓄力餘額。
* 潛力刻度線被 `overflow:hidden` 裁掉。
* 結算圖在 `fillText` 之後才設 `textAlign`，標題永遠對不齊；畫布高度寫死 420 但內容只
  畫到 200，下半張全白。改成先量後畫。
* 生涯數據表直接印出內部鍵值（`HOME`／`REG`／`AM`）。
* 開場畫面殘留棒球版的縫線圖示；`maximum-scale=1.0` 擋掉手機縮放。

### 平衡

* **生涯評分重新校準**。舊公式把累積 K／SOLO 直接乘上大係數，任何撐滿十幾季的選手都能
  破 2200，人人都是「傳奇」。新公式壓低「量」的權重、拉高榮譽與巔峰 OVR。
* **年度獎項門檻收緊**。舊版單殺王要求「單殺 ≥ 場次 ×1.2」，但上路／中路的單殺基線本來
  就是每場 1.2–1.3，等於年年必拿；一段生涯能堆出 40 幾項榮譽。改成與同位置基線比較並
  加上擲骰。
* 版本落差改為可被英雄池深度吸收（池深每多 1 隻抵銷 1 點），並設 0–10 上下界。
* 受傷、成長、衰退曲線維持設計文件數值，僅補上 `不老傳奇` 對反應／操作的二次減半。

### 新增：設計文件寫了但沒實作的部分

* **選手資料面板**（右上 ☰）：能力值與潛力刻度、英雄池與專精場次、目前版本與版本落差、
  隊友名單與教練加成、合約狀態、已覺醒的隱藏素質、榮譽紀錄。舊版只有在訓練加點那幾秒
  看得到能力值，英雄池與版本狀態全程沒有出口。
* **存檔續玩**：年初自動存檔，開場畫面出現「繼續上次的生涯」。
* **英雄池會成長**：出賽累積專精，練滿的英雄正式進入比賽池並回饋版本適應力。
* 海外青訓路線真正可走（舊版選了「直接投入海外賽區試訓」其實只是進主場青訓）。
* 新增事件卡：隊內矛盾、海外集訓。
* 新增合約選項：長約／短約的年限與年薪係數會隨表現浮動。
* 年度紀錄可折疊，長生涯不再是無盡卷軸。

## [v2.0.0] - 2026-08-11
### 重大改版：棒球 → LoL 電競生涯模擬
* 全新主題「電競人生」，位置改為五路（上路／打野／中路／射手／輔助）。
* 預設從 S2（2012）開始、16 歲；中度史實演進（GPL→LMS→PCS→LCP、MSI、世界賽 Swiss、薪資隨時代調整）。
* 能力系統重設計：8 共同能力＋位置專屬，位置加權 OVR。
* 新增英雄池（每路 8 隻）與版本 Meta 機制。
* 新增五路數據模擬（K/D/A、CS、視野、傷害、單殺、MVP）。
* 新增 MSI／世界賽（含 Swiss 賽制）與國際賽徵召。
* 新增老將長壽機制：衰退曲線、退役硬上限，可延後至 40。
* 新增歷史解散事件與改寫：解散隊奪世界冠可避免解散；未奪冠強制 FA；FA 不達標強迫退休。
* 新增隱藏特質與特質合成系統（完全隱藏，8 條合成配方）。
* 新增隊友與教練系統。
* 改寫成就、事件庫、命名（試訓／退役／榮譽殿堂）。
* 新增 `ESPORT-DESIGN.md` 並重寫 `README.md`、`YaKyoLife-WIKI.md`。

## [v1.3.3] - 2026-08-09
（棒球版最終更新，詳見 git 歷史。）
